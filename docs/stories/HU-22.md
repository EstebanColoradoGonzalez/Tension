# HU-22 — Transición a plan, rutinas y versiones 100% definidas por el usuario

## Requisitos relacionados

RF04, RF05, RF07, RF08, RF09, RF10, RF11, RF12, RF14, RF16, RF21, RF26, RF30, RF36, RF37, RF39, RF41, RF46, RF49, RF58, RF60, RF62, RF63, RF64 (modificación)
RNF09, RNF14, RNF19, RNF28, RNF29, RNF31

## Descripción

Como ejecutante, necesito que el sistema me permita crear, editar y eliminar cualquier número de rutinas y versiones dentro de mi plan de entrenamiento, asignando ejercicios libremente a cada combinación rutina/versión, para diseñar un plan completamente personalizado que se adapte a mis objetivos, preferencias y disponibilidad — sin estar limitado a una estructura fija de módulos o versiones predefinidas.

El concepto de "módulo fijo" (A, B, C) desaparece y es reemplazado por "rutina", una entidad que el usuario nombra, ordena y configura libremente. Cada rutina puede tener cualquier cantidad de versiones. La lógica de rotación cíclica, microciclo, progresión, fatiga, descarga, KPIs y alertas se adapta automáticamente a la estructura definida por el usuario.

## Criterios de Aceptación

### Gestión de Rutinas

- CA-01: El sistema permite al usuario crear una nueva rutina proporcionando un nombre descriptivo (texto libre, máximo 50 caracteres).
- CA-02: El sistema permite al usuario editar el nombre de una rutina existente en cualquier momento.
- CA-03: El sistema permite al usuario eliminar una rutina, siempre que no sea la única rutina del plan y no tenga una sesión activa en curso. Al eliminar, se solicita confirmación explícita.
- CA-04: El sistema permite al usuario reordenar las rutinas dentro del plan, definiendo así el orden de rotación cíclica.
- CA-05: No existe límite máximo de rutinas que el usuario puede crear.
- CA-06: El plan debe tener al menos una rutina con al menos una versión y al menos un ejercicio asignado para poder iniciar sesiones.

### Gestión de Versiones

- CA-07: El sistema permite al usuario crear una o más versiones dentro de cada rutina.
- CA-08: El sistema permite al usuario eliminar una versión de una rutina, siempre que no sea la única versión de esa rutina. Al eliminar, se solicita confirmación explícita.
- CA-09: Cada versión de una rutina puede tener un conjunto diferente de ejercicios asignados.
- CA-10: La rotación de versiones es independiente por rutina: cada rutina mantiene su propio índice de versión activa.
- CA-11: Al completar un microciclo (una pasada por todas las rutinas), la versión de cada rutina avanza a la siguiente en orden secuencial, volviendo a la primera al agotar todas.

### Asignación de Ejercicios

- CA-12: El sistema permite al usuario asignar cualquier ejercicio del Diccionario (precargado o creado por el usuario) a cualquier versión de cualquier rutina, especificando series (por defecto 4) y rango de repeticiones (por defecto 8-12).
- CA-13: El sistema permite al usuario desasignar (remover) un ejercicio de una versión de una rutina sin eliminar el ejercicio del Diccionario ni afectar su historial.
- CA-14: Un mismo ejercicio puede estar asignado a múltiples rutinas y/o versiones simultáneamente.
- CA-15: Los ejercicios son agnósticos de la rutina: no pertenecen inherentemente a ninguna rutina, sino que se asignan libremente.

### Rotación Cíclica y Microciclo

- CA-16: La rotación cíclica opera sobre las rutinas creadas por el usuario, en el orden definido por él.
- CA-17: Un microciclo se define como una pasada completa por todas las rutinas del plan, independientemente de cuántas sean.
- CA-18: La posición en la rotación de rutinas y la secuencia de versiones se persiste indefinidamente en la base de datos local, sin reiniciarse por ausencias del ejecutante.
- CA-19: Al cerrar una sesión (completada o incompleta), el sistema avanza a la siguiente rutina en la rotación.

### Prescripción y Sesión

- CA-20: Al iniciar una nueva sesión, el sistema muestra la rutina y versión que corresponde según la rotación, con la lista de ejercicios prescritos y la carga objetivo derivada del historial.
- CA-21: Cada registro de serie se asocia automáticamente con la rutina, versión, ejercicio y número de serie, sin intervención manual del usuario.
- CA-22: La sustitución puntual en sesión activa filtra ejercicios por zona muscular (no por rutina ni módulo), ofreciendo cualquier ejercicio del Diccionario de la misma zona que no esté actualmente asignado a la sesión.

### Progresión y Reglas de Negocio

- CA-23: El incremento de carga por Doble Umbral se determina por zona muscular del ejercicio: +2.5 Kg para zonas de tren superior, +5 Kg para zonas de tren inferior, independientemente de la rutina.
- CA-24: La detección de fatiga acumulada se evalúa por rutina (regresión simultánea en ≥ 50% de los ejercicios de una sesión de esa rutina).
- CA-25: La detección de necesidad de descarga se evalúa por rutina (≥ 50% de ejercicios en meseta o regresión en esa rutina).
- CA-26: La descarga se mantiene activa durante un microciclo completo (todas las rutinas del plan), sin cambiar versiones durante el período de descarga.
- CA-27: Las acciones correctivas de meseta incluyen rotar a otra versión de la misma rutina (no de un módulo fijo).

### KPIs y Alertas

- CA-28: El RIR Promedio se calcula por rutina (no por módulo).
- CA-29: La Distribución de Volumen por Zona Muscular se calcula respecto al total de series de la rutina.
- CA-30: El conteo de microciclos se incrementa al completar todas las rutinas del plan en la rotación.
- CA-31: La alerta de inactividad se emite cuando transcurren más de 10 días sin ejecutar una rutina específica (no un módulo), con referencia a las zonas musculares de esa rutina.

### Historial y Trazabilidad

- CA-32: El historial de sesiones pasadas muestra rutina y versión (no módulo) para cada sesión.
- CA-33: La progresión de cada ejercicio se evalúa contra su último registro histórico del mismo ejercicio, independientemente de la rutina o versión en que se ejecutó.
- CA-34: Los datos históricos previos a esta migración se preservan íntegramente; las sesiones anteriores mantienen su referencia al módulo original como identificador de rutina migrada.

### Migración de Datos

- CA-35: Al actualizar la app, los módulos existentes (A, B, C) se migran automáticamente como rutinas con sus nombres originales, preservando todas las versiones, asignaciones de ejercicios, posición en la rotación y datos históricos.
- CA-36: La migración es transparente para el usuario: al abrir la app tras la actualización, su plan existente funciona exactamente igual, pero ahora puede crear nuevas rutinas, editarlas o reorganizarlas.
- CA-37: La migración se ejecuta mediante una migración Room que transforma el esquema sin pérdida de datos.

---

## Análisis Arquitectónico (Arquitecto)

<!-- ============================================================================ -->
<!-- SECCIÓN AGREGADA POR: Workflow analizar-disenar-historia-usuario            -->
<!-- ETAPA: Análisis Arquitectónico                                              -->
<!-- RESPONSABLE: Arquitecto                                                     -->
<!-- FECHA: 2026-05-06                                                           -->
<!-- ESTADO: Refinado (Developer)                                               -->
<!-- ============================================================================ -->

### Decisiones de Diseño

**Patrón Arquitectónico:** MVVM + Domain Layer (4 capas), transformación completa del modelo de dominio — de "módulo fijo" (A, B, C hardcoded) a "rutina dinámica" (entidad definida por el usuario con N rutinas y M versiones por rutina).

**Justificación:** La arquitectura actual (ADR-05) define la separación UI → ViewModel → UseCase → Repository → DAO → Room. HU-22 no introduce un patrón arquitectónico nuevo — la misma separación de 4 capas se mantiene. Lo que cambia es el **modelo de dominio subyacente**: la entidad `module` (3 valores fijos A/B/C, PK tipo String) se reemplaza por `routine` (N valores dinámicos, PK autoincrement Long). Esto propaga cambios a todas las capas porque `module_code` es una FK referenciada por 5 tablas y hardcodeada en 5 clases de lógica de negocio (`RotationResolver`, `SessionRepositoryImpl`, `AlertRepositoryImpl`, `AlertThresholdRule`, evaluaciones de KPIs). La migración Room v8→v9 transforma datos existentes sin pérdida (CA-35, CA-36, CA-37). El enfoque exploratorio confirmó que el delta entre el modelo actual (v8) y el modelo objetivo (documentación actualizada) es extenso pero estructuralmente predecible — cada cambio sigue el patrón de reemplazar una referencia fija por una dinámica.

**Delta entre modelo actual (v8) y modelo objetivo:**

| Concepto actual (v8) | Concepto nuevo (v9) | Tipo de cambio |
| --- | --- | --- |
| `module` (tabla, PK=code String) | `routine` (tabla, PK=id Long autoincrement) | Nueva tabla, tabla anterior eliminada |
| `module_version` (FK→module.code) | `routine_version` (FK→routine.id) | Nueva tabla, tabla anterior eliminada |
| `exercise.module_code` FK→module | *(columna eliminada)* | Ejercicios agnósticos a la rutina (CA-15) |
| `module.load_increment_kg` | Calculado por zona muscular: tren sup=2.5, inf=5.0 (CA-23) | Lógica migra de tabla a regla de negocio |
| `rotation_state.current_version_module_a/b/c` | `routine_current_version` (tabla dinámica, 1 fila por rutina) | Nueva tabla, columnas fijas eliminadas |
| `deload.frozen_version_module_a/b/c` | `deload_frozen_version` (tabla dinámica, 1 fila por rutina por deload) | Nueva tabla, columnas fijas eliminadas |
| `alert.module_code` FK→module | `alert.routine_id` FK→routine | FK migra de String a Long |
| `session.module_version_id` FK→module_version | `session.routine_version_id` FK→routine_version | FK rename (IDs preservados) |
| `plan_assignment.module_version_id` | `plan_assignment.routine_version_id` | FK rename (IDs preservados) |
| `RotationResolver` (A/B/C hardcoded, 6 posiciones) | `RotationResolver` (N rutinas, N posiciones) | Rewrite completo |
| `ModuleSeeder`, `PlanSeeder` | *(no se invocan en v9+)* | Seeders obsoletos para nuevas instalaciones |

**Componentes Afectados:**

**Bloque A — Migración Room v8→v9 (MIGRATION_8_9)**

- **`MIGRATION_8_9` en `Migrations.kt` (Nuevo):** Migración SQL pura que transforma el esquema de módulos fijos a rutinas dinámicas sin pérdida de datos. Ejecutada en 4 fases transaccionales:
  - Nivel de cambio: Crítico
  - Especificaciones:

    **Fase 1 — Crear nuevas tablas:**

    ```sql
    CREATE TABLE routine (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL UNIQUE,
        sort_order INTEGER NOT NULL CHECK(sort_order >= 1),
        created_at TEXT NOT NULL
    );

    CREATE TABLE routine_version (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        routine_id INTEGER NOT NULL REFERENCES routine(id) ON DELETE CASCADE,
        version_number INTEGER NOT NULL CHECK(version_number >= 1),
        UNIQUE(routine_id, version_number)
    );
    CREATE INDEX idx_routine_version_routine_id ON routine_version(routine_id);

    CREATE TABLE routine_current_version (
        routine_id INTEGER PRIMARY KEY REFERENCES routine(id) ON DELETE CASCADE,
        current_version_number INTEGER NOT NULL DEFAULT 1 CHECK(current_version_number >= 1)
    );

    CREATE TABLE deload_frozen_version (
        deload_id INTEGER NOT NULL REFERENCES deload(id) ON DELETE CASCADE,
        routine_id INTEGER NOT NULL REFERENCES routine(id) ON DELETE RESTRICT,
        frozen_version_number INTEGER NOT NULL CHECK(frozen_version_number >= 1),
        PRIMARY KEY(deload_id, routine_id)
    );
    ```

    **Fase 2 — Poblar desde datos existentes (CA-35):**

    ```sql
    -- Módulos → Rutinas (preservar semántica)
    INSERT INTO routine (id, name, sort_order, created_at)
    VALUES (1, 'Pull + Abs', 1, date('now')),
           (2, 'Push', 2, date('now')),
           (3, 'Pierna', 3, date('now'));

    -- module_version → routine_version (preservar IDs exactos para mantener FKs)
    INSERT INTO routine_version (id, routine_id, version_number)
    SELECT mv.id,
           CASE mv.module_code WHEN 'A' THEN 1 WHEN 'B' THEN 2 WHEN 'C' THEN 3 END,
           mv.version_number
    FROM module_version mv;

    -- rotation_state → routine_current_version
    INSERT INTO routine_current_version (routine_id, current_version_number)
    SELECT 1, current_version_module_a FROM rotation_state WHERE id = 1
    UNION ALL
    SELECT 2, current_version_module_b FROM rotation_state WHERE id = 1
    UNION ALL
    SELECT 3, current_version_module_c FROM rotation_state WHERE id = 1;

    -- deload → deload_frozen_version (si hay deloads)
    INSERT INTO deload_frozen_version (deload_id, routine_id, frozen_version_number)
    SELECT id, 1, frozen_version_module_a FROM deload
    UNION ALL
    SELECT id, 2, frozen_version_module_b FROM deload
    UNION ALL
    SELECT id, 3, frozen_version_module_c FROM deload;
    ```

    **Fase 3 — Migrar tablas con FKs (recrear con nuevo esquema):**

    ```sql
    -- session: module_version_id → routine_version_id (IDs iguales, solo rename)
    CREATE TABLE session_new (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        routine_version_id INTEGER NOT NULL REFERENCES routine_version(id) ON DELETE RESTRICT,
        deload_id INTEGER REFERENCES deload(id) ON DELETE RESTRICT,
        date TEXT NOT NULL,
        status TEXT NOT NULL DEFAULT 'IN_PROGRESS'
            CHECK(status IN ('IN_PROGRESS', 'COMPLETED', 'INCOMPLETE'))
    );
    INSERT INTO session_new SELECT id, module_version_id, deload_id, date, status FROM session;
    DROP TABLE session;
    ALTER TABLE session_new RENAME TO session;
    CREATE INDEX idx_session_date ON session(date);
    CREATE INDEX idx_session_routine_version_id ON session(routine_version_id);
    CREATE INDEX idx_session_status ON session(status);
    CREATE INDEX idx_session_deload_id ON session(deload_id);

    -- plan_assignment: module_version_id → routine_version_id
    CREATE TABLE plan_assignment_new (
        routine_version_id INTEGER NOT NULL REFERENCES routine_version(id) ON DELETE CASCADE,
        exercise_id INTEGER NOT NULL REFERENCES exercise(id) ON DELETE RESTRICT,
        sets INTEGER NOT NULL CHECK(sets > 0),
        reps TEXT NOT NULL CHECK(reps IN ('8-12', 'TO_TECHNICAL_FAILURE', '30-45_SEC')),
        sort_order INTEGER NOT NULL DEFAULT 0,
        PRIMARY KEY(routine_version_id, exercise_id)
    );
    INSERT INTO plan_assignment_new
    SELECT module_version_id, exercise_id, sets, reps, sort_order FROM plan_assignment;
    DROP TABLE plan_assignment;
    ALTER TABLE plan_assignment_new RENAME TO plan_assignment;
    CREATE INDEX idx_plan_assignment_exercise_id ON plan_assignment(exercise_id);

    -- exercise: eliminar module_code (ejercicios agnósticos CA-15)
    CREATE TABLE exercise_new (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        equipment_type_id INTEGER NOT NULL REFERENCES equipment_type(id) ON DELETE RESTRICT,
        is_bodyweight INTEGER NOT NULL DEFAULT 0,
        is_isometric INTEGER NOT NULL DEFAULT 0,
        is_to_technical_failure INTEGER NOT NULL DEFAULT 0,
        is_custom INTEGER NOT NULL DEFAULT 0,
        media_resource TEXT
    );
    INSERT INTO exercise_new
    SELECT id, name, equipment_type_id, is_bodyweight, is_isometric,
           is_to_technical_failure, is_custom, media_resource FROM exercise;
    DROP TABLE exercise;
    ALTER TABLE exercise_new RENAME TO exercise;
    CREATE UNIQUE INDEX idx_exercise_name_equipment ON exercise(name, equipment_type_id);
    CREATE INDEX idx_exercise_equipment_type_id ON exercise(equipment_type_id);

    -- alert: module_code → routine_id
    CREATE TABLE alert_new (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        type TEXT NOT NULL,
        level TEXT NOT NULL,
        exercise_id INTEGER REFERENCES exercise(id) ON DELETE RESTRICT,
        routine_id INTEGER REFERENCES routine(id) ON DELETE CASCADE,
        muscle_group TEXT,
        message TEXT NOT NULL,
        is_active INTEGER NOT NULL DEFAULT 1,
        created_at TEXT NOT NULL,
        resolved_at TEXT
    );
    INSERT INTO alert_new (id, type, level, exercise_id, routine_id, muscle_group,
                           message, is_active, created_at, resolved_at)
    SELECT a.id, a.type, a.level, a.exercise_id,
           CASE a.module_code WHEN 'A' THEN 1 WHEN 'B' THEN 2 WHEN 'C' THEN 3 ELSE NULL END,
           a.muscle_group, a.message, a.is_active, a.created_at, a.resolved_at
    FROM alert a;
    DROP TABLE alert;
    ALTER TABLE alert_new RENAME TO alert;
    CREATE INDEX idx_alert_is_active ON alert(is_active);
    CREATE INDEX idx_alert_type ON alert(type);
    CREATE INDEX idx_alert_exercise_id ON alert(exercise_id);
    CREATE INDEX idx_alert_routine_id ON alert(routine_id);

    -- deload: eliminar frozen_version_module_a/b/c
    CREATE TABLE deload_new (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        status TEXT NOT NULL DEFAULT 'ACTIVE' CHECK(status IN ('ACTIVE', 'COMPLETED')),
        activation_date TEXT NOT NULL,
        completion_date TEXT
    );
    INSERT INTO deload_new SELECT id, status, activation_date, completion_date FROM deload;
    DROP TABLE deload;
    ALTER TABLE deload_new RENAME TO deload;
    CREATE INDEX idx_deload_status ON deload(status);

    -- rotation_state: eliminar current_version_module_a/b/c
    CREATE TABLE rotation_state_new (
        id INTEGER PRIMARY KEY DEFAULT 1 CHECK(id = 1),
        microcycle_position INTEGER NOT NULL DEFAULT 1 CHECK(microcycle_position >= 1),
        microcycle_count INTEGER NOT NULL DEFAULT 0 CHECK(microcycle_count >= 0)
    );
    INSERT INTO rotation_state_new
    SELECT id, microcycle_position, microcycle_count FROM rotation_state;
    DROP TABLE rotation_state;
    ALTER TABLE rotation_state_new RENAME TO rotation_state;
    ```

    **Fase 4 — Limpiar tablas obsoletas:**

    ```sql
    DROP TABLE module_version;
    DROP TABLE module;
    ```

    Nota: Los IDs de `routine_version` se insertan explícitamente con los mismos valores que `module_version.id` → las FKs de `session` y `plan_assignment` siguen válidas sin necesidad de UPDATE en cada fila.

- **`TensionDatabase` (Modificación):** Actualizar entities array: reemplazar `ModuleEntity`, `ModuleVersionEntity` por `RoutineEntity`, `RoutineVersionEntity`, `RoutineCurrentVersionEntity`, `DeloadFrozenVersionEntity`. Actualizar campos modificados en `ExerciseEntity`, `SessionEntity`, `PlanAssignmentEntity`, `AlertEntity`, `DeloadEntity`, `RotationStateEntity`. Actualizar DAOs: reemplazar `moduleDao()`, `moduleVersionDao()` por `routineDao()`, `routineVersionDao()`, `routineCurrentVersionDao()`, `deloadFrozenVersionDao()`. Incrementar version a 9. Agregar `MIGRATION_8_9` al builder.
  - Nivel de cambio: Crítico

**Bloque B — Entidades Room (4 nuevas, 6 modificadas, 2 eliminadas)**

- **`RoutineEntity` (Nuevo):** Tabla `routine` con campos: `id` (PK autoincrement), `name` (TEXT UNIQUE), `sortOrder` (INT), `createdAt` (TEXT).
  - Ubicación: `data/local/entity/RoutineEntity.kt`

- **`RoutineVersionEntity` (Nuevo):** Tabla `routine_version` con campos: `id` (PK autoincrement), `routineId` (FK→routine), `versionNumber` (INT). Unique(routineId, versionNumber).
  - Ubicación: `data/local/entity/RoutineVersionEntity.kt`

- **`RoutineCurrentVersionEntity` (Nuevo):** Tabla `routine_current_version` con campos: `routineId` (PK, FK→routine), `currentVersionNumber` (INT DEFAULT 1).
  - Ubicación: `data/local/entity/RoutineCurrentVersionEntity.kt`

- **`DeloadFrozenVersionEntity` (Nuevo):** Tabla `deload_frozen_version` con PK compuesta: `deloadId` (FK→deload), `routineId` (FK→routine), campo `frozenVersionNumber` (INT).
  - Ubicación: `data/local/entity/DeloadFrozenVersionEntity.kt`

- **`ExerciseEntity` (Modificación):** Eliminar campo `moduleCode` y su FK a `module`. Eliminar índice en `module_code`. Ejercicios son agnósticos a la rutina (CA-15).
  - Nivel de cambio: Mayor

- **`SessionEntity` (Modificación):** FK cambia de `moduleVersionId` a `routineVersionId`. Tabla destino cambia de `module_version` a `routine_version`.
  - Nivel de cambio: Menor (rename semántico)

- **`PlanAssignmentEntity` (Modificación):** FK y PK compuesta cambian de `moduleVersionId` a `routineVersionId`.
  - Nivel de cambio: Menor (rename semántico)

- **`AlertEntity` (Modificación):** Campo `moduleCode: String?` (FK→module) cambia a `routineId: Long?` (FK→routine).
  - Nivel de cambio: Mayor (cambio de tipo)

- **`DeloadEntity` (Modificación):** Eliminar campos `frozenVersionModuleA`, `frozenVersionModuleB`, `frozenVersionModuleC`. Las versiones congeladas ahora residen en `deload_frozen_version`.
  - Nivel de cambio: Mayor

- **`RotationStateEntity` (Modificación):** Eliminar campos `currentVersionModuleA`, `currentVersionModuleB`, `currentVersionModuleC`. Las versiones actuales ahora residen en `routine_current_version`.
  - Nivel de cambio: Mayor

- **`ModuleEntity` (Eliminada):** Tabla `module` ya no existe en v9.
- **`ModuleVersionEntity` (Eliminada):** Tabla `module_version` ya no existe en v9 (reemplazada por `routine_version`).

**Bloque C — DAOs (4 nuevos, 5 modificados, 2 eliminados)**

- **`RoutineDao` (Nuevo):** CRUD completo para rutinas. Queries: `getAll(): Flow<List<RoutineEntity>>` (ORDER BY sort_order), `getById(id): RoutineEntity?`, `insert(routine): Long`, `update(routine)`, `delete(id)`, `getMaxSortOrder(): Int?`, `countRoutines(): Int`, `updateSortOrder(id, sortOrder)`.
  - Ubicación: `data/local/dao/RoutineDao.kt`

- **`RoutineVersionDao` (Nuevo):** Queries: `getByRoutineId(routineId): Flow<List<RoutineVersionEntity>>`, `getById(id): Flow<RoutineVersionEntity?>`, `getByRoutineIdAndVersion(routineId, versionNumber): RoutineVersionEntity?`, `insert(version): Long`, `delete(id)`, `countByRoutineId(routineId): Int`, `getMaxVersionNumber(routineId): Int?`.
  - Ubicación: `data/local/dao/RoutineVersionDao.kt`

- **`RoutineCurrentVersionDao` (Nuevo):** Queries: `getAll(): Flow<List<RoutineCurrentVersionEntity>>`, `getAllOnce(): List<RoutineCurrentVersionEntity>`, `getByRoutineId(routineId): RoutineCurrentVersionEntity?`, `insert(entity)`, `update(entity)`, `deleteByRoutineId(routineId)`.
  - Ubicación: `data/local/dao/RoutineCurrentVersionDao.kt`

- **`DeloadFrozenVersionDao` (Nuevo):** Queries: `getByDeloadId(deloadId): List<DeloadFrozenVersionEntity>`, `insertAll(entities)`, `deleteByDeloadId(deloadId)`.
  - Ubicación: `data/local/dao/DeloadFrozenVersionDao.kt`

- **`PlanAssignmentDao` (Modificación):** Todas las queries con `module_version_id` cambian a `routine_version_id`. `getPreviewByModuleVersionId` → `getPreviewByRoutineVersionId`. El JOIN con `module m ON e.module_code = m.code` para obtener `loadIncrementKg` se elimina — `loadIncrementKg` se calcula en capa de aplicación por `LoadIncrementResolver`. DTOs: `SessionPreviewExerciseDto` pierde `loadIncrementKg` (se resuelve en Repository). `PlanAssignmentWithExerciseDetails` pierde `moduleCode` (se agrega `routineId` si es necesario, o se obtiene de la ruta de navegación).
  - Nivel de cambio: Mayor

- **`SessionDao` (Modificación):** DTOs `ActiveSessionInfo`, `SessionSummaryInfo`, `ClosedSessionDto` cambian `moduleCode` → `routineName`, `moduleVersionId` joins cambian de `module_version` a `routine_version` y agregan JOIN con `routine`. `getSessionIdsByModuleInRange` → `getSessionIdsByRoutineInRange(routineId, limit)`. `getLastSessionDateByModule` → `getLastSessionDateByRoutine(routineId)`.
  - Nivel de cambio: Mayor

- **`ExerciseDao` (Modificación):** DTO `ExerciseWithDetails` pierde `moduleCode`, `moduleName`. Queries eliminan JOIN con `module`. `getByModuleCodeNotInVersion` se elimina — reemplazado por `getEligibleForVersion(routineVersionId)` que no filtra por módulo. `getEligibleSubstitutesForSession` cambia: filtra por zona muscular en vez de `module_code` (CA-22). Nuevo query recibe lista de `muscleZoneIds` y excluye ejercicios ya en sesión.
  - Nivel de cambio: Mayor

- **`SessionExerciseDao` (Modificación):** DTOs con `moduleCode` cambian a `routineId` o lo eliminan. `SessionExerciseForProgression` pierde `moduleCode`, obtiene `routineId` del JOIN. JOINs con `module` para `loadIncrementKg` se eliminan — `loadIncrementKg` se resuelve en capa de aplicación. `ExerciseHistoryEntryDto` cambia `moduleCode` → `routineName`, `versionNumber` se mantiene.
  - Nivel de cambio: Mayor

- **`AlertDao` (Modificación):** `existsActiveByModule(moduleCode)` → `existsActiveByRoutine(routineId: Long)`. `resolveByModuleAndType(moduleCode)` → `resolveByRoutineAndType(routineId: Long)`. Queries WHERE cambian de `module_code = :moduleCode` a `routine_id = :routineId`.
  - Nivel de cambio: Menor

- **`ModuleDao` (Eliminado):** Ya no existe — la tabla `module` se eliminó.
- **`ModuleVersionDao` (Eliminado):** Ya no existe — reemplazado por `RoutineVersionDao`.

**Bloque D — Modelos de Dominio y Reglas (5 modificados, 3 nuevos)**

- **`Routine` — Modelo de dominio (Nuevo):** data class con campos: `id: Long`, `name: String`, `sortOrder: Int`, `createdAt: String`.
  - Ubicación: `domain/model/Routine.kt`

- **`RoutineVersion` — Modelo de dominio (Nuevo):** data class con campos: `id: Long`, `routineId: Long`, `versionNumber: Int`.
  - Ubicación: `domain/model/RoutineVersion.kt`

- **`LoadIncrementResolver` — Regla de negocio (Nuevo):** Reemplaza `module.load_increment_kg`. Determina el incremento mínimo de carga basado en la zona muscular principal del ejercicio (CA-23): tren superior (Pecho, Espalda, Abdomen, Hombro, Tríceps, Bíceps) → 2.5 Kg, tren inferior (Cuádriceps, Isquiotibiales, Glúteos, Aductores, Abductores, Gemelos) → 5.0 Kg. Recibe `muscleGroup: String` desde `muscle_zone.muscle_group` (via `exercise_muscle_zone`). Fallback a 2.5 si el grupo no es reconocido.
  - Ubicación: `domain/rules/LoadIncrementResolver.kt`
  - Especificaciones:

    ```kotlin
    object LoadIncrementResolver {
        private val LOWER_BODY_GROUPS = setOf(
            "Cuádriceps", "Isquiotibiales", "Glúteos",
            "Aductores", "Abductores", "Gemelos",
        )

        fun resolve(muscleGroup: String): Double =
            if (muscleGroup in LOWER_BODY_GROUPS) 5.0 else 2.5
    }
    ```

- **`NextSession` (Modificación):** `moduleCode: String` → `routineId: Long`, `routineName: String` agregado, `moduleVersionId: Long` → `routineVersionId: Long`.
  - Nivel de cambio: Mayor

- **`ActiveSession` (Modificación):** `moduleCode: String` → `routineName: String`.
  - Nivel de cambio: Menor

- **`RotationState` (Modificación):** Eliminar `currentVersionModuleA/B/C`. Solo mantiene `microcyclePosition: Int`, `microcycleCount: Int`.
  - Nivel de cambio: Mayor

- **`Deload` (Modificación):** Eliminar `frozenVersionModuleA/B/C`. Agregar `frozenVersions: Map<Long, Int>` (routineId → versionNumber) que se puebla desde `deload_frozen_version`.
  - Nivel de cambio: Mayor

- **`DeloadState` (Modificación):** `DeloadActive`: `frozenVersionA/B/C: Int` → `frozenVersions: Map<Long, Int>`. `DeloadRequired`: `modules: List<String>` → `routineIds: List<Long>`.
  - Nivel de cambio: Mayor

- **`RotationResolver` (Modificación — Rewrite completo):** Ya no usa A/B/C hardcoded ni posiciones 1-6 fijas. Opera con N rutinas dinámicas:
  - `resolveRoutineId(position: Int, routines: List<Routine>): Long` — indexa por sort_order
  - `resolveVersionNumber(routineId: Long, currentVersions: Map<Long, Int>): Int` — lookup directo
  - `advanceRotation(position: Int, totalRoutines: Int, currentVersions: Map<Long, Int>, versionCounts: Map<Long, Int>, isDeload: Boolean): AdvanceResult` — retorna nuevo `microcyclePosition`, nuevo `microcycleCount`, y `updatedVersions: Map<Long, Int>` (con wrap-around `(current % count) + 1` por rutina)
  - Nivel de cambio: Crítico

- **Modelos de dominio adicionales con `moduleCode` (15 modelos — Modificación global):** Los siguientes domain models/DTOs contienen `moduleCode: String` que debe cambiar a `routineId: Long` o `routineName: String`: `Exercise`, `ExerciseHistoryEntry`, `SessionHistoryItem`, `SessionDetail`, `SessionSummary`, `SessionPreviewExercise`, `PlanVersionDetail`, `SubstituteExerciseInfo`, `AlertTriggerData.RirTrigger`, `AlertTriggerData.InactivityTrigger`, `ModuleInactivityData`, `DeloadHomeState.Active`, `DeloadHomeState.Required`, `RirByModule`, `SetDistributionData`. Cada uno pierde su campo `moduleCode` y recibe `routineId` o `routineName` según el contexto de uso (display vs FK).
  - Nivel de cambio: Mayor (propagación masiva pero mecánica)

**Bloque E — Repository (1 rewrite masivo, 4 modificados)**

- **`SessionRepositoryImpl` (Modificación — 1040 líneas → ~1100 líneas):** Migrar toda la lógica de módulos fijos a rutinas dinámicas. Cambios principales:
  - **Constructor:** Eliminar `moduleDao: ModuleDao`, `moduleVersionDao: ModuleVersionDao` (de las 13 dependencias inyectadas: 12 DAOs + 1 TensionDatabase). Agregar `routineDao: RoutineDao`, `routineVersionDao: RoutineVersionDao`, `routineCurrentVersionDao: RoutineCurrentVersionDao`, `deloadFrozenVersionDao: DeloadFrozenVersionDao`.
  - **`getNextModuleVersionId()`** → **`getNextRoutineVersionId()`**: Ya no usa `RotationResolver.resolveModuleCode()` con posición fija. Consulta `routineDao.getAll()` para obtener rutinas ordenadas, luego `routineCurrentVersionDao` para obtener versión actual, luego `routineVersionDao.getByRoutineIdAndVersion()` para obtener el ID.
  - **`startSession()`**: Cambia `moduleVersionId` → `routineVersionId` en `SessionEntity`.
  - **`closeSession()`**: La lógica de `advanceRotation` ahora consulta `routineCurrentVersionDao.getAllOnce()` para versiones actuales y `routineVersionDao.countByRoutineId()` para total de versiones por rutina. El advance opera sobre N rutinas.
  - **`activateDeload()`**: En vez de escribir 3 columnas fijas en `DeloadEntity`, copia versiones actuales de `routine_current_version` a `deload_frozen_version` (una fila por rutina).
  - **`getActiveDeload()`**: Mapper ahora consulta `deloadFrozenVersionDao.getByDeloadId()` para poblar `frozenVersions: Map`.
  - **`getDeloadState()`**: `DeloadActive` usa `frozenVersions: Map<Long, Int>` en vez de 3 ints.
  - **Deload finalization** (en `closeSession`): El conteo de sesiones para completar deload ya no es fijo a 6 — es `routineDao.countRoutines()` multiplicado por la frecuencia del microciclo (1 sesión por rutina = N sesiones totales, donde N = número de rutinas).
  - **`evaluateRirOutOfRange()`**: Ya no itera sobre `listOf("A", "B", "C")`. Itera sobre todas las rutinas de `routineDao.getAll()`.
  - **`evaluateModuleInactivity()`** → **`evaluateRoutineInactivity()`**: Itera sobre todas las rutinas. Las zonas musculares se obtienen dinámicamente de las asignaciones de la rutina (via plan_assignment + exercise_muscle_zone), no de un mapa hardcodeado.
  - **`getSessionPreviewExercises()`**: `moduleVersionId` → `routineVersionId`. `loadIncrementKg` se resuelve con `LoadIncrementResolver` en el mapper en vez del JOIN con `module`.
  - **`AlertThresholdRule.MUSCLE_GROUPS_BY_MODULE`** — Mapa hardcodeado eliminado. Los grupos musculares de una rutina se calculan dinámicamente.
  - Nivel de cambio: Crítico

- **`SessionRepository` (Interfaz — Modificación):** Renombrar métodos con `Module` → `Routine`. `getNextModuleVersionId()` → `getNextRoutineVersionId()`. `getSessionPreviewExercises(moduleVersionId)` → `getSessionPreviewExercises(routineVersionId)`. Agregar métodos para CRUD de rutinas si se decide centralizar aquí, o crear `RoutineRepository` separado.
  - Nivel de cambio: Mayor

- **`AlertRepositoryImpl` (Modificación):** Usa `moduleCode` en 10+ puntos. Llama `sessionDao.getSessionIdsByModuleInRange(moduleCode)`, usa `AlertThresholdRule.MUSCLE_GROUPS_BY_MODULE`, renderiza "Módulo ${entity.moduleCode}" en mensajes. Todos cambian a `routineId: Long` y obtención dinámica de grupos musculares.
  - Nivel de cambio: Mayor

- **`MetricsRepositoryImpl` (Modificación):** Recibe `moduleCode: String` como parámetro en métodos de KPIs. Llama `getSessionIdsByModuleInRange`. Cambia a `routineId: Long`.
  - Nivel de cambio: Mayor

- **`ExerciseRepositoryImpl` (Modificación):** Tiene `moduleCode: String` como parámetro en 2 métodos de filtrado. Pasa `moduleCode` a `ExerciseDao`. Eliminar parámetro (ejercicios ya son agnósticos) o cambiar a `routineVersionId` para contexto de asignación.
  - Nivel de cambio: Menor

- **`PlanRepositoryImpl` (Modificación):** Agrupa por `moduleCode`, mapea nombres de módulo, llama `getByModuleCodeNotInVersion`. Toda la interfaz `PlanRepository` cambia: `getModulesWithVersionCounts()` → `getRoutinesWithVersionCounts()`, `assignExercise(moduleVersionId, ...)` → `assignExercise(routineVersionId, ...)`. Considerar absorber en un nuevo `RoutineRepository` o mantener separado.
  - Nivel de cambio: Mayor

**Bloque F — UseCases (6 nuevos, 6+ modificados)**

**Nuevos (gestión del plan — CA-01 a CA-08):**

- **`CreateRoutineUseCase`** — Valida nombre (max 50 chars, no vacío, no duplicado). Crea rutina con `sortOrder = MAX + 1`. Crea versión V1 automática. Crea `routine_current_version` con valor 1.
  - Ubicación: `domain/usecase/routine/CreateRoutineUseCase.kt`

- **`UpdateRoutineNameUseCase`** — Edita nombre de rutina existente (CA-02). Valida unicidad.
  - Ubicación: `domain/usecase/routine/UpdateRoutineNameUseCase.kt`

- **`DeleteRoutineUseCase`** — Valida: `countRoutines() > 1` (CA-03), no hay sesión activa con esta rutina. Cascade delete via Room (`ON DELETE CASCADE` en `routine_version`, `plan_assignment`, `routine_current_version`). Reordena `sort_order` de rutinas restantes.
  - Ubicación: `domain/usecase/routine/DeleteRoutineUseCase.kt`

- **`ReorderRoutinesUseCase`** — Recibe lista ordenada de routineIds, actualiza `sort_order` de cada una (CA-04). Actualiza `microcycle_position` en `rotation_state` si es necesario recalibrar.
  - Ubicación: `domain/usecase/routine/ReorderRoutinesUseCase.kt`

- **`CreateRoutineVersionUseCase`** — Crea versión `V(MAX + 1)` para una rutina (CA-07).
  - Ubicación: `domain/usecase/routine/CreateRoutineVersionUseCase.kt`

- **`DeleteRoutineVersionUseCase`** — Valida: `countByRoutineId() > 1` (CA-08). Cascade delete via Room.
  - Ubicación: `domain/usecase/routine/DeleteRoutineVersionUseCase.kt`

**Existentes — Modificados (CA-12 a CA-15):**

- **`AssignExerciseToVersionUseCase`** (ya existe en `domain/usecase/plan/`) — Parámetro cambia de `moduleVersionId` a `routineVersionId` (CA-12). Resto de la lógica se mantiene.

- **`RemoveExerciseFromVersionUseCase`** (ya existe como `UnassignExerciseFromVersionUseCase` en `domain/usecase/plan/`) — Parámetro cambia de `moduleVersionId` a `routineVersionId` (CA-13). Resto de la lógica se mantiene.

**Modificados:**

- **`GetNextSessionInfoUseCase`** — Usa nuevo `RotationResolver` con rutinas dinámicas y `routineCurrentVersionDao`. Retorna `NextSession` con `routineId`, `routineName`, `routineVersionId`.

- **`CloseSessionUseCase`** — Sin cambio directo (delega a `sessionRepository.closeSession()`), pero la implementación interna cambia completamente.

- **`ActivateDeloadUseCase`** — Sin cambio directo (delega a `sessionRepository.activateDeload()`), pero la implementación interna ahora escribe en `deload_frozen_version`.

- **`GetDeloadStateUseCase`** — Sin cambio de interfaz pero el `DeloadState` retornado usa `Map<Long, Int>`.

**Bloque G — UI: Nuevas pantallas de gestión del plan**

- **`RoutineListScreen` + `RoutineListViewModel` (Nuevo):** Pantalla con lista de rutinas en orden de `sort_order`. Drag-and-drop para reordenar (CA-04). Acciones por item: editar nombre (inline o dialog), eliminar (con confirmación CA-03). FAB "Crear rutina" (CA-01). Tap rutina → navega a versiones. CenterAlignedTopAppBar con título "Mi Plan". Bottom Nav visible.
  - Ubicación: `ui/routine/RoutineListScreen.kt`, `ui/routine/RoutineListViewModel.kt`

- **`RoutineVersionListScreen` + `RoutineVersionListViewModel` (Nuevo):** Lista de versiones de una rutina con conteo de ejercicios. Acciones: crear versión (CA-07), eliminar versión con confirmación (CA-08). Tap versión → navega al detalle (PlanVersionDetailScreen existente, reparametrizado con `routineVersionId`). TopAppBar con `routineName`.
  - Ubicación: `ui/routine/RoutineVersionListScreen.kt`, `ui/routine/RoutineVersionListViewModel.kt`

- **Modificación de `PlanVersionDetailScreen`/`PlanVersionDetailViewModel`:** Ya existe (HU-04). Cambia `moduleVersionId` → `routineVersionId`. Se agrega capacidad de **asignar** y **remover** ejercicios (CA-12, CA-13) — actualmente es solo lectura. Nuevo botón "Agregar ejercicio" que abre selector.

- **`ExerciseSelectorScreen` + `ExerciseSelectorViewModel` (Nuevo):** Bottom sheet o pantalla que muestra ejercicios del diccionario filtrados (excluye los ya asignados a la versión). Permite seleccionar un ejercicio para asignarlo. Campos de series (default 4) y reps (default '8-12') editables.
  - Ubicación: `ui/routine/ExerciseSelectorScreen.kt`, `ui/routine/ExerciseSelectorViewModel.kt`

**Bloque H — UI: Pantallas existentes modificadas**

- **`HomeScreen`/`HomeViewModel`:** `NextSessionCard` muestra nombre de rutina en vez de código de módulo. `moduleCode` → `routineName` en `HomeUiState`.
- **`ActiveSessionViewModel`/`ActiveSessionScreen`:** `moduleCode` → `routineName` en header.
- **`SessionPreviewViewModel`/`SessionPreviewScreen`:** `moduleCode` → `routineName`. `loadIncrementKg` calculado por `LoadIncrementResolver` en vez de obtenido del DTO.
- **`SessionSummaryViewModel`:** `moduleCode` → `routineName`.
- **`SessionHistoryScreen`:** `moduleCode` → `routineName` en cada item.
- **`SessionDetailScreen`:** `moduleCode` → `routineName`.
- **`SubstituteExerciseViewModel`:** Filtro cambia de `module_code` a zona muscular (CA-22).
- **`ExerciseDictionaryScreen`:** Sin `moduleCode`/`moduleName` en ejercicios. Grouping por zona muscular o alfabético.
- **`ExerciseDetailScreen`/`ExerciseDetailViewModel`:** Eliminar `moduleCode` y `moduleName` de display (ejercicios agnósticos, CA-15).
- **`CreateExerciseScreen`/`CreateExerciseViewModel`:** Eliminar selector de módulo (dropdown A/B/C). Ejercicios se crean sin `moduleCode` (CA-15).
- **`TrainingPlanScreen`/`TrainingPlanViewModel` (Eliminados):** Reemplazados por `RoutineListScreen` + `RoutineVersionListScreen`.
- **`AlertsScreen`:** `moduleCode` → `routineName`.
- **`TensionNavHost`/`NavigationRoutes`:** Rutas con `moduleVersionId`/`moduleCode` → `routineVersionId`/`routineId`. Nuevas rutas para gestión de rutinas y versiones.

**Hitos de Implementación:**

1. **Migración Room v8→v9 + Entidades nuevas/modificadas** — `MIGRATION_8_9` (SQL), `RoutineEntity`, `RoutineVersionEntity`, `RoutineCurrentVersionEntity`, `DeloadFrozenVersionEntity`. Entidades modificadas: `ExerciseEntity`, `SessionEntity`, `PlanAssignmentEntity`, `AlertEntity`, `DeloadEntity`, `RotationStateEntity`. `TensionDatabase` v9. Test de migración instrumentado.
   - Dependencias: Ninguna
   - Nota: Este hito es **bloqueante** para todos los demás. La migración debe ser exhaustivamente testeada antes de continuar.

2. **DAOs nuevos + DAOs modificados** — `RoutineDao`, `RoutineVersionDao`, `RoutineCurrentVersionDao`, `DeloadFrozenVersionDao`. Modificar `PlanAssignmentDao`, `SessionDao`, `ExerciseDao`, `SessionExerciseDao`, `AlertDao`. Eliminar `ModuleDao`, `ModuleVersionDao`.
   - Dependencias: Hito 1

3. **`LoadIncrementResolver` + `RotationResolver` rewrite + Modelos de dominio** — `LoadIncrementResolver`, `RotationResolver` reescrito para N rutinas. Modelos: `Routine`, `RoutineVersion`, `NextSession`, `ActiveSession`, `RotationState`, `Deload`, `DeloadState` actualizados. Tests unitarios exhaustivos para `RotationResolver` (N=1, N=2, N=3, N=5, N=10) y `LoadIncrementResolver`.
   - Dependencias: Hito 2

4. **`SessionRepositoryImpl` rewrite** — Migrar toda la lógica de rotación, closeSession, activateDeload, deload finalization, evaluateProgression, alertas de hardcoded A/B/C a N rutinas dinámicas. Eliminar dependencias de `ModuleDao`. Inyectar nuevos DAOs.
   - Dependencias: Hito 2, Hito 3

5. **UseCases de gestión de rutinas** — `CreateRoutineUseCase`, `UpdateRoutineNameUseCase`, `DeleteRoutineUseCase`, `ReorderRoutinesUseCase`, `CreateRoutineVersionUseCase`, `DeleteRoutineVersionUseCase`. Modificar existentes: `AssignExerciseToVersionUseCase`, `UnassignExerciseFromVersionUseCase`. Tests unitarios.
   - Dependencias: Hito 2

6. **UseCases existentes modificados** — `GetNextSessionInfoUseCase`, `CloseSessionUseCase`, `ActivateDeloadUseCase`, `GetDeloadStateUseCase`, `GetSessionSummaryUseCase` adaptados al nuevo modelo.
   - Dependencias: Hito 3, Hito 4

7. **UI — Gestión de plan** — `RoutineListScreen/ViewModel`, `RoutineVersionListScreen/ViewModel`, `ExerciseSelectorScreen/ViewModel`. Modificar `PlanVersionDetailScreen` para CRUD de asignaciones. Rutas de navegación nuevas.
   - Dependencias: Hito 5

8. **UI — Pantallas existentes** — Actualizar Home, ActiveSession, SessionPreview, SessionSummary, SessionHistory, SessionDetail, SubstituteExercise, ExerciseDictionary, Alerts. Cambiar todas las referencias `moduleCode` → `routineName` en UI.
   - Dependencias: Hito 4, Hito 6

### Validación de Impacto

**Código real verificado (Paso 1.5):**

| Componente | Estado Actual (v8) | Impacto para v9 |
| --- | --- | --- |
| `ModuleEntity` | Tabla `module`, PK=code (String), 3 filas fijas (A, B, C) | **Eliminada** — reemplazada por `RoutineEntity` con PK Long autoincrement |
| `ModuleVersionEntity` | Tabla `module_version`, FK→module.code, 9 filas (3 módulos × 3 versiones) | **Eliminada** — reemplazada por `RoutineVersionEntity` con FK→routine.id. IDs preservados en migración |
| `ExerciseEntity.moduleCode` | FK→module.code, obliga a cada ejercicio a pertenecer a un módulo | **Columna eliminada** — ejercicios agnósticos (CA-15). 43 ejercicios existentes pierden su module_code sin impacto funcional |
| `RotationStateEntity` | 5 campos: position, versionA, versionB, versionC, count. Fila única | **Reducida a 3 campos** — position, count, id. Versiones migran a `routine_current_version` (3 filas iniciales, N futuras) |
| `DeloadEntity` | 6 campos incluyendo frozenVersionA/B/C | **Reducida a 4 campos** — id, status, activation_date, completion_date. Versiones congeladas migran a `deload_frozen_version` |
| `RotationResolver` | 47 líneas, hardcoded A/B/C con posiciones 1-6, versiones hardcoded %3 | **Rewrite completo** — parametrizado por `List<Routine>`, `Map<Long, Int>` para versiones, wrap-around por rutina |
| `SessionRepositoryImpl` | 1040 líneas, 13 dependencias inyectadas (12 DAOs + 1 TensionDatabase), lógica de A/B/C en 8+ métodos | **~300 líneas cambian** — rotación, deload, alertas, inactividad, session preview. Constructor cambia de 13 a 15 dependencias (quita 2 DAOs, agrega 4 DAOs) |
| `GetNextSessionInfoUseCase` | 52 líneas, usa `RotationResolver.resolveModuleCode()` + `resolveVersionNumber()` | **Rewrite** — usa `routineDao.getAll()`, `routineCurrentVersionDao`, nuevo `RotationResolver` |
| `SessionDao` | 120 líneas, 6 DTOs con `moduleCode` | **~80% queries modificadas** — JOINs con `routine`+`routine_version` en vez de `module_version`+`module` |
| `ExerciseDao` | 120 líneas, 3 queries con `module_code` filter | **3 queries modificadas** — eliminar JOIN module, `getEligibleSubstitutesForSession` filtra por zona muscular |
| `PlanAssignmentDao` | 109 líneas, 2 DTOs, 8 queries con `module_version_id` | **Todas las queries cambian** `module_version_id` → `routine_version_id`. JOIN `module` eliminado |
| `AlertDao` | 74 líneas, `existsActiveByModule`, `resolveByModuleAndType` | **2 queries modificadas** — `module_code` → `routine_id` |
| `AlertThresholdRule.MUSCLE_GROUPS_BY_MODULE` | Mapa hardcoded: A→[Espalda,Bíceps,Abdomen], B→[...], C→[...] | **Eliminado** — grupos musculares se obtienen dinámicamente de plan_assignment + exercise_muscle_zone |
| `ModuleSeeder` / `PlanSeeder` | Seed data para v1-v8. Callback en DB creation | **Se mantienen** para migraciones ≤v8. Ya no se invocan en nuevas instalaciones v9+ |
| `ExerciseSeeder` | Seeds 43 ejercicios con `module_code` | **Requiere actualización** — omitir `module_code` en v9+ (columna eliminada en exercise). Se mantiene versión actual para migraciones ≤v8 |
| `AlertRepositoryImpl` | Usa `moduleCode` 10+ veces, `MUSCLE_GROUPS_BY_MODULE` | **Rewrite parcial** — `moduleCode` → `routineId`, grupos musculares dinámicos |
| `MetricsRepositoryImpl` | `moduleCode: String` param, `getSessionIdsByModuleInRange` | **Parámetro cambia** a `routineId: Long` |
| `PlanRepositoryImpl` | Agrupa por `moduleCode`, mapea nombres módulo | **Rewrite** — interfaz `PlanRepository` cambia completamente o se absorbe en `RoutineRepository` |
| `TensionNavHost` | 22+ rutas con `moduleVersionId`/`moduleCode` como params | **~10 rutas modificadas** — params renombrados. ~4 rutas nuevas para gestión de plan |
| Todas las pantallas UI | 15+ pantallas con `moduleCode` en display | **Display textual cambia** — "Módulo A" → nombre de rutina ("Pull + Abs") |
| Modelos de dominio adicionales | 15 data classes con campo `moduleCode: String` (`Exercise`, `SessionHistoryItem`, `SessionSummary`, `PlanVersionDetail`, etc.) | **Cambio mecánico masivo** — `moduleCode` → `routineId: Long` o `routineName: String` según contexto |

**Verificaciones críticas:**

- `routine_version.id` en migración se inserta con el mismo valor que `module_version.id` → las FKs de `session` y `plan_assignment` permanecen válidas sin UPDATE masivo. Verificado: los IDs 1-9 (3 módulos × 3 versiones) se preservan.
- `exercise.module_code` se elimina sin afectar `exercise_muscle_zone` (la relación ejercicio↔zona muscular es independiente del módulo). Los 43 ejercicios precargados y cualquier custom existente mantienen todas sus propiedades excepto la pertenencia a módulo.
- La lógica de deload cambia de "6 sesiones fijas" a "N sesiones" (N = número de rutinas). Esto es correcto: un microciclo recorre todas las rutinas exactamente una vez (CA-26).
- `LoadIncrementResolver` usa `muscle_zone.muscle_group` que ya existe en la DB y está indexado. No requiere nueva tabla ni columna.
- Las migraciones Room son transaccionales por defecto — si `MIGRATION_8_9` falla a mitad, se revierte completamente.

**Cadena de invocación — Creación de rutina (CA-01):**

```text
RoutineListScreen (tap FAB "Crear rutina")
  → Dialog: nombre (max 50 chars, validación unicidad)
    → RoutineListViewModel.createRoutine(name)
      → CreateRoutineUseCase(name)
        → routineDao.getMaxSortOrder() → sortOrder = MAX + 1
        → routineDao.insert(RoutineEntity(name, sortOrder, today)) → routineId
        → routineVersionDao.insert(RoutineVersionEntity(routineId, 1)) → versionId
        → routineCurrentVersionDao.insert(RoutineCurrentVersionEntity(routineId, 1))
        → return routineId
```

**Cadena de invocación — Rotación con N rutinas (CA-16, CA-19):**

```text
CloseSession (cierre de sesión)
  → SessionRepositoryImpl.closeSession(sessionId)
    → sessionDao.updateStatus(sessionId, status)
    → evaluateProgression(sessionId, routineVersionId, isDeload)
    → routineDao.getAll() → routines (ordenadas por sort_order)
    → routineCurrentVersionDao.getAllOnce() → currentVersions: Map<Long, Int>
    → rotationStateDao.getRotationState() → RotationState(position, count)
    → RotationResolver.advanceRotation(
        position = rotationState.microcyclePosition,
        totalRoutines = routines.size,
        currentVersions = currentVersions,
        versionCounts = routines.map { routineVersionDao.countByRoutineId(it.id) },
        isDeload = deloadId != null,
      ) → AdvanceResult(newPosition, newCount, updatedVersions)
    → rotationStateDao.update(newPosition, newCount)
    → Si microciclo completó: para cada rutina, routineCurrentVersionDao.update(updatedVersion)
    → Si deload: check if deloadSessionCount == totalRoutines → complete deload
```

**Cadena de invocación — Activar deload (CA-25, CA-26):**

```text
SessionSummaryScreen (tap "Activar Descarga")
  → ActivateDeloadUseCase()
    → SessionRepositoryImpl.activateDeload()
      → deloadDao.insert(DeloadEntity(today)) → deloadId
      → routineCurrentVersionDao.getAllOnce() → currentVersions
      → deloadFrozenVersionDao.insertAll(
          currentVersions.map { DeloadFrozenVersionEntity(deloadId, it.routineId, it.currentVersionNumber) }
        )
      → exerciseProgressionDao.transitionToDeload()
```

**Riesgos identificados:**

| Riesgo | Impacto | Mitigación |
| --- | --- | --- |
| Migración SQL falla o corrompe datos | Pérdida total del plan y sesiones del usuario | Migración dentro de transacción Room (atómica). Tests de migración instrumentados con datos reales de v8. IDs de routine_version preservados exactamente como module_version |
| `exercise.module_code` eliminado rompe queries activas | Crash en pantallas que usan ExerciseWithDetails | Todas las queries que hacían JOIN module se reescriben en el mismo hito que la migración. No hay ventana donde la DB sea v9 pero las queries sean v8 |
| Deload activo al momento de migración | Versiones congeladas se pierden | Fase 2 de migración copia `frozen_version_module_a/b/c` a `deload_frozen_version` para CADA deload existente (activo o completado) |
| Rotación con N=1 rutina | Microciclo de 1 sesión, versión rota cada sesión | Comportamiento correcto por diseño (CA-17: microciclo = pasada por todas las rutinas). Tests específicos para N=1 |
| Rotación con N rutinas y versiones heterogéneas | Rutina A tiene 2 versiones, Rutina B tiene 5 | `RotationResolver.advanceRotation` aplica wrap-around independiente por rutina: `(current % count) + 1`. Tests para combinaciones heterogéneas |
| `loadIncrementKg` calculado en app vs en SQL | Valor incorrecto si el ejercicio tiene múltiples zonas de distinto tren | `LoadIncrementResolver` recibe el `muscle_group` principal. Para ejercicios multi-zona (ej: Sentadilla Búlgara → Cuádriceps + Glúteos, ambos tren inferior), se usa el grupo de la primera zona. En la práctica, ningún ejercicio actual cruza tren superior e inferior |
| Sesión activa al momento de actualizar la app | La migración corre con sesión IN_PROGRESS | La migración es puramente esquemática — no modifica datos transaccionales en curso. La sesión activa sigue funcionando porque `routine_version.id == module_version.id` |
| UI muestra "Módulo A" brevemente post-migración | Código viejo renderiza antes de que el nuevo se compile | Imposible — el APK actualizado incluye código y migración juntos. No hay estado intermedio |

### Notas Técnicas (Si aplica)

- **Scope de la migración:** MIGRATION_8_9 es la migración más extensa del proyecto (7 tablas recreadas, 4 tablas nuevas, 2 tablas eliminadas). Debe testearse con `MigrationTestHelper` de Room usando datos representativos de v8 (3 módulos, 9 versiones, 82 asignaciones, sesiones con y sin deload, alertas activas).
- **Seeders obsoletos:** `ModuleSeeder` y `PlanSeeder` se mantienen en el código porque las migraciones v1→v7 y v7→v8 los referencian indirectamente (las tablas `module` y `module_version` deben existir para migrar). En nuevas instalaciones v9+, el callback de creación inicial ya no invoca estos seeders — en su lugar, inicializa una DB vacía con solo las tablas de catálogo (muscle_zone, equipment_type, exercise) y espera que el usuario cree su plan.
- **`LoadIncrementResolver` vs columna en DB:** Se optó por calcular `loadIncrementKg` en capa de aplicación en vez de agregar una columna a `exercise` o `muscle_zone` porque: (a) el valor se deriva 1:1 del grupo muscular (regla de negocio pura), (b) evita duplicar datos, (c) es más fácil de modificar si los incrementos cambian en el futuro.
- **Onboarding para nuevas instalaciones:** Con la eliminación de módulos fijos, las nuevas instalaciones v9+ inician con un plan vacío. Esto requiere un flujo de configuración inicial donde el usuario crea al menos 1 rutina con 1 versión y 1 ejercicio (CA-06). Este flujo puede ser guiado ("Wizard de configuración") o libre. La decisión de UX se delega al Developer.
- **Impacto en tests existentes:** Todos los tests unitarios e instrumentados que referencian `module`, `moduleCode`, `ModuleEntity`, `ModuleVersionEntity`, `ModuleDao`, `ModuleVersionDao` deben reescribirse. Esto incluye tests de rotación, deload, progresión, alertas y migración.
- **Numeración de deload sessions:** La descarga dura exactamente 1 microciclo = N sesiones (donde N = número de rutinas, CA-26). El conteo `countDeloadSessions(deloadId)` sigue funcionando porque se basa en `session.deload_id`, pero la comparación `== 6` se reemplaza por `== routineDao.countRoutines()`.
- **Nombres en migración SQL (Fase 2):** Los nombres `'Pull + Abs'`, `'Push'`, `'Pierna'` se derivan de los valores actuales en `module.name` del `ModuleSeeder` ("Módulo A — Superior (Pull + Abs)", "Módulo B — Superior (Push)", "Módulo C — Inferior"). Se acortan para la nueva UI donde el usuario ve y edita directamente el nombre de rutina (CA-02). Son inmediatamente editables post-migración.
- **`ExerciseSeeder` requiere actualización para v9+:** El `ExerciseSeeder` actual inserta `module_code` en cada ejercicio (L80: `put("module_code", moduleCode)`). En v9+ la columna `module_code` no existe en `exercise`. Para nuevas instalaciones v9+, el seeder debe actualizarse para omitir `module_code`. Se mantiene la versión actual para migraciones ≤v8 (donde la columna aún existe durante el seed, antes de migrar a v9).
- **Impacto en Backup/Restore (HU-18):** El esquema JSON de exportación cambia completamente. Tablas exportadas `module` y `module_version` desaparecen; se agregan `routine`, `routine_version`, `routine_current_version`, `deload_frozen_version`. La lógica de importación debe detectar la versión del backup (v8 vs v9) y aplicar la transformación correspondiente. Backups v8 importados a una app v9 requieren un paso de migración en la capa de import.
- **Propagación de `muscleGroup` para `LoadIncrementResolver`:** El DTO `SessionExerciseForProgression` pierde `loadIncrementKg` (que venía del JOIN con `module`). Para que `LoadIncrementResolver.resolve(muscleGroup)` funcione en `evaluateProgression()`, el query de `getSessionExercisesForProgression` debe agregar un JOIN a `exercise_muscle_zone` + `muscle_zone` para obtener el `muscle_group` principal (primera zona). Alternativa: el DTO incluye `muscleGroup: String` como campo adicional.

### Referencias y Validación

**Documentación consultada:**

- Visión del Producto — §3 y §7 (plan personalizable, rotación definida por usuario), §4.5 (sesión pertenece al usuario), §4.8 (fricción → cero)
- Requerimientos — RF05 (crear/editar plan), RF09 (rotación por sesiones completadas), RF10 (versión por rutina), RF11 (persistir rotación), RF12 (preview), RF16 (sustitución por zona muscular), RF30 (fatiga por rutina), RF32 (isométricos), RF37 (detección descarga por rutina), RF38 (descarga al 60%), RF39 (descarga = 1 microciclo), RF41 (conteo microciclos), RF46 (RIR por rutina), RF58 (inactividad > 10 días por rutina), RF62 (ejercicios custom), RF63 (asignación libre), RF64 (desasignar ejercicio)
- Arquitectura Técnica — MVVM + Domain Layer (4 capas), Material 3, §4.3 (inventario rutas), §4.5 (reglas Bottom Nav)
- ADR-03 (Room/SQLite), ADR-05 (MVVM + Domain), ADR-07 (Navigation Compose), ADR-09 (StateFlow + SharedFlow), ADR-12 (esquema cromático)
- Modelo de Datos (actualizado para HU-22) — `routine`, `routine_version`, `routine_current_version`, `deload_frozen_version`, relaciones y cardinalidades, diagrama ER
- Mapa de Navegación — B1 (Home), D1-D2 (Diccionario), E1-E5 (session flow), F1-F3 (Historial), H1-H2 (Alertas)
- Especificación Visual — §7.1 (CenterAlignedTopAppBar), §10 (accesibilidad RNF05)
- Plan de Entrenamiento — Composiciones por rutina, rangos isométricos `'30-45_SEC'`
- Manifiesto de Dominio Sistémico — Restricciones de integridad, máquina de estados de progresión

**Historias relacionadas:**

- Historia #3: HU-03 — Registro de plan (base que HU-22 reemplaza con plan dinámico)
- Historia #4: HU-04 — Detalle de versión de módulo → se reparametriza para rutina/versión, se agrega CRUD de asignaciones
- Historia #5: HU-05 — Creación de sesión (`startSession()`). El `moduleVersionId` → `routineVersionId`
- Historia #6: HU-06 — Registro de serie. Sin cambio funcional, pero `session_exercise` ahora referencia `routine_version` indirectamente
- Historia #7: HU-07 — Sustitución de ejercicios. Filtro cambia de `module_code` a zona muscular (CA-22)
- Historia #8: HU-08 — Cross-cutting isométrico. Sin cambio funcional
- Historia #9: HU-09 — Cierre de sesión + rotación. La rotación se reescribe para N rutinas
- Historia #10: HU-10 — Motor de progresión. `ProgressionClassificationRule` sin cambio funcional (regla pura). Pero `SessionExerciseForProgression` DTO pierde `moduleCode` y `loadIncrementKg` (se resuelve vía `LoadIncrementResolver` en Repository)
- Historia #11: HU-11 — Regla Doble Umbral. `loadIncrementKg` ahora viene de `LoadIncrementResolver` en vez de `module.load_increment_kg`
- Historia #12: HU-12 — Detección de fatiga. `moduleCode` → `routineId`, `ModuleFatigueRule` sin cambio funcional. Además, define `CorrectiveActionRule` con acción `ROTATE_VERSION` que pasa de "rotar versión del módulo" a "rotar versión de la misma rutina" (CA-27)
- Historia #13: HU-13 — Resumen post-sesión. `SessionSummaryInfo` DTO tiene `moduleCode`. `GetSessionSummaryUseCase` llama `alertDao.existsActiveByModule(moduleCode)` para resolver señal de acción. Cambia a `existsActiveByRoutine(routineId)`
- Historia #14: HU-14 — Protocolo de descarga. `frozenVersionModuleA/B/C` → `deload_frozen_version`. Duración cambia de 6 a N sesiones
- Historia #15: HU-15 — Analítica y KPIs. `getSessionIdsByModuleInRange` → `getSessionIdsByRoutineInRange`. "RIR Promedio por Módulo" → "RIR Promedio por Rutina". `MetricsRepositoryImpl` recibe `routineId` en vez de `moduleCode`
- Historia #16: HU-16 — Historial de sesiones y ejercicios. `SessionHistoryItem`, `SessionDetail`, `ExerciseHistoryEntryDto` tienen `moduleCode` → `routineName`. Queries JOINan `routine_version` + `routine` en vez de `module_version` + `module`
- Historia #17: HU-17 — KPIs y alertas. `MODULE_REQUIRES_DELOAD` → `ROUTINE_REQUIRES_DELOAD`. `MODULE_INACTIVITY` → `ROUTINE_INACTIVITY`. Evaluación por rutina dinámica
- Historia #18: HU-18 — Backup y Restauración. El esquema JSON de exportación cambia: tablas `module`/`module_version` ya no existen, reemplazadas por `routine`/`routine_version`/`routine_current_version`/`deload_frozen_version`. La lógica de importación debe manejar backups v8 (con módulos) y v9 (con rutinas)
- Historia #20: HU-20 — `sort_order` en plan_assignment. Ya migrado a v8, se preserva en v9
- Historia #21: HU-21 — Preview de sesión + cronómetro. Preview se reparametriza con `routineVersionId`

**Validado por:** Esteban Colorado González | **Fecha:** 2026-05-06 | **Enfoque:** Exploratorio

### Métricas de Análisis Arquitectónico

**Tiempo estimado Método Ceiba (Análisis):** 30 minutos
**Inicio:** 2026-05-06 11:11 | **Fin:** 2026-05-06 12:21 | **Duración real de sesión:** 70 minutos

---

## Refinamiento Técnico (Developer)

<!-- ============================================================================ -->
<!-- SECCIÓN AGREGADA POR: Workflow refinamiento-tecnico                         -->
<!-- ETAPA: Refinamiento Técnico                                                  -->
<!-- RESPONSABLE: Developer                                                       -->
<!-- BASE: Análisis Arquitectónico (Arquitecto) - Ver sección arriba             -->
<!-- FECHA: 2026-05-06                                                            -->
<!-- ESTADO: Refinado (Developer) - Basado en Análisis Arquitectónico            -->
<!-- ============================================================================ -->

### Consideraciones Generales

**Basado en análisis arquitectónico:**
Sí — Análisis Arquitectónico (Arquitecto) del 2026-05-06 con exploración verificada del código existente. 8 hitos de implementación, delta completo entre modelo v8 (módulos fijos A/B/C) y modelo v9 (rutinas dinámicas). ~30 componentes afectados, migración SQL extensiva.

**Nivel de complejidad:**
MUY ALTA — Transformación completa del modelo de dominio. Migración Room v8→v9 con 7 tablas recreadas, 4 tablas nuevas, 2 eliminadas. Propagación masiva de `moduleCode` → `routineId`/`routineName` a través de las 4 capas (Entity→DAO→Repository→UseCase→ViewModel→Screen). 12 entidades Room afectadas (4 nuevas, 6 modificadas, 2 eliminadas), 9 DAOs (4 nuevos, 5 modificados, 2 eliminados), 6 Repositories (1 nuevo, 5 modificados), 14 UseCases (6 nuevos, 8+ modificados), 15+ modelos de dominio, 17+ pantallas UI (incluyendo 3 eliminadas). 37 CAs a cubrir. `SessionRepositoryImpl` (1030 líneas, 13 dependencias: 12 DAOs + 1 TensionDatabase) sufre rewrite parcial (~300 líneas). `RotationResolver` (47 líneas) rewrite completo para N rutinas dinámicas. 59 tests existentes potencialmente afectados.

**Riesgos técnicos conocidos:**
1. **Migración SQL corrompe datos (CRÍTICO):** MIGRATION_8_9 es la más extensa del proyecto (7 tablas recreadas, 4 nuevas, 2 eliminadas). Si falla a mitad, transacción Room revierte. Mitigación: test instrumentado con `MigrationTestHelper` + datos representativos v8.
2. **Ventana de queries inconsistentes:** Si la DB migra a v9 pero algún query sigue referenciando `module_version`, crash. Mitigación: todos los DAOs se modifican en el mismo hito que las entidades.
3. **Deload activo durante migración:** Versiones congeladas deben preservarse. Mitigación: Fase 2 de migración SQL copia `frozen_version_module_a/b/c` a `deload_frozen_version`.
4. **`loadIncrementKg` calculado vs en SQL:** Ejercicios multi-zona podrían recibir incremento incorrecto. Mitigación: `LoadIncrementResolver` usa la primera zona muscular. Ningún ejercicio actual cruza tren superior e inferior.
5. **Rotación con N heterogéneo de versiones:** Rutina A con 2 versiones, B con 5. Mitigación: `RotationResolver` aplica wrap-around independiente por rutina `(current % count) + 1`. Tests para N=1, N=2, N=5 y combinaciones heterogéneas.
6. **Sesión activa IN_PROGRESS durante upgrade APK:** Migración esquemática, no modifica datos transaccionales. `routine_version.id == module_version.id` → sesión activa sigue funcionando.
7. **Backup v8 importado en app v9:** Esquema JSON de exportación cambia. Mitigación: `ImportBackupUseCase` detecta versión y transforma v8→v9.
8. **Seeders para nuevas instalaciones v9+:** `ExerciseSeeder` actual inserta `module_code`. Mitigación: `PrepopulateCallbackV9` con seeders actualizados; callback original intacto para migraciones ≤v8.

**Patrones y convenciones del equipo:**
- MVVM + Domain Layer con `StateFlow` + `SharedFlow` (`_uiState` privado / `uiState` público).
- Hilt `@HiltViewModel` + `SavedStateHandle` para argumentos de navegación.
- Room queries con `LEFT JOIN` + `COALESCE` para datos opcionales.
- `viewModelScope.launch` para coroutines. Domain layer Kotlin puro sin dependencias Android (RNF29).
- `collectAsStateWithLifecycle()` en Composables. Lambdas `onNavigateTo*` para navegación.
- Convención de nombres: `{Feature}Screen`, `{Feature}ViewModel`, `{Feature}UiState`, `{Acción}{Entidad}UseCase`.
- Repository interface en `domain/repository/`, implementación en `data/repository/`.

**Dependencias nuevas a instalar:**
`room-testing` (`androidx.room:room-testing`) — solo `androidTestImplementation`. Necesaria para `MigrationTestHelper` en test instrumentado de migración v8→v9. Agregar a `libs.versions.toml` y `app/build.gradle.kts`. Requiere configurar `exportSchema = true` en `TensionDatabase` y `room.schemaLocation` en KSP args para generar schemas JSON.

**Estrategia de testing:**
JUnit 4 + MockK | Tests unitarios (domain/rules, UseCases nuevos, ViewModels modificados) | Cobertura: 100% para `LoadIncrementResolver` y `RotationResolver` rewrite, UseCases nuevos de rutinas; tests modificados para UseCases existentes y ViewModels | Test instrumentado: `MigrationTestHelper` para MIGRATION_8_9 (MANUAL) | Builders: no requeridos — datos inline (patrón existente)

### Historias Relacionadas Consultadas

**Implementaciones similares analizadas:**
- HU-04: `PlanAssignmentDao.getDetailsByModuleVersionId()`, `PlanVersionDetailViewModel` con assign/unassign.
- HU-05: `startSession()` / `StartSessionUseCase`, `SessionEntity.moduleVersionId`.
- HU-07: Sustitución — `getEligibleSubstitutesForSession(moduleCode, sessionId)` filtra por módulo.
- HU-09: `closeSession()` con `RotationResolver.advanceRotation()`, `evaluateProgression()`.
- HU-11: Regla Doble Umbral — `loadIncrementKg` de `module.load_increment_kg`.
- HU-12: `ModuleFatigueRule`, `CorrectiveActionRule` con `ROTATE_VERSION` → ahora por rutina (CA-27).
- HU-13: `SessionSummaryInfo.moduleCode`, `alertDao.existsActiveByModule()`.
- HU-14: Deload — `DeloadEntity.frozenVersionModuleA/B/C`, `countDeloadSessions() == 6`.
- HU-15: KPIs — `getRirValuesByModule(moduleCode)`, `getSessionIdsByModuleInRange()`.
- HU-17: Alertas — `AlertThresholdRule.MUSCLE_GROUPS_BY_MODULE`, evaluación por módulo hardcoded.
- HU-18: Backup — esquema JSON con tablas `module`/`module_version`.
- HU-21: Preview — `SessionPreviewExercise.moduleCode`, `LoadDisplayMapper`.

**Patrones de código reutilizados:**
- Room migration con `CREATE TABLE _new → INSERT INTO _new SELECT FROM old → DROP TABLE old → ALTER TABLE _new RENAME TO` — patrón de `MIGRATION_7_8`.
- `RoutineDao` CRUD sigue patrón de `ModuleDao` + `ModuleVersionDao` existentes.
- `RoutineRepository` interface/impl sigue patrón de `PlanRepository`, `AlertRepository`, `MetricsRepository`.
- Deload frozen versions en tabla dinámica sigue patrón de `exercise_muscle_zone` (PK compuesta).
- `LoadIncrementResolver` como regla pura sigue patrón de `DeloadLoadRule`, `DoubleThresholdRule`.

**Mejores prácticas aplicadas:**
- `LoadIncrementResolver` como función pura en `domain/rules/` — testeable sin Android.
- Migración SQL con IDs preservados (`routine_version.id == module_version.id`) — evita UPDATE masivo de FKs.
- `RoutineRepository` separado para cohesión funcional (decisión validada con equipo).
- `PrepopulateCallbackV9` para nuevas instalaciones sin romper migraciones históricas (decisión validada).
- Empty state en Home con CTA para crear plan — UX sin wizard, confía en flujo natural (decisión validada).
- Zonas musculares dinámicas en vez de mapa hardcoded `MUSCLE_GROUPS_BY_MODULE` — escalable a N rutinas.

---

## Tareas de Implementación (Developer)

### Fase 1: Migración Room v8→v9 + Entidades (Hito 1)

#### 📦 Data — Entidades Nuevas

- [ ] **Crear `RoutineEntity`** (AC: CA-01, CA-02, CA-04, CA-05)
  - [ ] Data class con `@Entity(tableName = "routine")`: `id: Long` (PK autoincrement), `name: String` (UNIQUE), `sortOrder: Int` (CHECK ≥ 1), `createdAt: String` - Archivo: `data/local/entity/RoutineEntity.kt`

- [ ] **Crear `RoutineVersionEntity`** (AC: CA-07, CA-09, CA-14)
  - [ ] Data class con `@Entity(tableName = "routine_version")`: `id: Long` (PK autoincrement), `routineId: Long` (FK→routine, ON DELETE CASCADE), `versionNumber: Int` (CHECK ≥ 1). Unique(routineId, versionNumber). Índice en `routineId` - Archivo: `data/local/entity/RoutineVersionEntity.kt`

- [ ] **Crear `RoutineCurrentVersionEntity`** (AC: CA-10, CA-18)
  - [ ] Data class con `@Entity(tableName = "routine_current_version")`: `routineId: Long` (PK, FK→routine ON DELETE CASCADE), `currentVersionNumber: Int` (DEFAULT 1, CHECK ≥ 1) - Archivo: `data/local/entity/RoutineCurrentVersionEntity.kt`

- [ ] **Crear `DeloadFrozenVersionEntity`** (AC: CA-26)
  - [ ] Data class con `@Entity(tableName = "deload_frozen_version")`: PK compuesta `(deloadId, routineId)`, `deloadId: Long` (FK→deload ON DELETE CASCADE), `routineId: Long` (FK→routine ON DELETE RESTRICT), `frozenVersionNumber: Int` (CHECK ≥ 1) - Archivo: `data/local/entity/DeloadFrozenVersionEntity.kt`

#### 📦 Data — Entidades Modificadas

- [ ] **Modificar `ExerciseEntity`** (AC: CA-15)
  - [ ] Eliminar campo `moduleCode: String` y su `@ForeignKey` a `module` - Archivo: `data/local/entity/ExerciseEntity.kt`
  - [ ] Eliminar `@Index` en `module_code`
  - [ ] Mantener `@Index` en `equipment_type_id` y unique index `(name, equipment_type_id)`

- [ ] **Modificar `SessionEntity`** (AC: CA-21)
  - [ ] Renombrar `moduleVersionId` → `routineVersionId`, FK apunta a `routine_version` - Archivo: `data/local/entity/SessionEntity.kt`

- [ ] **Modificar `PlanAssignmentEntity`** (AC: CA-12, CA-14)
  - [ ] Renombrar `moduleVersionId` → `routineVersionId` en PK compuesta y FK. PK `(exerciseId, routineVersionId)` permite mismo ejercicio en múltiples rutinas/versiones - Archivo: `data/local/entity/PlanAssignmentEntity.kt`

- [ ] **Modificar `AlertEntity`** (AC: CA-31)
  - [ ] Cambiar `moduleCode: String?` → `routineId: Long?` con FK→routine ON DELETE CASCADE - Archivo: `data/local/entity/AlertEntity.kt`

- [ ] **Modificar `DeloadEntity`** (AC: CA-26)
  - [ ] Eliminar `frozenVersionModuleA`, `frozenVersionModuleB`, `frozenVersionModuleC` - Archivo: `data/local/entity/DeloadEntity.kt`

- [ ] **Modificar `RotationStateEntity`** (AC: CA-18)
  - [ ] Eliminar `currentVersionModuleA`, `currentVersionModuleB`, `currentVersionModuleC` - Archivo: `data/local/entity/RotationStateEntity.kt`
  - [ ] Conservar solo `id`, `microcyclePosition`, `microcycleCount`
  - [ ] Nota: `ProfileRepositoryImpl.createProfile()` inserta `RotationStateEntity()` con constructor por defecto — al eliminar las 3 columnas de versión, el constructor se adapta automáticamente. Verificar que los defaults (microcyclePosition=1, microcycleCount=0) sean correctos para v9

#### 📦 Data — Migración SQL

- [ ] **Crear `MIGRATION_8_9`** (AC: CA-35, CA-36, CA-37)
  - [ ] Fase 1 SQL: CREATE TABLE `routine`, `routine_version`, `routine_current_version`, `deload_frozen_version` - Archivo: `data/local/database/Migrations.kt`
  - [ ] Fase 2 SQL: INSERT datos existentes (módulos→rutinas con nombres 'Pull + Abs', 'Push', 'Pierna', IDs de routine_version preservados de module_version)
  - [ ] Fase 3 SQL: Recrear `session`, `plan_assignment`, `exercise`, `alert`, `deload`, `rotation_state` con nuevo esquema (patrón CREATE _new → INSERT → DROP → RENAME)
  - [ ] Fase 4 SQL: DROP TABLE `module_version`, DROP TABLE `module`
  - [ ] Crear índices en todas las tablas recreadas

#### 📦 Data — Database y Seeders

- [ ] **Modificar `TensionDatabase`** (AC: CA-35)
  - [ ] Incrementar version a 9, cambiar `exportSchema = false` → `exportSchema = true` - Archivo: `data/local/database/TensionDatabase.kt`
  - [ ] Actualizar entities array: reemplazar `ModuleEntity`, `ModuleVersionEntity` por `RoutineEntity`, `RoutineVersionEntity`, `RoutineCurrentVersionEntity`, `DeloadFrozenVersionEntity`
  - [ ] Actualizar entities modificadas: `ExerciseEntity`, `SessionEntity`, `PlanAssignmentEntity`, `AlertEntity`, `DeloadEntity`, `RotationStateEntity`
  - [ ] Reemplazar DAOs: `moduleDao()`, `moduleVersionDao()` → `routineDao()`, `routineVersionDao()`, `routineCurrentVersionDao()`, `deloadFrozenVersionDao()`
  - [ ] Agregar `MIGRATION_8_9` al builder `.addMigrations()`
  - [ ] Configurar `room.schemaLocation` en `app/build.gradle.kts` KSP args

- [ ] **Crear `PrepopulateCallbackV9`** (AC: CA-06)
  - [ ] Callback para nuevas instalaciones v9+: seeds de `muscle_zone`, `equipment_type`, `exercise` (sin `module_code`). NO invoca `ModuleSeeder` ni `PlanSeeder`. Plan vacío — usuario crea su propio plan - Archivo: `data/local/seed/PrepopulateCallbackV9.kt`
  - [ ] Condicional en `TensionDatabase.Builder`: usar `PrepopulateCallbackV9` para creación nueva, mantener `PrepopulateCallback` original para migraciones

#### 📦 Test

- [ ] **Test instrumentado de migración** (AC: CA-35, CA-36, CA-37) — MANUAL
  - [ ] Configurar `MigrationTestHelper` con schema v8
  - [ ] Datos representativos: 3 módulos, 9 versiones, 82 asignaciones, sesiones con/sin deload, alertas activas, ejercicios custom
  - [ ] Verificar: IDs preservados, FKs válidas, datos de deload migrados, alertas con routineId correcto

### Fase 2: DAOs Nuevos + Modificados (Hito 2)

#### 📦 Data — DAOs Nuevos

- [ ] **Crear `RoutineDao`** (AC: CA-01, CA-02, CA-03, CA-04, CA-05)
  - [ ] `getAll(): Flow<List<RoutineEntity>>` ORDER BY sort_order - Archivo: `data/local/dao/RoutineDao.kt`
  - [ ] `getById(id: Long): RoutineEntity?`
  - [ ] `insert(routine: RoutineEntity): Long`
  - [ ] `update(routine: RoutineEntity)`
  - [ ] `delete(id: Long)`
  - [ ] `getMaxSortOrder(): Int?`
  - [ ] `countRoutines(): Int`
  - [ ] `updateSortOrder(id: Long, sortOrder: Int)`

- [ ] **Crear `RoutineVersionDao`** (AC: CA-07, CA-08, CA-10, CA-11)
  - [ ] `getByRoutineId(routineId: Long): Flow<List<RoutineVersionEntity>>` - Archivo: `data/local/dao/RoutineVersionDao.kt`
  - [ ] `getByRoutineIdAndVersion(routineId: Long, versionNumber: Int): RoutineVersionEntity?`
  - [ ] `insert(version: RoutineVersionEntity): Long`
  - [ ] `delete(id: Long)`
  - [ ] `countByRoutineId(routineId: Long): Int`
  - [ ] `getMaxVersionNumber(routineId: Long): Int?`
  - [ ] `getVersionCountsByRoutine(): List<VersionCountDto>` — query agregado `SELECT routine_id, COUNT(*) AS count FROM routine_version GROUP BY routine_id` (evita N+1 en `closeSession`)

- [ ] **Crear `RoutineCurrentVersionDao`** (AC: CA-10, CA-11, CA-18)
  - [ ] `getAll(): Flow<List<RoutineCurrentVersionEntity>>` - Archivo: `data/local/dao/RoutineCurrentVersionDao.kt`
  - [ ] `getAllOnce(): List<RoutineCurrentVersionEntity>` (suspend)
  - [ ] `getByRoutineId(routineId: Long): RoutineCurrentVersionEntity?`
  - [ ] `insert(entity: RoutineCurrentVersionEntity)`
  - [ ] `update(entity: RoutineCurrentVersionEntity)`
  - [ ] `deleteByRoutineId(routineId: Long)`

- [ ] **Crear `DeloadFrozenVersionDao`** (AC: CA-26)
  - [ ] `getByDeloadId(deloadId: Long): List<DeloadFrozenVersionEntity>` - Archivo: `data/local/dao/DeloadFrozenVersionDao.kt`
  - [ ] `insertAll(entities: List<DeloadFrozenVersionEntity>)`
  - [ ] `deleteByDeloadId(deloadId: Long)`

#### 📦 Data — DAOs Modificados

- [ ] **Modificar `PlanAssignmentDao`** (AC: CA-12, CA-13)
  - [ ] Todas las queries: `module_version_id` → `routine_version_id` - Archivo: `data/local/dao/PlanAssignmentDao.kt`
  - [ ] `getPreviewByModuleVersionId` → `getPreviewByRoutineVersionId`: eliminar JOIN `module m ON e.module_code = m.code`, eliminar `loadIncrementKg` del DTO (se resuelve en Repository via `LoadIncrementResolver`)
  - [ ] DTO `SessionPreviewExerciseDto`: eliminar campo `loadIncrementKg` (11 campos → 11 campos, reemplaza `loadIncrementKg` por `muscleGroup: String?` obtenido del JOIN existente con `muscle_zone`)
  - [ ] DTO `PlanAssignmentWithExerciseDetails`: eliminar `moduleCode`, agregar `muscleGroup: String?` si necesario

- [ ] **Modificar `SessionDao`** (AC: CA-20, CA-32)
  - [ ] DTOs: `moduleCode: String` → `routineName: String` - Archivo: `data/local/dao/SessionDao.kt`
  - [ ] JOINs: `module_version` + `module` → `routine_version` + `routine`
  - [ ] `getSessionIdsByModuleInRange(moduleCode, limit)` → `getSessionIdsByRoutineInRange(routineId: Long, limit: Int)`
  - [ ] `getLastSessionDateByModule(moduleCode)` → `getLastSessionDateByRoutine(routineId: Long)`

- [ ] **Modificar `ExerciseDao`** (AC: CA-15, CA-22)
  - [ ] DTO `ExerciseWithDetails`: eliminar `moduleCode`, `moduleName` - Archivo: `data/local/dao/ExerciseDao.kt`
  - [ ] Eliminar `getByModuleCodeNotInVersion(moduleCode, moduleVersionId)` — reemplazar por `getEligibleForVersion(routineVersionId: Long)` que no filtra por módulo
  - [ ] `getEligibleSubstitutesForSession(moduleCode, sessionId)` → `getEligibleSubstitutesForSession(muscleZoneIds: List<Long>, sessionId: Long)`: filtra por zona muscular, excluye ejercicios ya en sesión

- [ ] **Modificar `SessionExerciseDao`** (AC: CA-21, CA-33)
  - [ ] DTOs con `moduleCode` → `routineId: Long` o eliminarlo - Archivo: `data/local/dao/SessionExerciseDao.kt`
  - [ ] `SessionExerciseWithDetails`: eliminar JOIN `module` para `loadIncrementKg`; agregar `muscleGroup: String?` via JOIN `exercise_muscle_zone` + `muscle_zone`
  - [ ] `SessionExerciseForProgression`: eliminar `moduleCode` y `loadIncrementKg`; agregar `muscleGroup: String?`
  - [ ] `ExerciseHistoryEntryDto`: `moduleCode` → `routineName` via JOIN `routine_version` + `routine`

- [ ] **Modificar `AlertDao`** (AC: CA-31)
  - [ ] `existsActiveByModule(moduleCode)` → `existsActiveByRoutine(routineId: Long)` - Archivo: `data/local/dao/AlertDao.kt`
  - [ ] `resolveByModuleAndType(moduleCode)` → `resolveByRoutineAndType(routineId: Long)`

#### 📦 Data — DAOs Eliminados

- [ ] **Eliminar `ModuleDao`** - Archivo: `data/local/dao/ModuleDao.kt`
- [ ] **Eliminar `ModuleVersionDao`** - Archivo: `data/local/dao/ModuleVersionDao.kt`

#### 📦 Data — Entidades Eliminadas

- [ ] **Eliminar `ModuleEntity`** - Archivo: `data/local/entity/ModuleEntity.kt`
- [ ] **Eliminar `ModuleVersionEntity`** - Archivo: `data/local/entity/ModuleVersionEntity.kt`

### Fase 3: Reglas de Negocio + Modelos de Dominio (Hito 3)

#### 📦 Domain — Reglas de Negocio

- [ ] **Crear `LoadIncrementResolver`** (AC: CA-23)
  - [ ] Object con `fun resolve(muscleGroup: String): Double` — tren inferior → 5.0, tren superior → 2.5, fallback → 2.5 - Archivo: `domain/rules/LoadIncrementResolver.kt`
  - [ ] `LOWER_BODY_GROUPS = setOf("Cuádriceps", "Isquiotibiales", "Glúteos", "Aductores", "Abductores", "Gemelos")`
  - [ ] Test unitario - Archivo: `src/test/.../domain/rules/LoadIncrementResolverTest.kt`
    - Caso: "Cuádriceps" → 5.0
    - Caso: "Pecho" → 2.5
    - Caso: "Bíceps" → 2.5
    - Caso: "Gemelos" → 5.0
    - Caso: string no reconocido → 2.5

- [ ] **Reescribir `RotationResolver`** (AC: CA-16, CA-17, CA-19, CA-11)
  - [ ] `resolveRoutineId(position: Int, routines: List<Routine>): Long` — indexa por sort_order - Archivo: `domain/model/RotationResolver.kt`
  - [ ] `resolveVersionNumber(routineId: Long, currentVersions: Map<Long, Int>): Int` — lookup directo
  - [ ] `advanceRotation(position: Int, totalRoutines: Int, currentVersions: Map<Long, Int>, versionCounts: Map<Long, Int>, isDeload: Boolean): AdvanceResult` — retorna `newPosition`, `newMicrocycleCount`, `updatedVersions`
  - [ ] AdvanceResult: `data class AdvanceResult(val newPosition: Int, val newMicrocycleCount: Int, val updatedVersions: Map<Long, Int>)`
  - [ ] Wrap-around por rutina: `(current % count) + 1` al completar microciclo
  - [ ] Reescribir test - Archivo: `src/test/.../domain/model/RotationResolverTest.kt`
    - Caso: N=1 rutina, 1 versión → posición siempre 1, versión siempre 1
    - Caso: N=3 rutinas, 3 versiones cada una → rotación clásica
    - Caso: N=5 rutinas, versiones heterogéneas (1,2,3,5,10)
    - Caso: N=2 rutinas con deload activo → versiones no avanzan
    - Caso: completar microciclo → incrementar microcycleCount

- [ ] **Eliminar `MUSCLE_GROUPS_BY_MODULE` de `AlertThresholdRule`** (AC: CA-24, CA-27, CA-31)
  - [ ] Eliminar mapa hardcoded A→[Espalda,Bíceps,Abdomen], B→[...], C→[...] - Archivo: `domain/rules/AlertThresholdRule.kt`
  - [ ] Los grupos musculares se obtienen dinámicamente en Repository desde `plan_assignment` + `exercise_muscle_zone` + `muscle_zone`
  - [ ] `CorrectiveActionRule.ROTATE_VERSION` opera con `routineId` en vez de `moduleCode` para rotar versión de la misma rutina (CA-27)
  - [ ] Modificar test - Archivo: `src/test/.../domain/rules/AlertThresholdRuleTest.kt`

#### 📦 Domain — Modelos Nuevos

- [ ] **Crear `Routine`** (AC: CA-01)
  - [ ] `data class Routine(val id: Long, val name: String, val sortOrder: Int, val createdAt: String)` - Archivo: `domain/model/Routine.kt`

- [ ] **Crear `RoutineVersion`** (AC: CA-07)
  - [ ] `data class RoutineVersion(val id: Long, val routineId: Long, val versionNumber: Int)` - Archivo: `domain/model/RoutineVersion.kt`

#### 📦 Domain — Modelos Modificados (15+ data classes)

- [ ] **Modificar `NextSession`** (AC: CA-20)
  - [ ] `moduleCode: String` → `routineId: Long`, agregar `routineName: String`, `moduleVersionId: Long` → `routineVersionId: Long` - Archivo: `domain/model/NextSession.kt`

- [ ] **Modificar `ActiveSession`** (AC: CA-21)
  - [ ] `moduleCode: String` → `routineName: String` - Archivo: `domain/model/ActiveSession.kt`

- [ ] **Modificar `RotationState`** (AC: CA-18)
  - [ ] Eliminar `currentVersionModuleA/B/C`. Solo: `microcyclePosition: Int`, `microcycleCount: Int` - Archivo: `domain/model/RotationState.kt`

- [ ] **Modificar `Deload`** (AC: CA-26)
  - [ ] Eliminar `frozenVersionModuleA/B/C`. Agregar `frozenVersions: Map<Long, Int>` - Archivo: `domain/model/Deload.kt`

- [ ] **Modificar `DeloadState`** (AC: CA-25, CA-26)
  - [ ] `DeloadActive`: `frozenVersionA/B/C: Int` → `frozenVersions: Map<Long, Int>` - Archivo: `domain/model/DeloadState.kt`
  - [ ] `DeloadRequired`: `modules: List<String>` → `routineIds: List<Long>`

- [ ] **Modificar `Exercise`** (AC: CA-15)
  - [ ] Eliminar `moduleCode: String` - Archivo: `domain/model/Exercise.kt`

- [ ] **Modificar `SubstituteExerciseInfo`** (AC: CA-22)
  - [ ] `moduleCode: String` → `muscleZoneIds: List<Long>` - Archivo: `domain/model/SubstituteExerciseInfo.kt`

- [ ] **Modificar `SessionPreviewExercise`** (AC: CA-20)
  - [ ] Eliminar `moduleCode`, `loadIncrementKg`. Agregar `muscleGroup: String?` - Archivo: `domain/model/SessionPreviewExercise.kt`

- [ ] **Modificar `SessionHistoryItem`** (AC: CA-32)
  - [ ] `moduleCode: String` → `routineName: String` - Archivo: `domain/model/SessionHistoryItem.kt`

- [ ] **Modificar `SessionDetail`** (AC: CA-32)
  - [ ] `moduleCode: String` → `routineName: String` - Archivo: `domain/model/SessionDetail.kt`

- [ ] **Modificar `PlanVersionDetail`**
  - [ ] `moduleCode: String` → `routineName: String` - Archivo: `domain/model/PlanVersionDetail.kt`

- [ ] **Modificar `DeloadHomeState`** (AC: CA-25)
  - [ ] `Active/Required`: `moduleCode` → `routineId`/`routineName` - Archivo: `domain/model/DeloadHomeState.kt`

- [ ] **Modificar `ExerciseHistoryEntry`** (AC: CA-33)
  - [ ] `moduleCode: String` → `routineName: String` - Archivo: `domain/model/ExerciseHistoryEntry.kt`

- [ ] **Modificar `SessionSummary`** (cambio mecánico)
  - [ ] `moduleCode: String` → `routineName: String` - Archivo: `domain/model/SessionSummary.kt`

- [ ] **Renombrar `RirByModule` → `RirByRoutine`** (cambio mecánico)
  - [ ] `moduleCode: String` → `routineId: Long`, agregar `routineName: String` - Archivo: `domain/model/RirByModule.kt` → `domain/model/RirByRoutine.kt`

- [ ] **Modificar `SetDistributionData`** (cambio mecánico)
  - [ ] `moduleCode: String` → `routineId: Long` - Archivo: `domain/model/SetDistributionData.kt`

- [ ] **Modificar `AlertTriggerData`** (cambio mecánico)
  - [ ] `RirTrigger.moduleCode` → `routineId: Long`. `InactivityTrigger.moduleCode` → `routineId: Long` - Archivo: `domain/model/AlertTriggerData.kt`

- [ ] **Renombrar `ModuleInactivityData` → `RoutineInactivityData`** (cambio mecánico)
  - [ ] `moduleCode: String` → `routineId: Long`, `moduleName: String` → `routineName: String` - Archivo: `domain/model/ModuleInactivityData.kt` → `domain/model/RoutineInactivityData.kt`

### Fase 4: Repositories (Hito 4)

#### 📦 Domain — RoutineRepository (Nuevo)

- [ ] **Crear interfaz `RoutineRepository`** (AC: CA-01 a CA-13)
  - [ ] `fun getRoutines(): Flow<List<Routine>>` - Archivo: `domain/repository/RoutineRepository.kt`
  - [ ] `suspend fun getRoutineById(id: Long): Routine?`
  - [ ] `suspend fun createRoutine(name: String): Long`
  - [ ] `suspend fun updateRoutineName(id: Long, name: String)`
  - [ ] `suspend fun deleteRoutine(id: Long)`
  - [ ] `suspend fun reorderRoutines(orderedIds: List<Long>)`
  - [ ] `fun getVersionsByRoutine(routineId: Long): Flow<List<RoutineVersion>>`
  - [ ] `suspend fun createVersion(routineId: Long): Long`
  - [ ] `suspend fun deleteVersion(versionId: Long)`
  - [ ] `suspend fun countRoutines(): Int`
  - [ ] `suspend fun hasActiveSessionForRoutine(routineId: Long): Boolean`

- [ ] **Crear `RoutineRepositoryImpl`** (AC: CA-01 a CA-13)
  - [ ] Inyectar: `routineDao`, `routineVersionDao`, `routineCurrentVersionDao`, `planAssignmentDao`, `sessionDao` - Archivo: `data/repository/RoutineRepositoryImpl.kt`
  - [ ] `createRoutine()`: validar nombre (no vacío, ≤50 chars, UNIQUE). Insertar rutina + V1 automática + `routine_current_version` con valor 1
  - [ ] `deleteRoutine()`: validar `countRoutines() > 1`, no sesión activa con esta rutina. Reordenar `sort_order` de restantes
  - [ ] `reorderRoutines()`: actualizar `sort_order` de cada rutina. Recalibrar `microcyclePosition` si necesario

#### 📦 Data — SessionRepositoryImpl (Rewrite parcial)

- [ ] **Modificar constructor de `SessionRepositoryImpl`**
  - [ ] Quitar: `moduleDao: ModuleDao`, `moduleVersionDao: ModuleVersionDao` - Archivo: `data/repository/SessionRepositoryImpl.kt`
  - [ ] Agregar: `routineDao: RoutineDao`, `routineVersionDao: RoutineVersionDao`, `routineCurrentVersionDao: RoutineCurrentVersionDao`, `deloadFrozenVersionDao: DeloadFrozenVersionDao`
  - [ ] Constructor pasa de 13 dependencias inyectadas (12 DAOs + 1 TensionDatabase) a 15 (14 DAOs + 1 TensionDatabase): quita 2 DAOs, agrega 4

- [ ] **`getNextModuleVersionId()` → `getNextRoutineVersionId()`** (AC: CA-20)
  - [ ] Consultar `routineDao.getAll()` para rutinas ordenadas
  - [ ] `RotationResolver.resolveRoutineId(position, routines)` para obtener rutina actual
  - [ ] `routineCurrentVersionDao.getByRoutineId()` para versión actual
  - [ ] `routineVersionDao.getByRoutineIdAndVersion()` para obtener el ID

- [ ] **`closeSession()` — advanceRotation con N rutinas** (AC: CA-19, CA-11, CA-30)
  - [ ] Consultar `routineCurrentVersionDao.getAllOnce()` para versiones actuales
  - [ ] `routineDao.countRoutines()` para total
  - [ ] `routineVersionDao.getVersionCountsByRoutine()` para total de versiones (query agregado, sin N+1)
  - [ ] `RotationResolver.advanceRotation()` con parámetros dinámicos
  - [ ] Si microciclo completó: actualizar `routine_current_version` por rutina

- [ ] **`activateDeload()` — escribir en `deload_frozen_version`** (AC: CA-26)
  - [ ] Copiar versiones actuales de `routine_current_version` a `deload_frozen_version`

- [ ] **Deload finalization** (AC: CA-26)
  - [ ] Comparar `countDeloadSessions(deloadId) == routineDao.countRoutines()` en vez de `== 6`

- [ ] **`evaluateRirOutOfRange()` — iterar rutinas dinámicas** (AC: CA-24, CA-28)
  - [ ] Reemplazar `listOf("A", "B", "C")` por `routineDao.getAll().map { it.id }` — fatiga evaluada por rutina individual
  - [ ] `getSessionIdsByModuleInRange` → `getSessionIdsByRoutineInRange(routineId)` — usar query batch `getSessionIdsByRoutinesInRange(routineIds: List<Long>)` para evitar N+1

- [ ] **`evaluateModuleInactivity()` → `evaluateRoutineInactivity()`** (AC: CA-25, CA-31)
  - [ ] Iterar `routineDao.getAll()` en vez de `listOf("A", "B", "C")`
  - [ ] Zonas musculares dinámicas: query `plan_assignment` + `exercise_muscle_zone` + `muscle_zone` para la rutina, en vez de `MUSCLE_GROUPS_BY_MODULE[moduleCode]`
  - [ ] Usar query batch para obtener zonas de todas las rutinas en una consulta (evitar N+1)

- [ ] **`getSessionPreviewExercises()` — LoadIncrementResolver** (AC: CA-20)
  - [ ] `loadIncrementKg` se resuelve con `LoadIncrementResolver.resolve(muscleGroup)` en el mapper en vez del campo del DTO

- [ ] **Mapper global de `moduleCode` → `routineName`/`routineId`**
  - [ ] Actualizar todos los mappers de DTOs en los ~15 métodos restantes

#### 📦 Data — Otros Repositories

- [ ] **Modificar `AlertRepositoryImpl`** (AC: CA-24, CA-25, CA-31)
  - [ ] `moduleCode` → `routineId: Long` en ~10 puntos - Archivo: `data/repository/AlertRepositoryImpl.kt`
  - [ ] `MUSCLE_GROUPS_BY_MODULE[moduleCode]` → query dinámico de zonas musculares por rutina
  - [ ] Mensajes: "Módulo ${moduleCode}" → "${routineName}" (obtener nombre desde `routineDao.getById()`)

- [ ] **Modificar `MetricsRepositoryImpl`** (AC: CA-28, CA-29)
  - [ ] `getRirValuesByModule(moduleCode: String)` → `getRirValuesByRoutine(routineId: Long)` - Archivo: `data/repository/MetricsRepositoryImpl.kt`

- [ ] **Modificar `ExerciseRepositoryImpl`** (AC: CA-15, CA-22)
  - [ ] Eliminar `moduleCode` como parámetro de filtrado - Archivo: `data/repository/ExerciseRepositoryImpl.kt`
  - [ ] `getEligibleSubstitutes(moduleCode, sessionId)` → `getEligibleSubstitutes(muscleZoneIds: List<Long>, sessionId: Long)`

- [ ] **Modificar `PlanRepositoryImpl`**
  - [ ] `moduleVersionId` → `routineVersionId` - Archivo: `data/repository/PlanRepositoryImpl.kt`
  - [ ] Eliminar agrupación por `moduleCode`
  - [ ] `getAvailableExercisesForVersion(moduleCode, moduleVersionId)` → `getAvailableExercisesForVersion(routineVersionId: Long)` (sin filtro por módulo)

#### 📦 Domain — Interfaces de Repository

- [ ] **Modificar `SessionRepository`** (AC: CA-20)
  - [ ] `getNextModuleVersionId()` → `getNextRoutineVersionId()` - Archivo: `domain/repository/SessionRepository.kt`
  - [ ] `getSessionPreviewExercises(moduleVersionId)` → `getSessionPreviewExercises(routineVersionId)`
  - [ ] Propagar cambios de modelo en firmas de métodos

- [ ] **Modificar `PlanRepository`**
  - [ ] `getAvailableExercisesForVersion(moduleCode, moduleVersionId)` → `getAvailableExercisesForVersion(routineVersionId: Long)` - Archivo: `domain/repository/PlanRepository.kt`

- [ ] **Modificar `ExerciseRepository`**
  - [ ] Firmas sin `moduleCode` - Archivo: `domain/repository/ExerciseRepository.kt`

- [ ] **Modificar `MetricsRepository`**
  - [ ] `moduleCode: String` → `routineId: Long` - Archivo: `domain/repository/MetricsRepository.kt`

#### 📦 DI

- [ ] **Agregar bindings para `RoutineRepository`**
  - [ ] `@Binds fun bindRoutineRepository(impl: RoutineRepositoryImpl): RoutineRepository` - Archivo: `di/RepositoryModule.kt`
  - [ ] Exponer nuevos DAOs desde `DatabaseModule`: `routineDao()`, `routineVersionDao()`, `routineCurrentVersionDao()`, `deloadFrozenVersionDao()`

### Fase 5: UseCases de Gestión de Rutinas (Hito 5)

> **Dependencia:** Fase 4 (RoutineRepository interfaz + impl + bindings Hilt) debe estar completada antes de iniciar esta fase.

#### 📦 Domain — UseCases Nuevos

- [ ] **Crear `CreateRoutineUseCase`** (AC: CA-01, CA-06)
  - [ ] Valida nombre (max 50 chars, no vacío, no duplicado). Delega a `routineRepository.createRoutine(name)` - Archivo: `domain/usecase/routine/CreateRoutineUseCase.kt`
  - [ ] Test unitario - Archivo: `src/test/.../domain/usecase/routine/CreateRoutineUseCaseTest.kt`

- [ ] **Crear `UpdateRoutineNameUseCase`** (AC: CA-02)
  - [ ] Valida nombre, delega a `routineRepository.updateRoutineName(id, name)` - Archivo: `domain/usecase/routine/UpdateRoutineNameUseCase.kt`
  - [ ] Test unitario - Archivo: `src/test/.../domain/usecase/routine/UpdateRoutineNameUseCaseTest.kt`

- [ ] **Crear `DeleteRoutineUseCase`** (AC: CA-03)
  - [ ] Valida `countRoutines() > 1`, no sesión activa. Delega a `routineRepository.deleteRoutine(id)` - Archivo: `domain/usecase/routine/DeleteRoutineUseCase.kt`
  - [ ] Test unitario - Archivo: `src/test/.../domain/usecase/routine/DeleteRoutineUseCaseTest.kt`

- [ ] **Crear `ReorderRoutinesUseCase`** (AC: CA-04)
  - [ ] Recibe lista ordenada de IDs, delega a `routineRepository.reorderRoutines(orderedIds)` - Archivo: `domain/usecase/routine/ReorderRoutinesUseCase.kt`
  - [ ] Test unitario - Archivo: `src/test/.../domain/usecase/routine/ReorderRoutinesUseCaseTest.kt`

- [ ] **Crear `CreateRoutineVersionUseCase`** (AC: CA-07)
  - [ ] Delega a `routineRepository.createVersion(routineId)` - Archivo: `domain/usecase/routine/CreateRoutineVersionUseCase.kt`
  - [ ] Test unitario - Archivo: `src/test/.../domain/usecase/routine/CreateRoutineVersionUseCaseTest.kt`

- [ ] **Crear `DeleteRoutineVersionUseCase`** (AC: CA-08)
  - [ ] Valida `countByRoutineId() > 1`. Delega a `routineRepository.deleteVersion(versionId)` - Archivo: `domain/usecase/routine/DeleteRoutineVersionUseCase.kt`
  - [ ] Test unitario - Archivo: `src/test/.../domain/usecase/routine/DeleteRoutineVersionUseCaseTest.kt`

### Fase 6: UseCases Existentes Modificados (Hito 6)

#### 📦 Domain — UseCases Modificados

- [ ] **Modificar `GetNextSessionInfoUseCase`** (AC: CA-16, CA-20)
  - [ ] Usar `RotationResolver` (reescrito para N rutinas) + `RoutineCurrentVersionDao`. Retornar `NextSession` con `routineId`, `routineName`, `routineVersionId` - Archivo: `domain/usecase/session/GetNextSessionInfoUseCase.kt`
  - [ ] Test: modificar - Archivo: `src/test/.../domain/usecase/session/GetNextSessionInfoUseCaseTest.kt`

- [ ] **Modificar `StartSessionUseCase`** (AC: CA-20)
  - [ ] `moduleVersionId` → `routineVersionId` - Archivo: `domain/usecase/session/StartSessionUseCase.kt`
  - [ ] Test: modificar - Archivo: `src/test/.../domain/usecase/session/StartSessionUseCaseTest.kt`

- [ ] **Modificar `GetSessionPreviewUseCase`** (AC: CA-20)
  - [ ] `moduleVersionId` → `routineVersionId` - Archivo: `domain/usecase/session/GetSessionPreviewUseCase.kt`
  - [ ] Test: modificar - Archivo: `src/test/.../domain/usecase/session/GetSessionPreviewUseCaseTest.kt`

- [ ] **Modificar `SubstituteExerciseUseCase`** (AC: CA-22)
  - [ ] Filtro por zona muscular en vez de `moduleCode` - Archivo: `domain/usecase/session/SubstituteExerciseUseCase.kt`
  - [ ] Test: modificar - Archivo: `src/test/.../domain/usecase/session/SubstituteExerciseUseCaseTest.kt`

- [ ] **Modificar `GetPlanVersionDetailUseCase`**
  - [ ] `moduleVersionId` → `routineVersionId` - Archivo: `domain/usecase/plan/GetPlanVersionDetailUseCase.kt`

- [ ] **Modificar `AssignExerciseToVersionUseCase`** (AC: CA-12)
  - [ ] `moduleVersionId` → `routineVersionId` - Archivo: `domain/usecase/plan/AssignExerciseToVersionUseCase.kt`

- [ ] **Modificar `UnassignExerciseFromVersionUseCase`** (AC: CA-13)
  - [ ] `moduleVersionId` → `routineVersionId` - Archivo: `domain/usecase/plan/UnassignExerciseFromVersionUseCase.kt`

- [ ] **Modificar `GetSessionSummaryUseCase`** (AC: CA-32)
  - [ ] `moduleCode` → `routineName` - Archivo: `domain/usecase/session/GetSessionSummaryUseCase.kt`

- [ ] **Modificar `ExportBackupUseCase`** (AC: CA-34)
  - [ ] Esquema con tablas `routine`, `routine_version`, `routine_current_version`, `deload_frozen_version` - Archivo: `domain/usecase/backup/ExportBackupUseCase.kt`
  - [ ] Test: modificar - Archivo: `src/test/.../domain/usecase/backup/ExportBackupUseCaseTest.kt`

- [ ] **Modificar `ImportBackupUseCase`** (AC: CA-34)
  - [ ] Detectar versión del backup (v8 con modules vs v9 con routines). Transformar v8→v9 en importación - Archivo: `domain/usecase/backup/ImportBackupUseCase.kt`
  - [ ] Test: modificar - Archivo: `src/test/.../domain/usecase/backup/ImportBackupUseCaseTest.kt`

- [ ] **Modificar `ValidateBackupUseCase`** (AC: CA-34)
  - [ ] Validar esquema v9 (tablas routine) - Archivo: `domain/usecase/backup/ValidateBackupUseCase.kt`
  - [ ] Test: modificar - Archivo: `src/test/.../domain/usecase/backup/ValidateBackupUseCaseTest.kt`

### Fase 7: UI — Gestión del Plan (Hito 7)

#### 📦 UI — Pantallas Nuevas (`ui/catalog/`)

- [ ] **Crear `RoutineListUiState`** (AC: CA-01, CA-04, CA-05)
  - [ ] `isLoading: Boolean`, `routines: List<RoutineItem>`, `showCreateDialog: Boolean`, `createDialogName: String`, `deleteTarget: RoutineItem?` - Archivo: `ui/catalog/RoutineListUiState.kt`
  - [ ] `data class RoutineItem(val id: Long, val name: String, val sortOrder: Int, val versionCount: Int)`

- [ ] **Crear `RoutineListViewModel`** (AC: CA-01, CA-02, CA-03, CA-04)
  - [ ] `@HiltViewModel` con `CreateRoutineUseCase`, `UpdateRoutineNameUseCase`, `DeleteRoutineUseCase`, `ReorderRoutinesUseCase`, `RoutineRepository` - Archivo: `ui/catalog/RoutineListViewModel.kt`
  - [ ] Acciones: `createRoutine(name)`, `updateName(id, name)`, `deleteRoutine(id)`, `reorderRoutines(orderedIds)`

- [ ] **Crear `RoutineListScreen`** (AC: CA-01, CA-02, CA-03, CA-04)
  - [ ] `CenterAlignedTopAppBar` con título "Mi Plan" - Archivo: `ui/catalog/RoutineListScreen.kt`
  - [ ] `LazyColumn` con rutinas en sort_order. Cada item: nombre, conteo de versiones, acciones (editar, eliminar)
  - [ ] Drag-and-drop para reordenar (CA-04) via `Modifier.clickable` + dialog de confirmación
  - [ ] FAB "Crear rutina" → dialog con input de nombre (max 50 chars)
  - [ ] Dialog de confirmación para eliminar (CA-03)
  - [ ] Tap rutina → navega a versiones
  - [ ] Bottom Nav visible

- [ ] **Crear `RoutineVersionListUiState`** (AC: CA-07, CA-08)
  - [ ] `isLoading: Boolean`, `routineName: String`, `versions: List<VersionItem>`, `deleteTarget: VersionItem?` - Archivo: `ui/catalog/RoutineVersionListUiState.kt`
  - [ ] `data class VersionItem(val id: Long, val versionNumber: Int, val exerciseCount: Int)`

- [ ] **Crear `RoutineVersionListViewModel`** (AC: CA-07, CA-08)
  - [ ] `@HiltViewModel` con `CreateRoutineVersionUseCase`, `DeleteRoutineVersionUseCase`, `RoutineRepository`, `SavedStateHandle` - Archivo: `ui/catalog/RoutineVersionListViewModel.kt`

- [ ] **Crear `RoutineVersionListScreen`** (AC: CA-07, CA-08)
  - [ ] `CenterAlignedTopAppBar` con `navigationIcon: ArrowBack`, título: nombre de rutina - Archivo: `ui/catalog/RoutineVersionListScreen.kt`
  - [ ] `LazyColumn` con versiones, conteo de ejercicios
  - [ ] FAB "Crear versión". Botón eliminar versión con confirmación (solo si >1 versión)
  - [ ] Tap versión → navega a `PlanVersionDetailScreen(routineVersionId)`

#### 📦 UI — Navegación

- [ ] **Agregar rutas en `NavigationRoutes`** (AC: CA-01, CA-07)
  - [ ] `ROUTINE_LIST = "routine-list"` - Archivo: `ui/navigation/NavigationRoutes.kt`
  - [ ] `ROUTINE_VERSIONS = "routine-versions/{routineId}"` + fun helper
  - [ ] Modificar rutas existentes: `moduleVersionId` → `routineVersionId`, `moduleCode` → `routineId` en params de sesión/preview

- [ ] **Agregar composables en `TensionNavHost`** (AC: CA-01, CA-07)
  - [ ] Composable para `RoutineListScreen` - Archivo: `ui/navigation/TensionNavHost.kt`
  - [ ] Composable para `RoutineVersionListScreen` con argumento `routineId: Long`
  - [ ] Bottom Nav visible para ambas pantallas

- [ ] **Modificar `BottomNavigationBar`**
  - [ ] Reemplazar tab "Diccionario" por "Plan" (o agregar tab) que navega a `ROUTINE_LIST` - Archivo: `ui/components/BottomNavigationBar.kt`
  - [ ] Alternativa: "Diccionario" y "Plan" como tabs horizontales dentro de `RoutineListScreen` (preservar acceso al diccionario)

#### 📦 UI — PlanVersionDetailScreen

- [ ] **Modificar `PlanVersionDetailViewModel`**
  - [ ] `moduleVersionId` → `routineVersionId` en `SavedStateHandle` y UseCases - Archivo: `ui/catalog/PlanVersionDetailViewModel.kt`

- [ ] **Modificar `PlanVersionDetailScreen`**
  - [ ] Adaptar parámetros de navegación - Archivo: `ui/catalog/PlanVersionDetailScreen.kt`

#### 📦 Recursos

- [ ] **Agregar strings para gestión de rutinas** (AC: CA-01 a CA-08)
  - [ ] "Mi Plan", "Crear rutina", "Nombre de la rutina", "Eliminar rutina", "Crear versión", "Eliminar versión", confirmaciones - Archivo: `res/values/strings.xml`

### Fase 8: UI — Pantallas Existentes Modificadas (Hito 8)

#### 📦 UI — Home

- [ ] **Modificar `HomeUiState`** (AC: CA-06, CA-20)
  - [ ] `NextSession` con `routineName` en vez de `moduleCode`. Agregar `hasPlan: Boolean` para empty state - Archivo: `ui/home/HomeUiState.kt`

- [ ] **Modificar `HomeViewModel`** (AC: CA-06, CA-20)
  - [ ] `startSession(routineVersionId)`. Empty state: si `routineRepository.countRoutines() == 0`, mostrar CTA - Archivo: `ui/home/HomeViewModel.kt`

- [ ] **Modificar `HomeScreen`** (AC: CA-06)
  - [ ] NextSessionCard: mostrar `routineName`. Si `!hasPlan`: empty state con CTA "Crear tu primer plan" → navega a `RoutineListScreen` - Archivo: `ui/home/HomeScreen.kt`

#### 📦 UI — Sesión Activa

- [ ] **Modificar `ActiveSessionUiState`** (AC: CA-21)
  - [ ] `moduleCode: String` → `routineName: String` - Archivo: `ui/session/ActiveSessionUiState.kt`

- [ ] **Modificar `ActiveSessionViewModel`** (AC: CA-21)
  - [ ] Header con `routineName`. `LoadDisplayMapper` recibe `LoadIncrementResolver.resolve(muscleGroup)` en vez de `detail.loadIncrementKg` - Archivo: `ui/session/ActiveSessionViewModel.kt`

- [ ] **Modificar `SessionPreviewViewModel`** (AC: CA-20)
  - [ ] `moduleCode` → `routineName`. `loadIncrementKg` via `LoadIncrementResolver` - Archivo: `ui/preview/SessionPreviewViewModel.kt`

- [ ] **Modificar `SubstituteExerciseViewModel`** (AC: CA-22)
  - [ ] Filtrar sustitutos por zona muscular del ejercicio actual (no por módulo) - Archivo: `ui/session/SubstituteExerciseViewModel.kt`
  - [ ] Obtener `muscleZoneIds` del ejercicio, pasar a `exerciseRepository.getEligibleSubstitutes(muscleZoneIds, sessionId)`
  - [ ] Test: modificar - Archivo: `src/test/.../ui/session/SubstituteExerciseViewModelTest.kt`

#### 📦 UI — Historial

- [ ] **Modificar pantallas de historial** (AC: CA-32, CA-33)
  - [ ] `SessionHistoryScreen`: `moduleCode` → `routineName` - Archivo: `ui/history/SessionHistoryScreen.kt`
  - [ ] `SessionDetailScreen`: `moduleCode` → `routineName` - Archivo: `ui/history/SessionDetailScreen.kt`
  - [ ] `ExerciseHistoryScreen`: `moduleCode` → `routineName` - Archivo: `ui/history/ExerciseHistoryScreen.kt`
  - [ ] Tests: modificar `SessionHistoryViewModelTest.kt`, `SessionDetailViewModelTest.kt`, `ExerciseHistoryViewModelTest.kt`

#### 📦 UI — Catálogo

- [ ] **Modificar `ExerciseDictionaryScreen`** (AC: CA-15)
  - [ ] Eliminar `moduleCode`/`moduleName` de display de ejercicios - Archivo: `ui/catalog/ExerciseDictionaryScreen.kt`
  - [ ] Eliminar filtro por módulo (ejercicios agnósticos)

- [ ] **Modificar `ExerciseDetailScreen`/`ExerciseDetailViewModel`/`ExerciseDetailUiState`** (AC: CA-15)
  - [ ] Eliminar `moduleCode` y `moduleName` de `ExerciseDetailUiState` (`ExerciseDetailItem`) - Archivo: `ui/catalog/ExerciseDetailUiState.kt`
  - [ ] Eliminar mapeo de `exercise.moduleCode`/`exercise.moduleName` en ViewModel - Archivo: `ui/catalog/ExerciseDetailViewModel.kt`
  - [ ] Eliminar display de módulo en la pantalla de detalle - Archivo: `ui/catalog/ExerciseDetailScreen.kt`

- [ ] **Modificar `CreateExerciseScreen`/`CreateExerciseViewModel`/`CreateExerciseUiState`** (AC: CA-15)
  - [ ] Eliminar selector de módulo (dropdown A/B/C) en `CreateExerciseScreen` - Archivo: `ui/catalog/CreateExerciseScreen.kt`
  - [ ] Eliminar `selectedModuleCode`, `modules`, `onModuleSelected()` y validación de módulo en ViewModel - Archivo: `ui/catalog/CreateExerciseViewModel.kt`
  - [ ] Eliminar campos `selectedModuleCode` y `modules` de `CreateExerciseUiState` - Archivo: `ui/catalog/CreateExerciseUiState.kt`
  - [ ] Actualizar `CreateExerciseUseCase` para no recibir `moduleCode` - Archivo: `domain/usecase/catalog/CreateExerciseUseCase.kt`

- [ ] **Eliminar `TrainingPlanScreen`/`TrainingPlanViewModel`/`TrainingPlanUiState`** (reemplazados por `RoutineListScreen`)
  - [ ] Eliminar `TrainingPlanScreen.kt` - Archivo: `ui/catalog/TrainingPlanScreen.kt`
  - [ ] Eliminar `TrainingPlanViewModel.kt` - Archivo: `ui/catalog/TrainingPlanViewModel.kt`
  - [ ] Eliminar `TrainingPlanUiState.kt` - Archivo: `ui/catalog/TrainingPlanUiState.kt`
  - [ ] Eliminar ruta `TRAINING_PLAN` de `NavigationRoutes.kt` (reemplazada por `ROUTINE_LIST`)
  - [ ] Eliminar composable de `TrainingPlanScreen` en `TensionNavHost.kt`
  - [ ] Actualizar tests que referencien `TrainingPlanViewModel` - Archivo: `src/test/.../ui/catalog/TrainingPlanViewModelTest.kt`

#### 📦 UI — Alertas, Deload, Métricas

- [ ] **Modificar `AlertCenterScreen`/`AlertDetailScreen`** (AC: CA-31)
  - [ ] `moduleCode` → `routineName` en display - Archivos: `ui/alerts/AlertCenterScreen.kt`, `ui/alerts/AlertDetailScreen.kt`
  - [ ] Tests: modificar `AlertCenterViewModelTest.kt`, `AlertDetailViewModelTest.kt`

- [ ] **Modificar `DeloadManagementScreen`/ViewModel** (AC: CA-25, CA-26)
  - [ ] Adaptar modelo `DeloadState`/`DeloadHomeState` a Map dinámico - Archivos: `ui/deload/DeloadManagementScreen.kt`, `ui/deload/DeloadManagementViewModel.kt`

- [ ] **Modificar `MetricsScreen`/ViewModel** (AC: CA-28, CA-29)
  - [ ] `moduleCode` → `routineId` en params de consulta - Archivos: `ui/metrics/MetricsScreen.kt`, `ui/metrics/MetricsViewModel.kt`

#### 📦 UI — Backup

- [ ] **Modificar ViewModels de backup** (AC: CA-34)
  - [ ] Adaptar a nuevos UseCases de export/import - Archivos: `ui/settings/ExportBackupViewModel.kt`, `ui/settings/ImportBackupViewModel.kt`
  - [ ] Tests: modificar `ExportBackupViewModelTest.kt`, `ImportBackupViewModelTest.kt`

#### 📦 UI — Navegación Global

- [ ] **Modificar `NavigationRoutes`**
  - [ ] Parámetros: `moduleVersionId` → `routineVersionId`, `moduleCode` → `routineId` en rutas existentes - Archivo: `ui/navigation/NavigationRoutes.kt`

- [ ] **Modificar `TensionNavHost`**
  - [ ] Actualizar composables existentes con nuevos parámetros de navegación - Archivo: `ui/navigation/TensionNavHost.kt`
  - [ ] Agregar callback `onNavigateToRoutineList` en HomeScreen
  - [ ] Agregar callback `onNavigateToRoutineVersions` en RoutineListScreen

### Fase N: QA y Deployment

#### 📦 Code Quality

- [ ] **Ejecutar Agente Peer Review** (MANUAL)
- [ ] **Resolver incidentes del Peer Review** (MANUAL, condicional)

#### 📦 Deployment DEV

- [ ] **Crear Pull Request** (MANUAL)
- [ ] **Ejecutar pipeline deployment DEV** (MANUAL)

#### 📦 Testing Manual

- [ ] **Diseñar set de pruebas manuales** (MANUAL)
  - Migración: Instalar app v8 con datos (sesiones, deloads, alertas). Actualizar a v9. Verificar que todo funciona idénticamente (CA-35, CA-36)
  - Gestión de rutinas: Crear rutina (CA-01), editar nombre (CA-02), eliminar con confirmación (CA-03), reordenar (CA-04)
  - Gestión de versiones: Crear versión (CA-07), eliminar con confirmación (CA-08)
  - Asignación: Asignar ejercicio a versión (CA-12), desasignar (CA-13), mismo ejercicio en múltiples versiones (CA-14)
  - Rotación: Verificar rotación con N rutinas creadas por usuario (CA-16, CA-17, CA-19)
  - Sesión: Iniciar sesión con rutina custom, verificar prescripción correcta (CA-20, CA-21)
  - Sustitución: Verificar que sustitutos se filtran por zona muscular (CA-22)
  - Deload: Activar deload con rutinas custom, verificar duración = N sesiones (CA-26)
  - Progresión: Verificar incremento por zona muscular (CA-23), detección fatiga por rutina (CA-24, CA-25)
  - Historial: Verificar nombre de rutina en sesiones pasadas (CA-32), datos previos preservados (CA-34)
  - Backup: Exportar v9, importar v9. Exportar con app v8, importar en v9 (CA-34)
  - Empty state: Nueva instalación v9 → plan vacío, CTA para crear plan (CA-06)
- [ ] **Ejecutar pruebas manuales** (MANUAL)

---

**Notas sobre vinculación con Criterios de Aceptación:**
- Cada tarea indica qué criterio(s) de aceptación ayuda a completar usando (AC: X, Y, Z)
- La Fase 1 (migración) cubre CA-35, CA-36, CA-37 (migración de datos) y es prerequisito para todas las demás
- Las tareas de Fase N son MANUALES — no aplica descuento Método Ceiba en estimación

---

## Notas para el equipo

### Dependencias

- **Ninguna historia previa es prerequisito directo.** HU-22 opera sobre el esquema v8 existente y lo transforma. Todas las HUs previas (1-21) ya están reflejadas en el código base actual.
- **HU-18 (Backup/Restore):** Impacto en esquema de exportación. La lógica de import debe detectar backups v8 (con módulos) y transformarlos al formato v9 (con rutinas).
- **HU-21 (Preview de sesión):** El preview se reparametriza de `moduleVersionId` a `routineVersionId`. `LoadDisplayMapper` usa `LoadIncrementResolver` en vez de `loadIncrementKg` del DTO.

### Secuencia de implementación

1. **Arquitecto:** ✅ Completado (2026-05-06). Análisis arquitectónico con delta completo v8→v9, migración SQL, 8 hitos.
2. **Desarrollador:** ✅ Completado (2026-05-06). Refinamiento técnico con 9 fases (Fases 1-8 + Fase N), ~120 subtareas, 15 tests nuevos, 20+ tests modificados.
3. **Desarrollador:** ⏳ Pendiente. Implementar las 9 fases de tareas definidas en el refinamiento técnico.

### Principio rector

HU-22 materializa la transición de un sistema con estructura fija (3 módulos hardcoded) a uno completamente personalizable por el usuario. Cada decisión de diseño prioriza la **compatibilidad hacia atrás** (migración sin pérdida de datos, CA-35/36/37) y la **escalabilidad** (N rutinas con M versiones heterogéneas). La lógica de negocio que antes dependía de constantes ("A", "B", "C") ahora opera sobre datos dinámicos consultados en runtime — esto es más flexible pero requiere queries adicionales donde antes bastaba un string literal.

<!-- timestamp_inicio_refinamiento: 2026-05-06 16:38 -->
