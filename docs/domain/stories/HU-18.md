# HU-18 — Sistema de Alertas

## §1 Metadatos

| Campo | Valor |
|---|---|
| **ID** | HU-18 |
| **ID Legado** | HU-17 (consolida HU-26 a HU-30 originales) |
| **Épica** | EPIC-03 — Motor de Progresión y Alertas |
| **Estado** | Done |
| **Requisitos** | RF53, RF54, RF55, RF56, RF57, RF58, RNF05 |
| **Dependencias** | HU-12 (infraestructura de alertas), HU-14 (deload — `deload_id`), HU-15 (Rules de KPIs), HU-17 (F3 para navegación H2→F3) |
| **Habilita** | HU-19 (backup — tabla `alert` con alertas resueltas se exporta como histórico) |

### Historias originales consolidadas

- **HU-26 original** — Alertas de tasa de progresión baja (RF53)
- **HU-27 original** — Alertas de RIR por módulo (RF54, RF55)
- **HU-28 original** — Alertas de adherencia baja (RF56)
- **HU-29 original** — Alertas de caída de tonelaje por grupo muscular (RF57)
- **HU-30 original** — Alertas de inactividad por módulo (RF58)

---

## §2 Narrativa de Negocio

Como ejecutante, quiero que el sistema emita alertas proactivas con dos niveles de severidad (alerta y crisis) cuando detecte condiciones que requieran mi atención — tasa de progresión baja, RIR fuera de rango por módulo, adherencia semanal insuficiente, caída de tonelaje por grupo muscular o inactividad prolongada por módulo — para recibir señales tempranas y objetivas de problemas antes de que comprometan mis adaptaciones, sin que estas señales bloqueen mi autonomía de entrenamiento.

**Patrón arquitectónico:** Motor de Alertas Write-Time en Pipeline Existente + MVVM Read-Only para 2 pantallas (H1, H2) con `AlertRepository` dedicado.

HU-18 tiene dos caras:
- **Write-side:** extiende `SessionRepositoryImpl.evaluateProgression()` con 5 bloques de evaluación nuevos (Steps 7-11) que crean/resuelven alertas al cierre de cada sesión, reutilizando las Rules de HU-15.
- **Read-side:** crea 2 pantallas nuevas (H1 Centro de Alertas, H2 Detalle de Alerta) de solo lectura con recálculo dinámico de datos.

---

## §3 Criterios de Aceptación (BDD)

### Bloque A — Alertas de Tasa de Progresión Baja (RF53)

#### CA-18.01 — Alerta por tasa de progresión < 40%

**Dado que** el sistema calcula la Tasa de Progresión de un ejercicio en un período de 4 semanas,
**cuando** el resultado es menor al 40%,
**entonces** emite una alerta informativa indicando el nombre del ejercicio, su tasa de progresión actual y que está por debajo del umbral de alerta.

#### CA-18.02 — Alerta de crisis por tasa de progresión < 20%

**Dado que** el sistema calcula la Tasa de Progresión de un ejercicio en un período de 4 semanas,
**cuando** el resultado es menor al 20%,
**entonces** emite una alerta de crisis con mayor urgencia visual, indicando el nombre del ejercicio, su tasa de progresión actual y que está en estado crítico de estancamiento.

#### CA-18.03 — Diferenciación visual entre alerta y crisis de progresión

**Dado que** el sistema emite una alerta de tasa de progresión,
**cuando** el ejecutante visualiza la alerta,
**entonces** la alerta de nivel normal (< 40%) y la de crisis (< 20%) son visualmente distinguibles entre sí mediante colores e iconografía diferenciada, sin depender únicamente del texto.

#### CA-18.04 — Alertas de progresión informativas, no bloqueantes

**Dado que** el sistema emite una alerta de tasa de progresión baja,
**cuando** el ejecutante interactúa con el sistema,
**entonces** la alerta es informativa y no impide iniciar sesiones, registrar series ni cerrar sesiones; el ejecutante mantiene autonomía total.

#### CA-18.05 — Evaluación periódica de progresión

**Dado que** el sistema evalúa la tasa de progresión,
**cuando** se completa un período de 4 semanas o un microciclo,
**entonces** recalcula la tasa y emite o retira alertas según corresponda al estado actual de cada ejercicio.

---

### Bloque B — Alertas de RIR por Módulo (RF54, RF55)

#### CA-18.06 — Alerta por RIR Promedio < 1.5 (intensidad excesiva)

**Dado que** el sistema calcula el RIR Promedio de un módulo,
**cuando** el resultado es < 1.5 de forma sostenida durante 2 o más sesiones del mismo módulo,
**entonces** emite una alerta al ejecutante indicando: el módulo afectado, el RIR Promedio actual, que está entrenando demasiado cerca del fallo técnico de forma sostenida, y recomienda prescribir una descarga para permitir recuperación del SNC.

#### CA-18.07 — Alerta por RIR Promedio > 3.5 (intensidad insuficiente)

**Dado que** el sistema calcula el RIR Promedio de un módulo,
**cuando** el resultado es > 3.5 de forma sostenida durante 2 o más sesiones del mismo módulo,
**entonces** emite una alerta al ejecutante indicando: el módulo afectado, el RIR Promedio actual, que el estímulo puede ser insuficiente para generar adaptación, y recomienda incrementar la carga de los ejercicios del módulo.

#### CA-18.08 — Condición de "sostenido" durante 2+ sesiones

**Dado que** el sistema evalúa si el RIR Promedio está fuera de rango de forma sostenida,
**cuando** determina si emitir la alerta,
**entonces** verifica que la condición (< 1.5 o > 3.5) se cumple en las últimas 2 o más sesiones consecutivas del mismo módulo; una sola sesión fuera de rango no dispara la alerta.

#### CA-18.09 — Diferenciación visual entre alertas de RIR

**Dado que** el sistema emite alertas de RIR por módulo,
**cuando** el ejecutante visualiza las alertas,
**entonces** la alerta por RIR bajo (< 1.5, riesgo de fatiga) y la alerta por RIR alto (> 3.5, estímulo insuficiente) son visualmente distinguibles entre sí mediante colores e iconografía diferenciada.

#### CA-18.10 — Alertas de RIR informativas, no bloqueantes

**Dado que** el sistema emite una alerta de RIR por módulo,
**cuando** el ejecutante interactúa con el sistema,
**entonces** la alerta es informativa; no impide entrenar ni fuerza una descarga automática. El ejecutante decide si seguir la recomendación.

#### CA-18.11 — Retiro automático de alerta de RIR

**Dado que** el sistema ha emitido una alerta de RIR por módulo,
**cuando** las sesiones posteriores del módulo retornan el RIR Promedio a la zona óptima (1.5-3.5),
**entonces** el sistema retira la alerta activa para ese módulo.

---

### Bloque C — Alertas de Adherencia Baja (RF56)

#### CA-18.12 — Alerta informativa por adherencia < 60% en una semana

**Dado que** el sistema calcula el Índice de Adherencia al finalizar una semana natural,
**cuando** el resultado es < 60%,
**entonces** emite una alerta informativa indicando la adherencia de la semana, cuántas sesiones se completaron respecto al objetivo, y que la baja frecuencia puede afectar la resolución temporal de las señales del sistema.

#### CA-18.13 — Alerta de crisis por adherencia < 60% durante 2+ semanas consecutivas

**Dado que** el sistema calcula el Índice de Adherencia semanal,
**cuando** el resultado es < 60% durante 2 o más semanas consecutivas,
**entonces** emite una alerta de crisis con mayor urgencia visual, indicando la racha de semanas con baja adherencia y advirtiendo que las comparaciones entre sesiones pierden validez por el excesivo tiempo entre ellas.

#### CA-18.14 — Diferenciación visual entre alerta y crisis de adherencia

**Dado que** el sistema emite alertas de adherencia,
**cuando** el ejecutante visualiza las alertas,
**entonces** la alerta informativa (1 semana < 60%) y la de crisis (2+ semanas < 60%) son visualmente distinguibles mediante colores e iconografía diferenciada.

#### CA-18.15 — Alertas de adherencia informativas, no bloqueantes

**Dado que** el sistema emite una alerta de adherencia baja,
**cuando** el ejecutante interactúa con el sistema,
**entonces** la alerta es informativa; no impide entrenar, registrar sesiones ni acceder a ninguna funcionalidad. El ejecutante es autónomo.

#### CA-18.16 — Retiro de alerta de crisis de adherencia

**Dado que** el sistema ha emitido una alerta de crisis por adherencia,
**cuando** el ejecutante completa una semana con adherencia ≥ 60%,
**entonces** el sistema retira la alerta de crisis, manteniendo el registro histórico de las semanas con baja adherencia.

---

### Bloque D — Alertas de Caída de Tonelaje por Grupo Muscular (RF57)

#### CA-18.17 — Alerta por caída de tonelaje > 10%

**Dado que** el sistema calcula el Tonelaje por Grupo Muscular al completar un microciclo,
**cuando** el tonelaje de un grupo muscular cae más del 10% respecto al microciclo anterior,
**entonces** emite una alerta indicando el grupo muscular afectado, el porcentaje de caída y el tonelaje comparado de ambos microciclos.

#### CA-18.18 — Alerta de crisis por caída de tonelaje > 20%

**Dado que** el sistema calcula el Tonelaje por Grupo Muscular al completar un microciclo,
**cuando** el tonelaje de un grupo muscular cae más del 20% respecto al microciclo anterior,
**entonces** emite una alerta de crisis con mayor urgencia visual, indicando que la pérdida de volumen es severa y puede comprometer las adaptaciones del grupo muscular.

#### CA-18.19 — Verificación de descarga planificada

**Dado que** el sistema detecta una caída de tonelaje en un grupo muscular,
**cuando** analiza la causa de la caída,
**entonces** verifica si el microciclo evaluado corresponde a un período de descarga activa (HU-14); si es descarga planificada, la alerta se contextualiza como "Caída esperada por descarga" y no se clasifica como crisis.

#### CA-18.20 — Diferenciación entre descarga y regresión

**Dado que** el sistema emite una alerta de caída de tonelaje,
**cuando** la caída corresponde a una descarga planificada,
**entonces** la alerta indica claramente "Descarga planificada — caída de tonelaje esperada y controlada", diferenciándola de una regresión no intencional.

#### CA-18.21 — Diferenciación visual entre alerta y crisis de tonelaje

**Dado que** el sistema emite alertas de tonelaje,
**cuando** el ejecutante visualiza las alertas,
**entonces** la alerta de nivel normal (> 10%) y la de crisis (> 20%) son visualmente distinguibles mediante colores e iconografía diferenciada.

#### CA-18.22 — Alertas de tonelaje informativas, no bloqueantes

**Dado que** el sistema emite una alerta de caída de tonelaje,
**cuando** el ejecutante interactúa con el sistema,
**entonces** la alerta es informativa y no impide ninguna operación del ejecutante.

---

### Bloque E — Alertas de Inactividad por Módulo (RF58)

#### CA-18.23 — Alerta por inactividad > 10 días naturales

**Dado que** el sistema monitorea la última fecha de ejecución de cada módulo,
**cuando** transcurren más de 10 días naturales sin que se ejecute un módulo específico (A, B o C),
**entonces** emite una alerta indicando el módulo inactivo, la cantidad de días transcurridos desde su última ejecución y los grupos musculares asociados que pueden verse afectados.

#### CA-18.24 — Alerta de crisis por inactividad > 14 días naturales

**Dado que** el sistema monitorea la última fecha de ejecución de cada módulo,
**cuando** transcurren más de 14 días naturales sin que se ejecute un módulo específico,
**entonces** emite una alerta de crisis con mayor urgencia visual, informando al ejecutante que el grupo muscular asociado puede estar perdiendo adaptaciones musculares y que se recomienda priorizar ese módulo.

#### CA-18.25 — Referencia a los grupos musculares afectados

**Dado que** el sistema emite una alerta de inactividad por módulo,
**cuando** presenta la alerta al ejecutante,
**entonces** detalla los grupos musculares asociados al módulo inactivo: Módulo A = Espalda, Bíceps, Abdomen; Módulo B = Pecho, Hombro, Tríceps; Módulo C = Cuádriceps, Isquiotibiales, Glúteos, Aductores, Abductores, Gemelos.

#### CA-18.26 — Conteo basado en días naturales

**Dado que** el sistema evalúa la inactividad de un módulo,
**cuando** cuenta los días transcurridos,
**entonces** utiliza días naturales del calendario (no sesiones ni microciclos), ya que la pérdida de adaptaciones musculares se correlaciona con el tiempo absoluto sin estímulo.

#### CA-18.27 — Diferenciación visual entre alerta y crisis de inactividad

**Dado que** el sistema emite alertas de inactividad,
**cuando** el ejecutante visualiza las alertas,
**entonces** la alerta de nivel normal (> 10 días) y la de crisis (> 14 días) son visualmente distinguibles mediante colores e iconografía diferenciada.

#### CA-18.28 — Alertas de inactividad informativas, no bloqueantes

**Dado que** el sistema emite una alerta de inactividad por módulo,
**cuando** el ejecutante interactúa con el sistema,
**entonces** la alerta es informativa; no altera la rotación cíclica ni fuerza un cambio de módulo. El ejecutante sigue la rotación normal.

#### CA-18.29 — Retiro automático de alerta de inactividad

**Dado que** el sistema ha emitido una alerta de inactividad para un módulo,
**cuando** el ejecutante completa una sesión de ese módulo,
**entonces** el sistema retira la alerta de inactividad y reinicia el conteo de días para ese módulo.

---

## §4 Dependencias Técnicas e Integración

### Rules reutilizadas (sin modificación)

| # | Rule | Uso en HU-18 |
|---|---|---|
| R1 | `ProgressionRateRule` | Tasa de progresión para CA-18.01/02 (write-side) y recálculo H2 |
| R2 | `AvgRirRule` | RIR promedio por módulo para CA-18.06/07 (write-side) y H2 |
| R3 | `AdherenceRule` | Adherencia semanal para CA-18.12/13 (write-side) y H2 |
| R4 | `TonnageRule` | Tonelaje por grupo muscular para CA-18.17/18 (write-side) y H2 |
| R5 | `PlateauCausalAnalysisRule` | Análisis causal de mesetas en H2 (HU-12) |
| R6 | `CorrectiveActionRule` | Recomendaciones escalonadas en H2 (HU-12) |
| R7 | `ModuleFatigueRule` | Detección de fatiga acumulada — contextualiza alertas meseta grupal (HU-12) |

### Queries DAO reutilizados (sin modificación)

| # | Query | Uso |
|---|---|---|
| R8 | `AlertDao.insert()` | Inserción de alertas (HU-12) |
| R9 | `AlertDao.existsActiveByExercise()` | Deduplicación por ejercicio (HU-12) |
| R10 | `AlertDao.existsActiveByModule()` | Deduplicación por módulo (HU-12) |
| R11 | `AlertDao.resolveByExerciseAndType()` | Resolución por ejercicio (HU-12) |
| R12 | `AlertDao.resolveByModuleAndType()` | Resolución por módulo (HU-12) |
| R13 | `AlertDao.countActive()` | Conteo reactivo badge B1 (HU-12) |
| R14 | `AlertDao.getActiveAlerts()` | Lista de alertas activas (HU-12) |
| R15 | `SessionDao.getSessionIdsByModuleInRange()` | Últimas N sesiones de módulo para RIR (con `deload_id IS NULL`) |
| R16 | `SessionDao.countSessionsInWeek()` | Sesiones completadas en semana para adherencia |
| R17 | `ExerciseSetDao.getRirValuesBySessionIds()` | Valores RIR para cálculo RIR promedio |

### Componentes NO tocados

- `data/local/entity/` — `AlertEntity` no se modifica (0 cambios de esquema, 0 migraciones)
- `TensionDatabase.kt` — versión se mantiene en 7; `AlertDao` ya está registrado
- `data/local/seed/` — no se toca ningún seeder
- Flujos E, C, D, F, G, I, J — no se modifican sus pantallas

### Notas Técnicas

**Nota 1 — Mapeo tipo de alerta → nivel visual (H1).**
3 niveles: 🔴 Crisis (rojo/Error Container), 🟠 Alerta alta (naranja), 🟡 Alerta media (amarillo). Mapeo: `CRISIS` → 🔴, `HIGH_ALERT` → 🟠, `MEDIUM_ALERT` → 🟡. H1 agrupa en 2 secciones: "Crisis" (solo 🔴) y "Alertas" (🟠 + 🟡).

**Nota 2 — H2: Análisis causal varía por tipo de alerta.**
PLATEAU: `PlateauCausalAnalysisRule.analyze()` + `CorrectiveActionRule.recommend()` (HU-12). LOW_PROGRESSION_RATE: texto fijo según nivel. RIR_OUT_OF_RANGE: texto condicional (< 1.5 → fatiga, > 3.5 → estímulo insuficiente). LOW_ADHERENCE: datos de semana(s) afectada(s). TONNAGE_DROP: descarga planificada (azul) vs regresión. MODULE_INACTIVITY: días transcurridos + grupos musculares (CA-18.25).

**Nota 3 — H2: Links de acción condicionales.**
"Ver historial del ejercicio →" (→ F3): visible si `type ∈ {PLATEAU, LOW_PROGRESSION_RATE}` y `exercise_id != null`. "Gestionar descarga →" (→ I1): visible si `type ∈ {MODULE_REQUIRES_DELOAD}` o si `RIR_OUT_OF_RANGE` con RIR < 1.5.

**Nota 4 — Deduplicación de alertas.**
El pipeline verifica `existsActiveByExercise/Module/MuscleGroup()` antes de insertar. Si la condición cambia de nivel (ej: inactividad pasa de 10 a 14 días), la alerta MEDIUM_ALERT se resuelve y se crea nueva CRISIS. Patrón: resolve + insert (no update).

**Nota 5 — Escalamiento de nivel (MEDIUM_ALERT → CRISIS).**
Pipeline: (1) resolver alerta existente, (2) crear nueva con nivel correcto. Al bajar: CRISIS → resolver + crear MEDIUM_ALERT. Al desaparecer: resolver todas las alertas activas del tipo para la entidad.

**Nota 6 — Grupos musculares por módulo para CA-18.25.**
Datos fijos del dominio: A = Espalda, Bíceps, Abdomen; B = Pecho, Hombro, Tríceps; C = Cuádriceps, Isquiotibiales, Glúteos, Aductores, Abductores, Gemelos. Codificados como constante en `AlertThresholdRule.MUSCLE_GROUPS_BY_MODULE`.

**Nota 7 — Bottom Navigation en H1 y H2.**
El wireframe muestra "Inicio" activo en H1/H2. `showBottomBar` en `TensionNavHost` ya mostrará el Bottom Nav (alert-center/alert-detail no están en el blocklist). Solo falta agregar `childRoutePrefixes = setOf("alert-center", "alert-detail")` al tab HOME en `BottomNavigationBar.kt`.

**Nota 8 — Evaluación de tonelaje reutiliza lógica de `GetMicrocycleMapUseCase`.**
`closedSessions.chunked(6)` → microciclos. Tonelaje con `TonnageRule.calculateForMuscleGroup()`. Verificación de descarga: `currentMicrocycle.any { it.deloadId != null }` → nivel MEDIUM_ALERT (no CRISIS) y mensaje contextualizado.

**Nota 9 — `getSessionIdsByModuleInRange()` ya filtra sesiones de descarga.**
El query existente incluye `AND s.deload_id IS NULL` — garantiza que CA-18.08 evalúe solo sesiones normales.

**Nota 10 — Corrección crítica: Steps 9 y 11 se ejecutan ANTES del deload guard.**
El Análisis Arquitectónico asumía que todos los Steps se ejecutan después del deload guard. Corrección: CA-18.12 evalúa adherencia — una sesión de descarga SÍ cuenta para la frecuencia semanal. CA-18.29 dice que "completar una sesión de ese módulo" resuelve la inactividad — incluyendo sesiones de descarga. Steps 9 y 11 se mueven ANTES de `if (isDeloadSession) return`. Steps 7, 8, 10 permanecen protegidos por el guard (correcto).

---

## §5 Análisis Arquitectónico

### Decisiones Fundamentadas

**1. Se crea `AlertRepository` (interfaz + impl) como nuevo dominio funcional de lectura.**
Las responsabilidades de lectura de alertas (listado H1, detalle H2, conteo badge B1) no pertenecen a `SessionRepository`. `AlertRepository` encapsula lectura de alertas y construcción de datos de contexto para H2. La escritura permanece en `SessionRepositoryImpl` (ya tiene `AlertDao` y todas las dependencias transaccionales).

**2. La evaluación de los 5 nuevos tipos extiende `evaluateProgression()` con bloques post-loop.**
Steps 7-11 se agregan después del bloque MODULE_REQUIRES_DELOAD. El deload guard existente en línea 536 protege Steps 7, 8, 10. Steps 9, 11 se mueven antes del guard (Nota 10 — corrección crítica).

**3. No se crean nuevas Rules de cálculo — se reutilizan las de HU-15.**
Solo se crea `AlertThresholdRule` — objeto con funciones puras que evalúan si un valor supera umbrales. Centraliza todos los umbrales en un solo lugar testeable.

**4. `AlertThresholdRule` centraliza todos los umbrales de los 5 bloques.**
Progresión: < 40% (alerta) / < 20% (crisis). RIR: < 1.5 o > 3.5 (sostenido 2+ sesiones). Adherencia: < 60% (alerta) / 2+ semanas (crisis). Tonelaje: > 10% caída (alerta) / > 20% (crisis). Inactividad: > 10 días (alerta) / > 14 días (crisis).

**5. MODULE_INACTIVITY se evalúa al cerrar sesión, no como background job.**
El ejecutante entrena 3-6 sesiones/semana → evalúa inactividad de los otros módulos cada 1-2 días. El umbral mínimo es 10 días. Evita permisos adicionales (RNF30: permisos mínimos).

**6. H2 recalcula datos dinámicamente — no se almacenan en la tabla `alert`.**
Modelo de Datos §3.16: los datos que dispararon la alerta se recalculan dinámicamente. Las sesiones cerradas son inmutables → el recálculo produce el mismo resultado.

**7. Verificación de descarga planificada para CA-18.19/CA-18.20 usa `deload_id`.**
Si las sesiones del microciclo evaluado tienen `deload_id IS NOT NULL` → alerta con nivel `MEDIUM_ALERT` y mensaje contextualizado (nunca CRISIS).

**8. "Sostenido 2+ sesiones" para RIR usa `getSessionIdsByModuleInRange()` existente.**
Se calcula RIR promedio de cada una de las 2 últimas sesiones por separado. Si ambas cumplen la condición → se emite la alerta. Una sola sesión fuera de rango no la dispara (CA-18.08).

**9. La adherencia usa semanas naturales y se evalúa al cierre de sesión.**
`DayOfWeek.MONDAY` a `DayOfWeek.SUNDAY` para definir semana natural. Para crisis 2+ semanas (CA-18.13): verificar semana anterior también con `countSessionsInWeek(prevWeekStart, prevWeekEnd)`.

**10. Badge B1 se conecta con `Flow<Int>` reactivo.**
`AlertDao.countActive()` ya retorna `Flow<Int>`. El `HomeViewModel` reemplaza `alertCount = 0` hardcoded con el 4to Flow en el `combine()`.

### Componentes Afectados

**Componentes nuevos:**

| # | Componente | Tipo | Paquete | Responsabilidad |
|---|---|---|---|---|
| 1 | `AlertThresholdRule` | Rule (object) | `domain.rules` | Funciones puras + constantes de umbrales de los 5 tipos de alerta. `MUSCLE_GROUPS_BY_MODULE` |
| 2 | `AlertRepository` | Interfaz | `domain.repository` | `countActive(): Flow<Int>`, `getActiveAlerts(): Flow<List<AlertItem>>`, `getAlertDetail(alertId): AlertDetail` |
| 3 | `AlertRepositoryImpl` | Implementación | `data.repository` | Lectura de alertas + construcción de datos de contexto para H2 con recálculo dinámico |
| 4 | `GetActiveAlertCountUseCase` | UseCase | `domain.usecase.alerts` | `Flow<Int>` del conteo de alertas activas para badge B1 |
| 5 | `GetActiveAlertsUseCase` | UseCase | `domain.usecase.alerts` | `Flow<List<AlertItem>>` de alertas activas para H1 |
| 6 | `GetAlertDetailUseCase` | UseCase | `domain.usecase.alerts` | Detalle completo de una alerta para H2 (recálculo dinámico) |
| 7 | `AlertItem` | domain model | `domain.model` | Fila de H1: alertId, type, level, entityName, message, createdAt |
| 8 | `AlertDetail` | domain model | `domain.model` | H2: type, level, entityName, triggerData, causalAnalysis, recommendations, showExerciseHistoryLink, showDeloadLink, exerciseId |
| 9 | `AlertTriggerData` | domain model (sealed) | `domain.model` | Datos que dispararon la alerta: PlateauTrigger, ProgressionRateTrigger, RirTrigger, AdherenceTrigger, TonnageDropTrigger, InactivityTrigger |
| 10 | `AlertCenterScreen` | Composable | `ui.alerts` | H1 — Centro de Alertas: sección Crisis (🔴) + sección Alertas (🟠🟡). Estado vacío |
| 11 | `AlertCenterViewModel` | HiltViewModel | `ui.alerts` | Estado reactivo para H1 via Flow |
| 12 | `AlertCenterUiState` | sealed interface | `ui.alerts` | Loading/Empty/Loaded con crisisAlerts + regularAlerts + totalCount |
| 13 | `AlertDetailScreen` | Composable | `ui.alerts` | H2 — Detalle con datos disparadores, análisis causal, recomendaciones, links condicionales |
| 14 | `AlertDetailViewModel` | HiltViewModel | `ui.alerts` | Estado reactivo para H2 — recálculo dinámico |
| 15 | `AlertDetailUiState` | sealed interface | `ui.alerts` | Loading/Loaded con AlertDetail |
| 16 | `AlertLevelIndicator` | Composable | `ui.components` | Indicador circular relleno por nivel (12dp H1, 16dp H2). Reutilizable |
| 17 | `AlertCard` | Composable | `ui.alerts` | Card clickable de alerta con fondo por nivel, leading indicator, tipo, entidad, dato clave |
| 18 | `ModuleInactivityData` | domain model | `domain.model` | moduleCode, lastSessionDate, daysSinceLastSession, muscleGroups |

**Componentes modificados:**

| # | Componente | Modificación | Nivel |
|---|---|---|---|
| 1 | `SessionRepositoryImpl` | Agregar 5 bloques (Steps 7-11) al final de `evaluateProgression()`. Inyectar `ProfileDao`. Extraer `moduleCode` antes del deload guard. Invocar Steps 9/11 antes del guard | Mayor |
| 2 | `AlertDao` | Agregar `getAlertById()`, `resolveByMuscleGroupAndType()`, `existsActiveByMuscleGroup()` | Medio |
| 3 | `SessionDao` | Agregar `getLastSessionDateByModule()` | Menor |
| 4 | `HomeViewModel` | Inyectar `GetActiveAlertCountUseCase`. Reemplazar `alertCount = 0` con 4to Flow en `combine()` | Menor |
| 5 | `NavigationRoutes` | Agregar `ALERT_CENTER`, `ALERT_DETAIL`, `alertDetailRoute()` | Menor |
| 6 | `TensionNavHost` | Registrar AlertCenterScreen, AlertDetailScreen. Conectar `onNavigateToAlerts` | Medio |
| 7 | `RepositoryModule` | Agregar binding `AlertRepository` → `AlertRepositoryImpl` | Menor |
| 8 | `BottomNavigationBar` | Agregar `childRoutePrefixes = setOf("alert-center", "alert-detail")` al tab HOME | Menor |
| 9 | `strings.xml` | Agregar ~30 strings para H1/H2 (títulos, tipos, análisis causal, recomendaciones, formatos) | Menor |

### Hitos de Implementación

| # | Componente(s) | Descripción | Dependencias |
|---|---|---|---|
| 1 | Domain Models + Rule | `AlertItem`, `AlertDetail`, `AlertTriggerData`, `ModuleInactivityData`, `AlertThresholdRule` | Ninguna |
| 2 | DAO Queries | `AlertDao`: 3 queries nuevos. `SessionDao`: 1 query nuevo | Ninguna |
| 3 | AlertRepository | Interfaz + `AlertRepositoryImpl` + binding `RepositoryModule` | Hitos 1, 2 |
| 4 | Pipeline de Alertas | 5 bloques (Steps 7-11) en `evaluateProgression()` + `ProfileDao` inyectado + reestructuración deload guard | Hitos 1, 2 |
| 5 | Use Cases | `GetActiveAlertCountUseCase`, `GetActiveAlertsUseCase`, `GetAlertDetailUseCase` | Hito 3 |
| 6 | B1 Badge | `HomeViewModel`: inyectar UseCase + 4to Flow en `combine()` | Hito 5 |
| 7 | H1 — Centro de Alertas | `AlertCenterScreen` + `AlertCenterViewModel` + `AlertCenterUiState` + `AlertCard` + `AlertLevelIndicator` | Hito 5 |
| 8 | H2 — Detalle de Alerta | `AlertDetailScreen` + `AlertDetailViewModel` + `AlertDetailUiState` | Hito 5 |
| 9 | Navegación y Strings | `NavigationRoutes`, `TensionNavHost`, `BottomNavigationBar`, `strings.xml` | Hitos 6, 7, 8 |
| 10 | Tests unitarios | `AlertThresholdRule`, 3 Use Cases, 5 Steps del pipeline, 2 ViewModels | Hitos 4, 5, 7, 8 |

### Verificación Cruzada de CAs

| CA | Mecanismo | Componente |
|---|---|---|
| CA-18.01 | `ProgressionRateRule.calculate()` → < 40% → insert MEDIUM_ALERT | `SessionRepositoryImpl` Step 7 + `AlertThresholdRule.isProgressionAlert()` |
| CA-18.02 | < 20% → insert CRISIS (o escalar MEDIUM→CRISIS) | Step 7 + `isProgressionCrisis()` |
| CA-18.03 | Card crisis (🔴 Error Container) vs card media (🟡 amarillo) con indicador circular | `AlertCard` + `AlertLevelIndicator` |
| CA-18.04 | No hay lógica de bloqueo — alertas son informativas | Todo el flujo es read-only excepto create/resolve automático |
| CA-18.05 | Evaluación al cierre de cada sesión — recalcula tasa y crea/resuelve según estado actual | Step 7 ejecuta en cada `evaluateProgression()` |
| CA-18.06 | `AvgRirRule.calculate()` por cada una de las 2 últimas sesiones → ambas < 1.5 → MEDIUM_ALERT | Step 8 |
| CA-18.07 | Ambas sesiones > 3.5 → MEDIUM_ALERT con recomendación de incrementar carga | Step 8 |
| CA-18.08 | `getSessionIdsByModuleInRange(moduleCode, 2)` → RIR por sesión individual → ambas fuera de rango | Step 8 |
| CA-18.09 | Card diferencia contenido por tipo de alerta RIR (mensaje + recomendación distinta) | `AlertCard` + H2 contenido dinámico |
| CA-18.10 | Sin lógica de bloqueo | Mismo que CA-18.04 |
| CA-18.11 | 2 últimas sesiones en rango óptimo → `resolveByModuleAndType(moduleCode, "RIR_OUT_OF_RANGE")` | Step 8 bloque else |
| CA-18.12 | `AdherenceRule.calculate()` semana actual → < 60% → MEDIUM_ALERT | Step 9 (ANTES del deload guard) |
| CA-18.13 | Semana anterior también < 60% → escalar a CRISIS | Step 9 |
| CA-18.14 | Card MEDIUM_ALERT (🟡 1 semana) vs CRISIS (🔴 2+ semanas) | `AlertCard` + `AlertLevelIndicator` |
| CA-18.15 | Sin lógica de bloqueo | Mismo que CA-18.04 |
| CA-18.16 | Semana actual ≥ 60% → `resolveAllByType("LOW_ADHERENCE")` | Step 9 bloque else |
| CA-18.17 | `TonnageRule.calculateForMuscleGroup()` → caída > 10% → MEDIUM_ALERT | Step 10 (DESPUÉS del deload guard) |
| CA-18.18 | Caída > 20% → CRISIS (o escalar) | Step 10 |
| CA-18.19 | Sesiones del microciclo con `deload_id IS NOT NULL` → contextualizar como descarga planificada | Step 10 condicional `isDeloadMicrocycle` |
| CA-18.20 | Mensaje: "Descarga planificada — caída esperada y controlada" + nivel MEDIUM_ALERT (nunca CRISIS) | Condicional en Step 10 |
| CA-18.21 | Card MEDIUM_ALERT (🟡 > 10%) vs CRISIS (🔴 > 20%) | `AlertCard` + `AlertLevelIndicator` |
| CA-18.22 | Sin lógica de bloqueo | Mismo que CA-18.04 |
| CA-18.23 | `getLastSessionDateByModule()` → `ChronoUnit.DAYS.between()` → > 10 → MEDIUM_ALERT | Step 11 (ANTES del deload guard) |
| CA-18.24 | > 14 días → CRISIS (o escalar) | Step 11 |
| CA-18.25 | `AlertThresholdRule.MUSCLE_GROUPS_BY_MODULE[moduleCode]` → lista de grupos musculares | `AlertThresholdRule` constante |
| CA-18.26 | `ChronoUnit.DAYS.between(LocalDate.parse(lastDate), LocalDate.now())` | Step 11 |
| CA-18.27 | Card MEDIUM_ALERT (🟡 > 10 días) vs CRISIS (🔴 > 14 días) | `AlertCard` + `AlertLevelIndicator` |
| CA-18.28 | Sin alteración de rotación — sigue A→B→C independientemente | La rotación no se toca |
| CA-18.29 | Al cerrar sesión del módulo → `resolveByModuleAndType(moduleCode, "MODULE_INACTIVITY")` | Step 11 (ANTES del deload guard, CA crítica — ver Nota 10) |

---

## §6 Refinamiento Técnico

### Consideraciones Generales

**Nivel de complejidad:** ALTA — dos caras de complejidad significativa. El **write-side** extiende `evaluateProgression()` (~335 líneas → ~535 líneas estimadas) con 5 bloques que ejecutan cálculos contra múltiples DAOs dentro de una transacción existente. El **read-side** crea 2 pantallas con recálculo dinámico — H2 especialmente complejo porque el análisis causal varía por tipo (7 tipos × 3 niveles ≈ 15 variantes de contenido).

**Riesgos técnicos conocidos:**

1. **`evaluateProgression()` crece significativamente:** Cada Step se extrae como método privado (`evaluateLowProgressionRate()`, etc.) para mantener el orquestador legible.
2. **Constructor con 13 parámetros:** Agregar `ProfileDao` lleva `SessionRepositoryImpl` de 12 a 13 params. Aceptable con Hilt.
3. **Transacción anidada:** Los Steps se ejecutan dentro del `database.withTransaction` de `closeSession()`. Cada Step usa try-catch individual — un fallo en alertas no revierte el cierre de sesión.
4. **`getRirValuesBySessionIds` retorna RIR de TODOS los ejercicios de la sesión:** Para CA-18.08 se invoca por sesión individual (`listOf(singleSessionId)`) y se calcula el promedio con `AvgRirRule.calculate()` — evalúa "RIR Promedio del módulo en cada sesión".
5. **`getClassificationCountsByPeriod` excluye sesiones de descarga:** Correcto para CA-18.05 — sesiones de descarga no cuentan para tasa de progresión. `startDate = LocalDate.now().minusWeeks(4).toString()`.
6. **Deload guard — corrección crítica Steps 9 y 11:** Se ejecutan ANTES de `if (isDeloadSession) return`. Steps 7, 8, 10 permanecen DESPUÉS del guard (correcto — progresión/RIR/tonelaje durante descarga usarían datos artificialmente bajos).

**Patrones y convenciones del equipo:**

- Rules: `object` en `domain.rules/` con funciones puras, constantes `const val`, testeable sin mocks
- Pipeline alertas: `if (!alertDao.existsActiveByXxx()) { alertDao.insert(...) } else if (resolved) { alertDao.resolveByXxxAndType(...) }` (HU-12)
- Escalamiento: resolve + re-insert (no update) para soportar MEDIUM_ALERT → CRISIS y de-escalamiento
- `Flow<Int>` reactivo para badge — `combine()` con 4 flows en `HomeViewModel`
- try-catch individual por Step — un fallo no revierte el cierre de sesión

**Dependencias nuevas a instalar:** Ninguna.

**Estrategia de testing:** JUnit 4 + MockK + Kotlin Coroutines Test | `AlertThresholdRule` (boundary values de todos los umbrales, 13 casos), 3 Use Cases (delegación), 5 Steps del pipeline (mocking DAOs + Rules), 2 ViewModels (transformación de datos y estados) | ~12 archivos de test.

---

## §7 Tareas de Implementación

### Fase 1: Domain Models + AlertThresholdRule

**AC: Infraestructura — Modelos compartidos por pipeline, H1, H2**

#### Domain Layer — Modelos

- [ ] **Crear `AlertItem`** (usado por H1)
  - [ ] Crear archivo: `domain/model/AlertItem.kt`
  - [ ] Campos: `alertId: Long`, `type: String`, `level: String`, `entityName: String`, `message: String`, `createdAt: String`

- [ ] **Crear `AlertTriggerData`** (sealed interface usado por H2)
  - [ ] Crear archivo: `domain/model/AlertTriggerData.kt`
  - [ ] Variantes:
    - `data class PlateauTrigger(val sessions: List<PlateauSession>)` con `data class PlateauSession(val date: String, val weightKg: Double, val totalReps: Int)`
    - `data class ProgressionRateTrigger(val rate: Double, val exerciseName: String)`
    - `data class RirTrigger(val avgRir: Double, val moduleCode: String, val isLow: Boolean)`
    - `data class AdherenceTrigger(val percentage: Double, val completedSessions: Int, val plannedSessions: Int, val consecutiveWeeks: Int)`
    - `data class TonnageDropTrigger(val muscleGroup: String, val dropPercentage: Double, val previousTonnage: Double, val currentTonnage: Double, val isDeloadContextualized: Boolean)`
    - `data class InactivityTrigger(val moduleCode: String, val daysSinceLastSession: Long, val muscleGroups: List<String>)`

- [ ] **Crear `AlertDetail`** (usado por H2)
  - [ ] Crear archivo: `domain/model/AlertDetail.kt`
  - [ ] Campos: `alertId: Long`, `type: String`, `level: String`, `entityName: String`, `message: String`, `createdAt: String`, `triggerData: AlertTriggerData`, `causalAnalysis: String`, `recommendations: List<String>`, `showExerciseHistoryLink: Boolean`, `showDeloadLink: Boolean`, `exerciseId: Long?`

- [ ] **Crear `ModuleInactivityData`** (usado por Step 11 del pipeline)
  - [ ] Crear archivo: `domain/model/ModuleInactivityData.kt`
  - [ ] Campos: `moduleCode: String`, `lastSessionDate: String?`, `daysSinceLastSession: Long`, `muscleGroups: List<String>`

#### Domain Layer — Rule

- [ ] **Crear `AlertThresholdRule`** (AC: 18.01, 18.02, 18.06, 18.07, 18.12, 18.13, 18.17, 18.18, 18.23, 18.24)
  - [ ] Crear archivo: `domain/rules/AlertThresholdRule.kt`
  - [ ] `object AlertThresholdRule` con constantes:
    ```kotlin
    const val PROGRESSION_ALERT_THRESHOLD = 40.0
    const val PROGRESSION_CRISIS_THRESHOLD = 20.0
    const val RIR_LOW_THRESHOLD = 1.5
    const val RIR_HIGH_THRESHOLD = 3.5
    const val RIR_SUSTAINED_SESSIONS = 2
    const val ADHERENCE_THRESHOLD = 60.0
    const val ADHERENCE_CRISIS_WEEKS = 2
    const val TONNAGE_ALERT_THRESHOLD = 10.0
    const val TONNAGE_CRISIS_THRESHOLD = 20.0
    const val INACTIVITY_ALERT_DAYS = 10L
    const val INACTIVITY_CRISIS_DAYS = 14L
    ```
  - [ ] Funciones puras:
    ```kotlin
    fun isProgressionAlert(rate: Double): Boolean = rate < PROGRESSION_ALERT_THRESHOLD
    fun isProgressionCrisis(rate: Double): Boolean = rate < PROGRESSION_CRISIS_THRESHOLD
    fun isRirLow(avgRir: Double): Boolean = avgRir < RIR_LOW_THRESHOLD
    fun isRirHigh(avgRir: Double): Boolean = avgRir > RIR_HIGH_THRESHOLD
    fun isRirOutOfRange(avgRir: Double): Boolean = isRirLow(avgRir) || isRirHigh(avgRir)
    fun isAdherenceLow(percentage: Double): Boolean = percentage < ADHERENCE_THRESHOLD
    fun isTonnageAlert(dropPercentage: Double): Boolean = dropPercentage > TONNAGE_ALERT_THRESHOLD
    fun isTonnageCrisis(dropPercentage: Double): Boolean = dropPercentage > TONNAGE_CRISIS_THRESHOLD
    fun isInactivityAlert(days: Long): Boolean = days > INACTIVITY_ALERT_DAYS
    fun isInactivityCrisis(days: Long): Boolean = days > INACTIVITY_CRISIS_DAYS
    fun progressionLevel(rate: Double): String? = when {
        isProgressionCrisis(rate) -> "CRISIS"
        isProgressionAlert(rate) -> "MEDIUM_ALERT"
        else -> null
    }
    fun tonnageLevel(dropPercentage: Double, isDeloadSession: Boolean): String? = when {
        !isTonnageAlert(dropPercentage) -> null
        isDeloadSession -> "MEDIUM_ALERT"  // nunca CRISIS durante descarga (CA-18.19/CA-18.20)
        isTonnageCrisis(dropPercentage) -> "CRISIS"
        else -> "MEDIUM_ALERT"
    }
    fun inactivityLevel(days: Long): String? = when {
        isInactivityCrisis(days) -> "CRISIS"
        isInactivityAlert(days) -> "MEDIUM_ALERT"
        else -> null
    }
    ```
  - [ ] Constante de grupos musculares por módulo (CA-18.25):
    ```kotlin
    val MUSCLE_GROUPS_BY_MODULE: Map<String, List<String>> = mapOf(
        "A" to listOf("Espalda", "Bíceps", "Abdomen"),
        "B" to listOf("Pecho", "Hombro", "Tríceps"),
        "C" to listOf("Cuádriceps", "Isquiotibiales", "Glúteos", "Aductores", "Abductores", "Gemelos")
    )
    ```

### Fase 2: DAO Queries

**AC: CA-18.23, CA-18.26 (inactividad), CA-18.17/18 (tonelaje), lectura alertas H1/H2**

#### Data Layer — DAO

- [ ] **Agregar query `getAlertById()`** en `AlertDao` (para H2)
  - [ ] Modificar archivo: `data/local/dao/AlertDao.kt`
  - [ ] `@Query("SELECT * FROM alert WHERE id = :alertId") suspend fun getAlertById(alertId: Long): AlertEntity?`

- [ ] **Agregar query `existsActiveByMuscleGroup()`** en `AlertDao` (deduplicación TONNAGE_DROP)
  - [ ] `@Query("SELECT EXISTS(SELECT 1 FROM alert WHERE muscle_group = :muscleGroup AND type = :type AND is_active = 1)") suspend fun existsActiveByMuscleGroup(muscleGroup: String, type: String): Boolean`

- [ ] **Agregar query `resolveByMuscleGroupAndType()`** en `AlertDao` (resolver TONNAGE_DROP)
  - [ ] `@Query("UPDATE alert SET is_active = 0, resolved_at = :resolvedAt WHERE muscle_group = :muscleGroup AND type = :type AND is_active = 1") suspend fun resolveByMuscleGroupAndType(muscleGroup: String, type: String, resolvedAt: String)`

- [ ] **Agregar query `getLastSessionDateByModule()`** en `SessionDao` (CA: 18.23, 18.24, 18.26)
  - [ ] Modificar archivo: `data/local/dao/SessionDao.kt`
  - [ ] SQL:
    ```sql
    SELECT MAX(s.date)
    FROM session s
    INNER JOIN module_version mv ON s.module_version_id = mv.id
    WHERE mv.module_code = :moduleCode
      AND s.status IN ('COMPLETED', 'INCOMPLETE')
    ```
  - [ ] `suspend fun getLastSessionDateByModule(moduleCode: String): String?`

### Fase 3: AlertRepository

**AC: Lectura de alertas para H1, H2 y badge B1**

#### Domain Layer — Interface

- [ ] **Crear interfaz `AlertRepository`**
  - [ ] Crear archivo: `domain/repository/AlertRepository.kt`
  - [ ] `fun countActive(): Flow<Int>`
  - [ ] `fun getActiveAlerts(): Flow<List<AlertItem>>`
  - [ ] `suspend fun getAlertDetail(alertId: Long): AlertDetail`

#### Data Layer — Implementation

- [ ] **Crear `AlertRepositoryImpl`**
  - [ ] Crear archivo: `data/repository/AlertRepositoryImpl.kt`
  - [ ] Constructor `@Inject constructor`: `alertDao`, `sessionDao`, `exerciseDao`, `exerciseSetDao`, `sessionExerciseDao`, `exerciseProgressionDao`, `profileDao`
  - [ ] **`countActive()`:** delega a `alertDao.countActive()`
  - [ ] **`getActiveAlerts()`:** mapea `alertDao.getActiveAlerts()` (Flow<List<AlertEntity>>) a `Flow<List<AlertItem>>`. Para `entityName`: si `exerciseId != null` → `exerciseDao.getByIdOnce(exerciseId)?.name`; si `moduleCode != null` → moduleCode; si `muscleGroup != null` → muscleGroup
  - [ ] **`getAlertDetail(alertId)`:** carga `alertDao.getAlertById(alertId)`, construye `triggerData` según tipo:
    - **PLATEAU:** últimas 3 sesiones del ejercicio → `PlateauTrigger`
    - **LOW_PROGRESSION_RATE:** tasa con `getClassificationCountsByPeriod()` → `ProgressionRateTrigger`
    - **RIR_OUT_OF_RANGE:** `getSessionIdsByModuleInRange(moduleCode, 2)` + RIR por sesión → `RirTrigger`
    - **LOW_ADHERENCE:** `countSessionsInWeek()` + `weeklyFrequency` → `AdherenceTrigger`
    - **TONNAGE_DROP:** tonelaje de 2 microciclos + `TonnageRule` → `TonnageDropTrigger` (verificar `deload_id` para CA-18.19/CA-18.20)
    - **MODULE_INACTIVITY:** `getLastSessionDateByModule()` + `ChronoUnit.DAYS.between()` → `InactivityTrigger` con `MUSCLE_GROUPS_BY_MODULE[moduleCode]`
    - **MODULE_REQUIRES_DELOAD:** `PlateauCausalAnalysisRule` + `CorrectiveActionRule` (HU-12)
  - [ ] Determina `showExerciseHistoryLink` = tipo ∈ {PLATEAU, LOW_PROGRESSION_RATE} && exerciseId != null
  - [ ] Determina `showDeloadLink` = tipo ∈ {MODULE_REQUIRES_DELOAD} || (tipo == RIR_OUT_OF_RANGE && isRirLow)

#### DI Layer

- [ ] **Agregar binding en `RepositoryModule`**
  - [ ] `@Binds @Singleton abstract fun bindAlertRepository(impl: AlertRepositoryImpl): AlertRepository`

### Fase 4: Pipeline de Alertas (Write-Side)

**AC: CA-18.01 a CA-18.29 (los 5 bloques de evaluación)**

#### Data Layer — SessionRepositoryImpl

- [ ] **Inyectar `ProfileDao` en constructor de `SessionRepositoryImpl`**
  - [ ] Agregar `private val profileDao: ProfileDao` al constructor (de 12 a 13 params)
  - [ ] Hilt lo resuelve automáticamente — `ProfileDao` ya está provisto por `DatabaseModule.provideProfileDao()`

- [ ] **Extraer `moduleCode` ANTES del deload guard** (prerequisito de reestructuración)
  - [ ] Mover `val moduleCode = exercises.firstOrNull()?.moduleCode ?: return` a ANTES de la línea `if (isDeloadSession) return`
  - [ ] Permite que Steps 9 y 11 accedan a `moduleCode` durante sesiones de descarga

- [ ] **Invocar Steps 9 y 11 ANTES del deload guard** (corrección crítica — CA-18.12, CA-18.29)
  - [ ] Agregar antes de `if (isDeloadSession) return`:
    ```kotlin
    // Steps 9 & 11 ejecutan independientemente del deload:
    // - Adherencia: sesiones de descarga cuentan para frecuencia semanal (CA-18.12)
    // - Inactividad: sesiones de descarga de un módulo resuelven su inactividad (CA-18.29)
    try { evaluateLowAdherence(today) } catch (_: Exception) { }
    try { evaluateModuleInactivity(today, moduleCode) } catch (_: Exception) { }
    ```

- [ ] **Agregar Step 7: LOW_PROGRESSION_RATE** (AC: 18.01, 18.02, 18.03, 18.04, 18.05)
  - [ ] Crear método privado `evaluateLowProgressionRate(today: String)`
  - [ ] Lógica:
    1. `val startDate = LocalDate.now().minusWeeks(4).toString()`
    2. `val counts = sessionExerciseDao.getClassificationCountsByPeriod(startDate)`
    3. Para cada ejercicio en `counts`:
       - `val rate = ProgressionRateRule.calculate(it.positiveCount, it.totalCount)`
       - `val level = AlertThresholdRule.progressionLevel(rate)`
       - Si `level != null`: resolver alerta existente (patrón resolve + re-insert) y crear nueva con nivel correcto
       - Si `level == null`: `alertDao.resolveByExerciseAndType(exerciseId, "LOW_PROGRESSION_RATE", today)`
  - [ ] Invocar después del bloque MODULE_REQUIRES_DELOAD (después del deload guard)

- [ ] **Agregar Step 8: RIR_OUT_OF_RANGE** (AC: 18.06, 18.07, 18.08, 18.09, 18.10, 18.11)
  - [ ] Crear método privado `evaluateRirOutOfRange(today: String)`
  - [ ] Lógica:
    1. Para cada módulo en `listOf("A", "B", "C")`:
    2. `val sessionIds = sessionDao.getSessionIdsByModuleInRange(moduleCode, 2)`
    3. Si `sessionIds.size < 2` → skip (no hay suficientes sesiones para evaluar sostenimiento)
    4. Para cada sessionId individual: `val rirValues = exerciseSetDao.getRirValuesBySessionIds(listOf(sessionId))` → `val avgRir = AvgRirRule.calculate(rirValues)`
    5. Si **ambas** sesiones cumplen `isRirLow(avgRir)` O ambas cumplen `isRirHigh(avgRir)`:
       - Si no existe alerta activa → insert MEDIUM_ALERT con mensaje diferenciado (< 1.5 vs > 3.5)
       - Si existe → skip (evitar duplicados, ya fue insertada)
    6. Si ambas sesiones están en rango óptimo y existe alerta activa → `resolveByModuleAndType(moduleCode, "RIR_OUT_OF_RANGE", today)` (CA-18.11)
  - [ ] Invocar después del deload guard

- [ ] **Agregar Step 9: LOW_ADHERENCE** (AC: 18.12, 18.13, 18.14, 18.15, 18.16)
  - [ ] Crear método privado `evaluateLowAdherence(today: String)`
  - [ ] Lógica:
    1. `val profile = profileDao.getProfile().first() ?: return`
    2. `val weeklyFrequency = profile.weeklyFrequency`
    3. Calcular semana actual: `val todayDate = LocalDate.now()`, `val weekStart = todayDate.with(DayOfWeek.MONDAY).toString()`, `val weekEnd = todayDate.with(DayOfWeek.SUNDAY).toString()`
    4. `val currentWeekSessions = sessionDao.countSessionsInWeek(weekStart, weekEnd)`
    5. `val currentAdherence = AdherenceRule.calculate(currentWeekSessions, weeklyFrequency)`
    6. Si `isAdherenceLow(currentAdherence)`:
       - Calcular semana anterior con `minusWeeks(1)` + `DayOfWeek.MONDAY/SUNDAY`
       - `val prevAdherence = AdherenceRule.calculate(prevWeekSessions, weeklyFrequency)`
       - Nivel: si `isAdherenceLow(prevAdherence)` → `"CRISIS"`, si no → `"MEDIUM_ALERT"`
       - Patrón resolve + re-insert (soporta escalamiento MEDIUM_ALERT → CRISIS): `alertDao.resolveAllByType("LOW_ADHERENCE", today)` y luego insert con nivel correcto
    7. Si `!isAdherenceLow(currentAdherence)` → `alertDao.resolveAllByType("LOW_ADHERENCE", today)` (CA-18.16)
  - [ ] Invocar ANTES del deload guard

- [ ] **Agregar Step 10: TONNAGE_DROP** (AC: 18.17, 18.18, 18.19, 18.20, 18.21, 18.22)
  - [ ] Crear método privado `evaluateTonnageDrop(today: String)`
  - [ ] Lógica:
    1. `val closedSessions = sessionDao.getClosedSessionsOrdered()` (orden ASC por fecha)
    2. `val microcycles = closedSessions.chunked(6)`
    3. Si `microcycles.size < 2` → return
    4. `val currentMicrocycle = microcycles.last()`, `val previousMicrocycle = microcycles[microcycles.size - 2]`
    5. `val isDeloadMicrocycle = currentMicrocycle.any { it.deloadId != null }`
    6. Calcular tonelaje para ambos microciclos con `exerciseSetDao.getTonnageDataBySessionIds()` + `TonnageRule.calculateForMuscleGroup()`
    7. Para cada grupo muscular:
       - `val dropPercentage = ((previous - current) / previous) * 100.0` (si previous > 0)
       - `val level = AlertThresholdRule.tonnageLevel(dropPercentage, isDeloadMicrocycle)` (CA-18.19/CA-18.20)
       - Si `level != null`: patrón resolve + re-insert con mensaje contextualizado (descarga → "Caída esperada", regresión → "Caída de tonelaje −XX%")
       - Si `level == null`: `alertDao.resolveByMuscleGroupAndType(muscleGroup, "TONNAGE_DROP", today)`
    8. Nota de mapeo: `tonnageData.map { SetForTonnage(it.weightKg, it.reps, it.muscleGroup) }` — patrón de `GetTonnageByMuscleGroupUseCase.kt`
  - [ ] Invocar después del deload guard

- [ ] **Agregar Step 11: MODULE_INACTIVITY** (AC: 18.23, 18.24, 18.25, 18.26, 18.27, 18.28, 18.29)
  - [ ] Crear método privado `evaluateModuleInactivity(today: String, currentModuleCode: String)`
  - [ ] Lógica:
    1. Para cada módulo en `listOf("A", "B", "C")`:
    2. Si `moduleCode == currentModuleCode` → resolver alerta si existe (CA-18.29) y skip evaluación
    3. `val lastDate = sessionDao.getLastSessionDateByModule(moduleCode)`
    4. Si `lastDate == null` → skip (nunca ha entrenado ese módulo)
    5. `val daysSince = ChronoUnit.DAYS.between(LocalDate.parse(lastDate), LocalDate.now())`
    6. `val level = AlertThresholdRule.inactivityLevel(daysSince)`
    7. Si `level != null`:
       - `val muscleGroups = AlertThresholdRule.MUSCLE_GROUPS_BY_MODULE[moduleCode] ?: emptyList()`
       - Patrón resolve + re-insert (soporta escalamiento MEDIUM_ALERT → CRISIS al pasar de >10 a >14 días)
       - Mensaje: `"Módulo $moduleCode: $daysSince días sin sesión — ${muscleGroups.joinToString(", ")}"`
    8. Si `level == null` → `alertDao.resolveByModuleAndType(moduleCode, "MODULE_INACTIVITY", today)`
  - [ ] Invocar ANTES del deload guard

- [ ] **Invocar Steps 7, 8, 10 DESPUÉS del deload guard y MODULE_REQUIRES_DELOAD**
  - [ ] Agregar después del bloque MODULE_REQUIRES_DELOAD:
    ```kotlin
    // Steps 7, 8, 10 protegidos correctamente por el deload guard:
    // - Progresión: sesiones de descarga usan IN_DELOAD, no evaluar progresión
    // - RIR: descarga apunta RIR 4-5 intencionalmente → false positivos HIGH
    // - Tonelaje: carga al 60% en descarga → caída esperada (manejada por CA-18.19/CA-18.20)
    try { evaluateLowProgressionRate(today) } catch (_: Exception) { }
    try { evaluateRirOutOfRange(today) } catch (_: Exception) { }
    try { evaluateTonnageDrop(today) } catch (_: Exception) { }
    ```

### Fase 5: Use Cases

**AC: Lectura para badge B1, lista H1, detalle H2**

#### Domain Layer — Use Cases

- [ ] **Crear `GetActiveAlertCountUseCase`** (badge B1)
  - [ ] Crear archivo: `domain/usecase/alerts/GetActiveAlertCountUseCase.kt`
  - [ ] `class GetActiveAlertCountUseCase @Inject constructor(private val alertRepository: AlertRepository)`
  - [ ] `operator fun invoke(): Flow<Int> = alertRepository.countActive()`

- [ ] **Crear `GetActiveAlertsUseCase`** (H1)
  - [ ] Crear archivo: `domain/usecase/alerts/GetActiveAlertsUseCase.kt`
  - [ ] `operator fun invoke(): Flow<List<AlertItem>> = alertRepository.getActiveAlerts()`

- [ ] **Crear `GetAlertDetailUseCase`** (H2)
  - [ ] Crear archivo: `domain/usecase/alerts/GetAlertDetailUseCase.kt`
  - [ ] `suspend operator fun invoke(alertId: Long): AlertDetail = alertRepository.getAlertDetail(alertId)`

### Fase 6: B1 Badge Reactivo

**AC: Badge refleja conteo real de alertas activas**

#### UI Layer — HomeViewModel

- [ ] **Inyectar `GetActiveAlertCountUseCase` en `HomeViewModel`**
  - [ ] Modificar archivo: `ui/home/HomeViewModel.kt`
  - [ ] Agregar al constructor: `private val getActiveAlertCountUseCase: GetActiveAlertCountUseCase`
  - [ ] Reemplazar `combine(3 flows)` con `combine(4 flows)`:
    ```kotlin
    // ANTES: combine(flow1, flow2, flow3) { nextSession, activeSession, microcycleCount ->
    //     HomeUiState(..., alertCount = 0)
    // }
    // DESPUÉS:
    combine(
        getNextSessionInfoUseCase(),
        getActiveSessionUseCase(),
        getMicrocycleCountUseCase(),
        getActiveAlertCountUseCase()
    ) { nextSession, activeSession, microcycleCount, alertCount ->
        HomeUiState(..., alertCount = alertCount)
    }
    ```
  - [ ] Nota: `combine` con 4 flows usa el overload destructurado (soporta hasta 5 sin array)

### Fase 7: H1 — Centro de Alertas (Screen + ViewModel + Componentes)

**AC: CA-18.03, 18.09, 18.14, 18.21, 18.27 (diferenciación visual), estado vacío, agrupación**

#### UI Layer — Componente reutilizable

- [ ] **Crear `AlertLevelIndicator`**
  - [ ] Crear archivo: `ui/components/AlertLevelIndicator.kt`
  - [ ] Parámetros: `level: String`, `size: Dp` (12dp para H1, 16dp para H2)
  - [ ] `Box` circular (`clip(CircleShape)`) con color de fondo según level + `isSystemInDarkTheme()`:
    - CRISIS → `Color(0xFFC62828)` (claro) / `Color(0xFFEF9A9A)` (oscuro)
    - HIGH_ALERT → `Color(0xFFE65100)` (claro) / `Color(0xFFFFB74D)` (oscuro)
    - MEDIUM_ALERT → `Color(0xFF8D6E00)` (claro) / `Color(0xFFFFD54F)` (oscuro)

#### UI Layer — ViewModel

- [ ] **Crear `AlertCenterUiState`**
  - [ ] `sealed interface AlertCenterUiState`: `data object Loading`, `data object Empty`, `data class Loaded(val crisisAlerts: List<AlertItem>, val regularAlerts: List<AlertItem>, val totalCount: Int)`

- [ ] **Crear `AlertCenterViewModel`**
  - [ ] `@HiltViewModel class AlertCenterViewModel @Inject constructor(private val getActiveAlertsUseCase: GetActiveAlertsUseCase)`
  - [ ] `init {}`: launch → collect Flow → si vacío → `Empty`; si no → separar en `crisisAlerts` (level == "CRISIS") y `regularAlerts` (level != "CRISIS") → `Loaded`
  - [ ] `MutableStateFlow<AlertCenterUiState>(Loading)` + `asStateFlow()`

#### UI Layer — Pantalla

- [ ] **Crear `AlertCenterScreen`** (H1)
  - [ ] Parámetros: `onNavigateBack: () -> Unit`, `onNavigateToAlertDetail: (Long) -> Unit`, `viewModel: AlertCenterViewModel = hiltViewModel()`
  - [ ] Scaffold con `CenterAlignedTopAppBar`: título "Alertas", subtítulo "N alertas activas", navigationIcon ArrowBack
  - [ ] Body `when (uiState)`:
    - `Loading`: `CircularProgressIndicator` centrado
    - `Empty`: ícono ✅ 48dp verde + "No hay alertas activas. Todo en orden." centrado
    - `Loaded`: `LazyColumn` con:
      1. **Sección "Crisis"** (si `crisisAlerts.isNotEmpty()`): header Title Small Error `#BA1A1A`, Divider Error, `AlertCard` por cada crisis
      2. **Sección "Alertas"** (si `regularAlerts.isNotEmpty()`): header Title Small, Divider, `AlertCard` por cada regular

- [ ] **Crear `AlertCard` composable**
  - [ ] Crear archivo: `ui/alerts/AlertCard.kt`
  - [ ] Parámetros: `alert: AlertItem`, `onClick: () -> Unit`
  - [ ] Fondo por level:
    - CRISIS → Elevated Card, fondo Error Container (`#FFDAD6`/`#930009`), texto On Error Container, Elevation 2
    - HIGH_ALERT → Filled Card, fondo `#FFF3E0`/`#4E2600`
    - MEDIUM_ALERT → Filled Card, fondo `#FFF8E1`/`#4A3800`
  - [ ] Layout: Row con `AlertLevelIndicator` leading + Column (tipo Title Medium + entidad Body Medium + dato clave Body Small)

### Fase 8: H2 — Detalle de Alerta (Screen + ViewModel)

**AC: Datos disparadores, análisis causal, recomendaciones, links condicionales**

#### UI Layer — ViewModel

- [ ] **Crear `AlertDetailUiState`**
  - [ ] `sealed interface AlertDetailUiState`: `data object Loading`, `data class Loaded(val detail: AlertDetail)`

- [ ] **Crear `AlertDetailViewModel`**
  - [ ] `@HiltViewModel class AlertDetailViewModel @Inject constructor(private val getAlertDetailUseCase: GetAlertDetailUseCase, savedStateHandle: SavedStateHandle)`
  - [ ] `val alertId: Long = savedStateHandle.get<Long>("alertId") ?: throw IllegalArgumentException("alertId is required")`
  - [ ] `init {}`: launch → invoke use case → `Loaded(detail)`

#### UI Layer — Pantalla

- [ ] **Crear `AlertDetailScreen`** (H2)
  - [ ] Parámetros: `onNavigateBack: () -> Unit`, `onNavigateToExerciseHistory: (Long) -> Unit`, `onNavigateToDeloadManagement: () -> Unit`, `viewModel: AlertDetailViewModel = hiltViewModel()`
  - [ ] Scaffold con `CenterAlignedTopAppBar`: título "Detalle de Alerta", navigationIcon ArrowBack
  - [ ] Body `when (uiState)`:
    - `Loading`: `CircularProgressIndicator` centrado
    - `Loaded`: `LazyColumn` con:
      1. Card tipo y nivel — fondo por nivel, leading `AlertLevelIndicator(level, 16.dp)`, título tipo, entidad, dato resumen
      2. Divider
      3. Datos que dispararon la alerta — contenido varía por tipo de `triggerData`:
         - `PlateauTrigger` → sesiones: "Sesión N: X Kg · Y reps"
         - `ProgressionRateTrigger` → "Tasa de progresión: XX%"
         - `RirTrigger` → "RIR promedio: X.X — Módulo Y"
         - `AdherenceTrigger` → "Adherencia: XX% (N/M sesiones)" + "N semanas consecutivas"
         - `TonnageDropTrigger` → "Grupo: X — Caída: XX%" + anterior vs actual. Si `isDeloadContextualized` → texto azul "Descarga planificada — caída esperada"
         - `InactivityTrigger` → "Módulo X: N días sin sesión" + "Grupos afectados: ..."
      4. Análisis causal (Body Large On Surface) — texto de `detail.causalAnalysis`
      5. Recomendaciones — header Title Small + lista con leading "▸"
      6. Links condicionales:
         - Si `showExerciseHistoryLink` → Text Button "Ver historial del ejercicio →" → `onNavigateToExerciseHistory(exerciseId!!)`
         - Si `showDeloadLink` → Text Button "Gestionar descarga →" → `onNavigateToDeloadManagement()`

### Fase 9: Navegación y Strings

**AC: Navegación B1→H1→H2→F3/I1, badge reactivo, Bottom Nav**

#### Navigation Layer

- [ ] **Agregar rutas en `NavigationRoutes`**
  - [ ] `const val ALERT_CENTER = "alert-center"`
  - [ ] `const val ALERT_DETAIL = "alert-detail/{alertId}"`
  - [ ] `fun alertDetailRoute(alertId: Long) = "alert-detail/$alertId"`

- [ ] **Actualizar `TensionNavHost`**
  - [ ] Reemplazar `onNavigateToAlerts = { /* TODO: HU-17 */ }` con:
    `onNavigateToAlerts = { navController.navigate(NavigationRoutes.ALERT_CENTER) }`
  - [ ] Agregar composable `ALERT_CENTER`:
    ```kotlin
    composable(NavigationRoutes.ALERT_CENTER) {
        AlertCenterScreen(
            onNavigateBack = { navController.popBackStack() },
            onNavigateToAlertDetail = { alertId ->
                navController.navigate(NavigationRoutes.alertDetailRoute(alertId))
            }
        )
    }
    ```
  - [ ] Agregar composable `ALERT_DETAIL`:
    ```kotlin
    composable(
        route = NavigationRoutes.ALERT_DETAIL,
        arguments = listOf(navArgument("alertId") { type = NavType.LongType })
    ) {
        AlertDetailScreen(
            onNavigateBack = { navController.popBackStack() },
            onNavigateToExerciseHistory = { exerciseId ->
                navController.navigate(NavigationRoutes.exerciseHistoryRoute(exerciseId))
            },
            onNavigateToDeloadManagement = {
                navController.navigate(NavigationRoutes.DELOAD_MANAGEMENT)
            }
        )
    }
    ```

- [ ] **Actualizar `BottomNavigationBar`** (Nota 7)
  - [ ] Agregar `childRoutePrefixes = setOf("alert-center", "alert-detail")` al `BottomNavItem` de HOME

#### Resources

- [ ] **Agregar ~30 strings en `strings.xml`**
  - [ ] Sección `<!-- Alert Center H1 -->`:
    - `alert_center_title` = "Alertas", `alert_center_subtitle` = "%1$d alertas activas", `alert_center_empty` = "No hay alertas activas. Todo en orden.", `alert_center_section_crisis` = "Crisis", `alert_center_section_alerts` = "Alertas"
  - [ ] Sección `<!-- Alert Detail H2 -->`:
    - `alert_detail_title` = "Detalle de Alerta", `alert_detail_recommendations_title` = "Recomendaciones", `alert_detail_view_history` = "Ver historial del ejercicio →", `alert_detail_manage_deload` = "Gestionar descarga →"
  - [ ] Sección `<!-- Alert Types -->`:
    - `alert_type_plateau` = "Meseta detectada", `alert_type_low_progression` = "Tasa de progresión baja", `alert_type_rir_out_of_range` = "RIR fuera de rango", `alert_type_low_adherence` = "Adherencia baja", `alert_type_tonnage_drop` = "Caída de tonelaje", `alert_type_module_inactivity` = "Inactividad por módulo", `alert_type_module_requires_deload` = "Módulo requiere descarga"
  - [ ] Sección `<!-- Alert Causal Analysis -->`:
    - `alert_analysis_rir_low` = "El ejecutante está entrenando demasiado cerca del fallo técnico de forma sostenida. Se recomienda prescribir una descarga para permitir recuperación del SNC."
    - `alert_analysis_rir_high` = "El estímulo puede ser insuficiente para generar adaptación. Se recomienda incrementar la carga de los ejercicios del módulo."
    - `alert_analysis_adherence_single` = "Adherencia por debajo del 60%% esta semana. La baja frecuencia puede afectar la resolución temporal de las señales del sistema."
    - `alert_analysis_adherence_consecutive` = "Adherencia por debajo del 60%% durante %1$d semanas consecutivas. Las comparaciones entre sesiones pierden validez por el excesivo tiempo entre ellas."
    - `alert_analysis_tonnage_deload` = "Descarga planificada — caída de tonelaje esperada y controlada."
    - `alert_analysis_tonnage_regression` = "Caída no intencional de tonelaje. Evaluar causas y considerar ajustar el volumen de entrenamiento."
    - `alert_analysis_inactivity` = "Módulo %1$s lleva %2$d días sin sesión. Los grupos musculares asociados (%3$s) pueden estar perdiendo adaptaciones."
    - `alert_analysis_progression_low` = "Tasa de progresión por debajo del umbral de alerta. El ejercicio muestra estancamiento sostenido."
    - `alert_analysis_progression_crisis` = "Tasa de progresión en estado crítico. El ejercicio muestra estancamiento prolongado que compromete las adaptaciones."
  - [ ] Sección `<!-- Alert Recommendations -->`:
    - `alert_recommendation_deload` = "Considerar prescribir una descarga para permitir recuperación"
    - `alert_recommendation_increase_load` = "Incrementar la carga de los ejercicios del módulo"
    - `alert_recommendation_increase_frequency` = "Incrementar la frecuencia de entrenamiento semanal"
    - `alert_recommendation_prioritize_module` = "Priorizar el módulo %1$s en las próximas sesiones"

### Fase 10: Tests Unitarios

**AC: Todos los CAs cubiertos por tests del pipeline y UI**

#### Tests — AlertThresholdRule

- [ ] **Test `AlertThresholdRuleTest`** (AC: 18.01, 18.02, 18.06, 18.07, 18.12, 18.17, 18.18, 18.23, 18.24)
  - [ ] 13 casos con boundary values:
    1. `isProgressionAlert`: `39.9 == true`, `40.0 == false`
    2. `isProgressionCrisis`: `19.9 == true`, `20.0 == false`
    3. `isRirLow`: `1.4 == true`, `1.5 == false`
    4. `isRirHigh`: `3.6 == true`, `3.5 == false`
    5. `isAdherenceLow`: boundary 60%
    6. `isTonnageAlert`: boundary 10%
    7. `isTonnageCrisis`: boundary 20%
    8. `isInactivityAlert`: `11L == true`, `10L == false`
    9. `isInactivityCrisis`: boundary 14 días
    10. `progressionLevel`: CRISIS < 20%, MEDIUM_ALERT < 40%, null ≥ 40%
    11. `tonnageLevel with deload always returns MEDIUM_ALERT never CRISIS` (CA-18.19/CA-18.20): `tonnageLevel(25.0, true) == "MEDIUM_ALERT"`
    12. `inactivityLevel`: boundary values
    13. `MUSCLE_GROUPS_BY_MODULE`: A/B/C contienen los grupos correctos (CA-18.25)

#### Tests — Use Cases

- [ ] **Test `GetActiveAlertCountUseCaseTest`**: invoke retorna Flow con count del repository
- [ ] **Test `GetActiveAlertsUseCaseTest`**: invoke retorna Flow con lista del repository
- [ ] **Test `GetAlertDetailUseCaseTest`**: invoke retorna AlertDetail del repository

#### Tests — ViewModels

- [ ] **Test `AlertCenterViewModelTest`**:
  - Caso 1: estado inicial Loading
  - Caso 2: Loaded con crisisAlerts y regularAlerts separados correctamente
  - Caso 3: Empty cuando lista vacía
  - Caso 4: totalCount refleja número total de alertas

- [ ] **Test `AlertDetailViewModelTest`**:
  - Caso 1: carga detalle al init con SavedStateHandle + alertId
  - Caso 2: throws IllegalArgumentException cuando alertId falta

#### Tests — Pipeline Steps

- [ ] **Test `EvaluateLowProgressionRateTest`** (AC: 18.01, 18.02, 18.05):
  - Crea MEDIUM_ALERT cuando tasa < 40%
  - Crea CRISIS cuando tasa < 20%
  - Resuelve alerta cuando tasa recupera ≥ 40%
  - Escala de MEDIUM_ALERT a CRISIS (patrón resolve + re-insert)

- [ ] **Test `EvaluateRirOutOfRangeTest`** (AC: 18.06, 18.07, 18.08, 18.11):
  - Crea alerta cuando ambas sesiones < 1.5
  - Crea alerta cuando ambas sesiones > 3.5
  - NO crea alerta cuando solo una sesión es fuera de rango (CA-18.08)
  - Resuelve alerta cuando RIR retorna a zona óptima (CA-18.11)
  - Skip si módulo tiene < 2 sesiones

- [ ] **Test `EvaluateLowAdherenceTest`** (AC: 18.12, 18.13, 18.16):
  - Crea MEDIUM_ALERT con una semana < 60%
  - Crea CRISIS con dos semanas consecutivas < 60% (CA-18.13)
  - Resuelve todas las alertas cuando adherencia ≥ 60% (CA-18.16)
  - Evalúa incluso durante sesión de descarga (Step 9 ANTES del deload guard)

- [ ] **Test `EvaluateTonnageDropTest`** (AC: 18.17, 18.18, 18.19, 18.20):
  - Crea MEDIUM_ALERT cuando caída > 10%
  - Crea CRISIS cuando caída > 20%
  - Crea MEDIUM_ALERT (no CRISIS) durante microciclo de descarga (CA-18.19/CA-18.20)
  - Skip si hay menos de 2 microciclos
  - Resuelve alerta cuando tonelaje recupera

- [ ] **Test `EvaluateModuleInactivityTest`** (AC: 18.23, 18.24, 18.26, 18.29):
  - Crea MEDIUM_ALERT cuando módulo inactivo > 10 días (CA-18.23)
  - Crea CRISIS cuando módulo inactivo > 14 días (CA-18.24)
  - Resuelve alerta cuando se completa sesión del módulo (CA-18.29)
  - Usa días naturales, no sesiones (CA-18.26)
  - Skip si módulo nunca ha sido entrenado
  - Resuelve inactividad incluso durante sesión de descarga del módulo (Step 11 ANTES del guard)

### Fase 11: QA y Deployment

#### Code Quality

- [ ] **Ejecutar Agente Peer Review**
- [ ] **Resolver incidentes del Peer Review** (condicional)

#### Deployment DEV

- [ ] **Crear Pull Request**
- [ ] **Ejecutar pipeline deployment DEV**

#### Testing Manual

- [ ] **Pipeline:** Cerrar sesión crea alertas correctas para cada tipo, deduplicación funcional, escalamiento de nivel, resolución automática
- [ ] **B1:** Badge se actualiza reactivamente al crear/resolver alertas
- [ ] **H1:** Listado agrupado (crisis vs alertas), estado vacío, navegación a H2
- [ ] **H2:** Card tipo/nivel, datos disparadores por tipo, análisis causal, recomendaciones, link a F3 (solo meseta/progresión), link a I1 (solo descarga/RIR bajo), solo lectura
- [ ] **CA-18.19/CA-18.20:** Caídas de tonelaje durante descarga → "Caída esperada" y no CRISIS
- [ ] **CA-18.08:** Una sola sesión con RIR fuera de rango NO dispara alerta
- [ ] **CA-18.29:** Completar sesión del módulo inactivo resuelve la alerta
- [ ] **Navegación:** "Inicio" activo en H1 y H2, H2→F3 muestra Bottom Nav, H2→I1 muestra Bottom Nav

---

### Resumen vinculación CA — Fases

| CA | Fases | Componentes principales |
|---|---|---|
| CA-18.01 | 1, 4 | `AlertThresholdRule.isProgressionAlert()`, `evaluateLowProgressionRate()` Step 7 |
| CA-18.02 | 1, 4 | `AlertThresholdRule.isProgressionCrisis()`, Step 7 |
| CA-18.03 | 7 | `AlertCard` + `AlertLevelIndicator` CRISIS vs MEDIUM_ALERT |
| CA-18.04 | 4 | Sin lógica de bloqueo |
| CA-18.05 | 4 | Step 7 ejecuta en cada `evaluateProgression()` |
| CA-18.06 | 1, 4 | `isRirLow()`, Step 8 |
| CA-18.07 | 1, 4 | `isRirHigh()`, Step 8 |
| CA-18.08 | 4 | `getSessionIdsByModuleInRange(moduleCode, 2)` + RIR por sesión individual |
| CA-18.09 | 7 | `AlertCard` diferencia contenido RIR bajo vs alto |
| CA-18.10 | 4 | Sin lógica de bloqueo |
| CA-18.11 | 4 | `resolveByModuleAndType(moduleCode, "RIR_OUT_OF_RANGE")` Step 8 else |
| CA-18.12 | 1, 4 | `isAdherenceLow()`, Step 9 (ANTES del deload guard) |
| CA-18.13 | 4 | Verificación semana anterior → escalamiento CRISIS, Step 9 |
| CA-18.14 | 7 | `AlertCard` MEDIUM_ALERT (1 semana) vs CRISIS (2+ semanas) |
| CA-18.15 | 4 | Sin lógica de bloqueo |
| CA-18.16 | 4 | `resolveAllByType("LOW_ADHERENCE")` cuando adherencia ≥ 60% |
| CA-18.17 | 1, 4 | `isTonnageAlert()`, Step 10 |
| CA-18.18 | 1, 4 | `isTonnageCrisis()`, Step 10 |
| CA-18.19 | 4 | `isDeloadMicrocycle` en Step 10 |
| CA-18.20 | 4, 9 | Mensaje "Descarga planificada" + nivel MEDIUM_ALERT en Step 10 |
| CA-18.21 | 7 | `AlertCard` MEDIUM_ALERT vs CRISIS tonelaje |
| CA-18.22 | 4 | Sin lógica de bloqueo |
| CA-18.23 | 1, 2, 4 | `getLastSessionDateByModule()` + `isInactivityAlert()`, Step 11 |
| CA-18.24 | 1, 4 | `isInactivityCrisis()`, Step 11 |
| CA-18.25 | 1 | `AlertThresholdRule.MUSCLE_GROUPS_BY_MODULE` |
| CA-18.26 | 4 | `ChronoUnit.DAYS.between(LocalDate.parse(lastDate), LocalDate.now())` |
| CA-18.27 | 7 | `AlertCard` MEDIUM_ALERT (>10 días) vs CRISIS (>14 días) |
| CA-18.28 | 4 | Sin alteración de rotación |
| CA-18.29 | 4 | `resolveByModuleAndType(moduleCode, "MODULE_INACTIVITY")` Step 11 (ANTES del deload guard) |
