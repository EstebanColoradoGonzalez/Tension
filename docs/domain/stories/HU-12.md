# HU-12 — Motor de Detección: Regresión, Meseta y Necesidad de Descarga

## §1 Metadatos

| Campo | Valor |
|---|---|
| **ID** | HU-12 |
| **Epic** | EPIC-03 — Motor de Decisión |
| **Estado** | Done |
| **Requisitos** | RF29, RF30, RF34, RF35, RF36, RF37, RNF05 |
| **Predecesoras** | HU-05, HU-06, HU-09, HU-10, HU-11 |
| **Sucesoras** | HU-13, HU-14, HU-17 |

---

## §2 Narrativa de Negocio

Como ejecutante, quiero que el sistema detecte automáticamente regresiones en mis ejercicios, identifique fatiga acumulada del módulo, diagnostique mesetas con análisis causal, me recomiende acciones correctivas escalonadas y señale cuando un módulo completo necesita descarga, para tener un diagnóstico integral y objetivo de mi estado de entrenamiento que me permita tomar decisiones informadas antes de que la acumulación de fatiga comprometa mi progreso.

### Historias originales consolidadas

Esta historia consolida las siguientes historias del diseño original, que forman una cadena de decisión inseparable (detectar → diagnosticar → prescribir → escalar):

- **HU-12 original** — Detectar regresión y fatiga acumulada (RF29, RF30)
- **HU-14 original** — Detectar y alertar meseta en ejercicios (RF34, RF35)
- **HU-15 original** — Recomendar acciones correctivas escalonadas ante meseta (RF36)
- **HU-16 original** — Detectar necesidad de descarga por módulo (RF37)

---

## §3 Criterios de Aceptación (BDD)

### Bloque A — Detección de Regresión (RF29)

#### CA-12.01 — Detección de regresión por caída de repeticiones

**Dado que** el sistema compara un ejercicio contra su último registro histórico al cerrar una sesión,
**cuando** detecta que el peso utilizado es igual al de la sesión anterior pero las repeticiones cayeron en ≥ 2 de las 4 series,
**entonces** clasifica al ejercicio con "Regresión" para esta sesión.

#### CA-12.02 — Detección de regresión por aumento de RIR

**Dado que** el sistema compara un ejercicio contra su último registro histórico al cerrar una sesión,
**cuando** detecta que la carga es la misma, las repeticiones son similares a la sesión anterior, pero el RIR promedio subió en ≥ 1.5 puntos,
**entonces** clasifica al ejercicio con "Regresión" para esta sesión, interpretando que el ejecutante necesitó más esfuerzo percibido para mantener el mismo rendimiento.

#### CA-12.03 — Regresión por caída de carga

**Dado que** el sistema compara un ejercicio contra su último registro histórico,
**cuando** detecta que el peso utilizado es menor al de la sesión anterior,
**entonces** clasifica al ejercicio con "Regresión" para esta sesión.

### Bloque B — Detección de Fatiga Acumulada del Módulo (RF30)

#### CA-12.04 — Detección de fatiga acumulada del módulo

**Dado que** el sistema ha clasificado la progresión de todos los ejercicios registrados en la sesión,
**cuando** identifica regresión simultánea en ≥ 50% de los ejercicios de esa sesión,
**entonces** detecta y registra "Fatiga acumulada del módulo" para el módulo de la sesión.

#### CA-12.05 — Umbral del 50% calculado sobre ejercicios registrados

**Dado que** una sesión se cierra como Incompleta con menos ejercicios registrados que los prescritos,
**cuando** el sistema evalúa fatiga acumulada,
**entonces** calcula el umbral del 50% sobre los ejercicios que efectivamente tienen registros en la sesión, no sobre el total de ejercicios prescritos.

#### CA-12.06 — Registro de las detecciones

**Dado que** el sistema detecta regresión en un ejercicio o fatiga acumulada en un módulo,
**cuando** completa el análisis post-sesión,
**entonces** persiste las detecciones vinculadas a la sesión para su uso en reglas de decisión posteriores (mesetas, alertas de descarga, KPIs).

### Bloque C — Detección de Meseta (RF34, RF35)

#### CA-12.07 — Detección de meseta por 3 sesiones sin progresión

**Dado que** el sistema evalúa la progresión de un ejercicio al cerrar una sesión,
**cuando** detecta que el ejercicio no ha registrado progresión positiva (ni aumento de carga ni aumento de repeticiones) durante 3 sesiones consecutivas del mismo ejercicio,
**entonces** marca el ejercicio con estado "En Meseta".

#### CA-12.08 — Sesiones consecutivas del mismo ejercicio

**Dado que** el sistema evalúa si un ejercicio está en meseta,
**cuando** cuenta las sesiones consecutivas sin progresión,
**entonces** solo cuenta sesiones que incluyen ese ejercicio específico, sin importar cuántas sesiones de otros módulos hayan ocurrido entre ellas; la consecutividad es del ejercicio, no del calendario.

#### CA-12.09 — Alerta de meseta al ejecutante

**Dado que** un ejercicio entra en estado de "En Meseta",
**cuando** el sistema completa la detección,
**entonces** emite una alerta visible al ejecutante indicando el nombre del ejercicio en meseta y que lleva 3 sesiones sin progresión.

#### CA-12.10 — Análisis causal: RIR bajo indica límite de carga

**Dado que** un ejercicio entra en meseta,
**cuando** el sistema analiza los datos de las últimas 3 sesiones y el RIR promedio es consistentemente bajo (0-1),
**entonces** la alerta incluye el análisis causal: "El ejecutante está entrenando cerca del fallo técnico — posible límite de carga con este peso. El cuerpo no tiene reserva suficiente para progresar."

#### CA-12.11 — Análisis causal: RIR alto indica carga conservadora

**Dado que** un ejercicio entra en meseta,
**cuando** el sistema analiza los datos de las últimas 3 sesiones y el RIR promedio es alto (3+),
**entonces** la alerta incluye el análisis causal: "El ejecutante mantiene reserva alta — posible carga conservadora. El estímulo puede ser insuficiente para generar adaptación."

#### CA-12.12 — Análisis causal: estancamiento grupal indica fatiga sistémica

**Dado que** un ejercicio entra en meseta,
**cuando** el sistema detecta que otros ejercicios del mismo grupo muscular también están estancados o en meseta,
**entonces** la alerta incluye el análisis causal: "Múltiples ejercicios del mismo grupo muscular estancados — posible fatiga sistémica del grupo muscular."

#### CA-12.13 — Señales visuales diferenciadas

**Dado que** el sistema emite una alerta de meseta,
**cuando** el ejecutante visualiza la alerta,
**entonces** la alerta es visualmente distinguible mediante colores e iconografía específica para meseta, diferenciándose de las señales de progresión positiva y regresión, sin depender únicamente del texto.

#### CA-12.14 — Actualización del estado del ejercicio

**Dado que** un ejercicio entra en meseta,
**cuando** el sistema actualiza el estado persistente de progresión,
**entonces** el estado del ejercicio cambia de "En Progresión" a "En Meseta" y permanece así hasta que se registre progresión positiva o se active una descarga.

### Bloque D — Acciones Correctivas Escalonadas ante Meseta (RF36)

#### CA-12.15 — Recomendación en sesión 4 sin progreso

**Dado que** un ejercicio lleva 4 sesiones consecutivas sin progresión positiva (1 sesión más allá de la detección de meseta),
**cuando** el sistema emite la recomendación,
**entonces** recomienda al ejecutante: aplicar un microincremento de carga (la menor carga posible disponible) o intentar extender las repeticiones dentro del rango (buscar llegar a 12 reps antes de subir carga), presentando ambas opciones como alternativas.

#### CA-12.16 — Recomendación en sesión 6 sin progreso

**Dado que** un ejercicio lleva 6 sesiones consecutivas sin progresión positiva,
**cuando** el sistema emite la recomendación,
**entonces** recomienda al ejecutante: rotar a otra versión del módulo para cambiar el patrón de estímulo muscular, indicando que la versión actual puede haber agotado su efecto adaptativo para ese ejercicio.

#### CA-12.17 — Escalonamiento acumulativo

**Dado que** un ejercicio alcanza la sesión 6 sin progreso,
**cuando** el sistema presenta la recomendación,
**entonces** muestra la recomendación de sesión 6 (rotar versión) como complemento a la de sesión 4 (microincremento o extensión de reps), indicando que ambas estrategias pueden combinarse.

#### CA-12.18 — Recomendaciones informativas, no bloqueantes

**Dado que** el sistema emite una recomendación correctiva,
**cuando** el ejecutante visualiza la recomendación,
**entonces** la recomendación es informativa: el ejecutante puede seguirla o ignorarla. El sistema no impide iniciar ni completar una sesión por existir una recomendación pendiente.

#### CA-12.19 — Vinculación con la alerta de meseta

**Dado que** un ejercicio está en meseta y el sistema ha emitido una alerta,
**cuando** el sistema emite las recomendaciones correctivas,
**entonces** las presenta asociadas a la alerta de meseta del ejercicio, permitiendo al ejecutante ver el diagnóstico (análisis causal) y la prescripción (acción correctiva) de forma conjunta.

### Bloque E — Detección de Necesidad de Descarga por Módulo (RF37)

#### CA-12.20 — Detección por meseta/regresión simultánea

**Dado que** el sistema evalúa el estado de los ejercicios de un módulo al cerrar una sesión,
**cuando** detecta que ≥ 50% de los ejercicios del módulo están simultáneamente en estado de "En Meseta" o clasificados con "Regresión" en la sesión actual,
**entonces** marca el módulo como "Requiere descarga" y emite una señal al ejecutante.

#### CA-12.21 — Detección por fatiga acumulada del módulo

**Dado que** el sistema ha detectado fatiga acumulada del módulo en la sesión actual (regresión simultánea en ≥ 50% de los ejercicios — CA-12.04),
**cuando** completa el análisis post-sesión,
**entonces** marca el módulo como "Requiere descarga" y emite una señal al ejecutante, independientemente del estado de meseta de los ejercicios individuales.

#### CA-12.22 — Cálculo del umbral del 50%

**Dado que** el sistema evalúa si un módulo requiere descarga,
**cuando** calcula el porcentaje de ejercicios en meseta/regresión,
**entonces** lo calcula sobre el total de ejercicios prescritos para la versión del módulo actualmente en ejecución (11 para A, 11 para B, 9 para C).

#### CA-12.23 — Señal informativa, no bloqueante

**Dado que** el sistema detecta que un módulo requiere descarga,
**cuando** emite la señal al ejecutante,
**entonces** la señal es informativa y recomienda activar el modo de descarga (HU-14), pero no impide al ejecutante continuar entrenando con la carga normal si así lo decide.

#### CA-12.24 — Registro de la detección

**Dado que** el sistema detecta que un módulo requiere descarga,
**cuando** completa la evaluación,
**entonces** persiste la detección con la fecha y el módulo afectado, disponible para consulta en el historial.

---

## §4 Dependencias Técnicas e Integración

### Componentes nuevos

| # | Componente | Capa | Responsabilidad |
|---|---|---|---|
| 1 | `AlertEntity` | Data (entity) | Entity Room para tabla `alert` (Modelo de Datos §3.16) |
| 2 | `AlertDao` | Data (DAO) | Insert, query activas, resolve, existsActive, countActive |
| 3 | `ModuleFatigueRule` | Domain (rules) | R4 módulo: `detectFatigue(regressionCount, exercisesWithRecords) → Boolean` |
| 4 | `DeloadNeedRule` | Domain (rules) | R3 §3ᵃ intervención + R4: `needsDeload(affectedCount, totalCount, fatigueDetected) → Boolean` |
| 5 | `PlateauCausalAnalysisRule` | Domain (rules) | R3 análisis: `analyze(avgRirs, isGroupStagnant) → PlateauCause` |
| 6 | `CorrectiveActionRule` | Domain (rules) | R3 §1ᵃ/2ᵃ intervención: `recommend(sessionsWithoutProgression) → List<CorrectiveAction>` |
| 7 | `PlateauCause` | Domain (model) | Enum: `LOW_RIR_LIMIT`, `HIGH_RIR_CONSERVATIVE`, `GROUP_STAGNATION`, `MIXED` |
| 8 | `CorrectiveAction` | Domain (model) | Enum: `MICRO_INCREMENT_OR_EXTEND_REPS`, `ROTATE_VERSION` |

### Componentes modificados

| # | Componente | Cambio | Nivel |
|---|---|---|---|
| 1 | `TensionDatabase` | Agregar `AlertEntity` a entities, agregar `alertDao()`, version 4 → 5, migración | Mayor |
| 2 | `DatabaseModule` | Agregar `provideAlertDao()` | Menor |
| 3 | `SessionRepositoryImpl` | Inyectar `AlertDao`; extender `evaluateProgression()` con gestión de alertas post-loop + guardia de descarga | Mayor |
| 4 | `SessionDao` | Agregar queries `getModuleVersionIdBySessionId()`, `getDeloadIdBySessionId()` | Menor |
| 5 | `PlanAssignmentDao` | Agregar queries: `countExercisesForModuleVersion()`, `countAffectedForDeload()` | Menor |

### Historias relacionadas

**Predecesoras (datos que HU-12 consume):**

- HU-05: `ExerciseProgressionEntity` con `status` enums, `deload_id`, `alert badge placeholder` (`alertCount=0`)
- HU-06: `exercise_set` como productor de datos (peso, reps, RIR) que alimentan la clasificación
- HU-09: `closeSession()` transacción — punto de extensión para HU-12
- HU-10: `evaluateProgression()` loop, `REGRESSION`, `IN_PLATEAU`, `PLATEAU_THRESHOLD=3`, contador `sessionsWithoutProgression`. Los 7 CAs ya cubiertos provienen de HU-10.
- HU-11: Prescripción de carga en el mismo loop — no afectada por HU-12

**Sucesoras (dependen de HU-12):**

- HU-13: E5 — señales "↓ Regresión" y "Considerar descarga" consumen `REGRESSION` + alerta `MODULE_REQUIRES_DELOAD`
- HU-14: Protocolo de descarga — lee alerta `MODULE_REQUIRES_DELOAD` para activar descarga
- HU-17: H1/H2 — lee alertas activas (`AlertEntity`/`AlertDao`), análisis causal (`PlateauCausalAnalysisRule`), acciones correctivas (`CorrectiveActionRule`)

---

## §5 Análisis Arquitectónico

### Decisiones de Diseño

**Patrón Arquitectónico:** Extensión del pipeline de cierre de sesión (HU-10/HU-11) + reglas puras en `domain/rules/` (ADR-06) + nueva capa de persistencia de alertas.

**Justificación:** HU-12 sigue la cadena establecida por HU-10 y HU-11: lógica de backend sin pantalla propia, ejecutada dentro de la transacción de `closeSession()`, con funciones puras en `domain/rules/` testeables sin emulador. HU-12 agrega una dimensión nueva: la evaluación a **nivel de módulo** (post-loop), que opera DESPUÉS de que el loop per-ejercicio actualice todos los estados. Además, establece la **infraestructura de alertas** (`AlertEntity` + `AlertDao`) que HU-17 (Sistema de Alertas) reutilizará.

**1. HU-12 es lógica pura de backend — no tiene pantalla propia.**

Mapa de Navegación: E5 (HU-13) consume señales de regresión/descarga, H1/H2 (HU-17) consume alertas de meseta, B1 muestra badge de alertas activas. HU-12 produce datos, no los visualiza. Todo ocurre dentro de la transacción de `closeSession()`.

**2. Siete de los 24 CAs ya están cubiertos por HU-10 → HU-12 no los reimplementa.**

| CA ya cubierto | Mecanismo existente (HU-10) |
|---|---|
| CA-12.01 (regresión por reps) | `classifyStandard()` → `totalReps < previous → REGRESSION` |
| CA-12.02 (regresión por RIR) | `classifyStandard()` → `rirRise >= 1.5 → REGRESSION` |
| CA-12.03 (regresión por carga) | `classifyStandard()` → `weightLower → REGRESSION` |
| CA-12.07 (meseta a 3 sesiones) | `resolveNewProgressionState()` → `counter >= 3 → IN_PLATEAU` |
| CA-12.08 (consecutividad por ejercicio) | Diseño inherente — `sessionsWithoutProgression` solo se incrementa cuando ESE ejercicio se evalúa |
| CA-12.14 (actualización de estado) | `resolveNewProgressionState()` → `status = IN_PLATEAU` |
| CA-12.18 (no bloqueante) | Las alertas y recomendaciones son informativas por diseño — no alteran ningún flujo |

**Nota sobre CA-12.01:** El MDS R4 define regresión como "reps caen en ≥ 2 de las 4 series" (criterio per-serie). HU-10 implementa un criterio agregado (`totalReps < previous → REGRESSION`). Ambos convergen en la práctica: si las repeticiones totales bajan, al menos algunas series individuales bajaron. El edge case (series individuales cambian pero total se mantiene) es un patrón atípico de entrenamiento real. Se mantiene el enfoque agregado como suficiente para el MVP.

**3. Cuatro reglas puras nuevas en `domain/rules/` (ADR-06).**

Cada regla es un `object` Kotlin con funciones puras, sin dependencias Android, testeable con JUnit:

| Regla | MDS | Invocación | Responsabilidad |
|---|---|---|---|
| `ModuleFatigueRule` | R4 (módulo) | Write-time (cierre de sesión) | Detectar fatiga acumulada del módulo (≥50% regresiones en sesión) |
| `DeloadNeedRule` | R3 §3ᵃ intervención + R4 | Write-time (cierre de sesión) | Detectar si módulo requiere descarga |
| `PlateauCausalAnalysisRule` | R3 (análisis) | Read-time (detalle de alerta) | Analizar causa de meseta (RIR bajo/alto, estancamiento grupal) |
| `CorrectiveActionRule` | R3 §1ᵃ/2ᵃ intervención | Read-time (detalle de alerta) | Recomendar acciones correctivas escalonadas (sesión 4/6) |

**4. HU-12 establece la infraestructura de alertas que HU-17 reutiliza.**

El Modelo de Datos §3.16 define la tabla `alert` con 7 tipos. HU-12 crea `AlertEntity` + `AlertDao` (la infraestructura completa) pero solo escribe 2 tipos: `PLATEAU` y `MODULE_REQUIRES_DELOAD`. HU-17 agregará los otros 5 tipos (`LOW_PROGRESSION_RATE`, `RIR_OUT_OF_RANGE`, `LOW_ADHERENCE`, `TONNAGE_DROP`, `MODULE_INACTIVITY`) y construirá las pantallas H1/H2. Esto sigue el principio de extensión incremental: HU-12 introduce la tabla y los primeros consumidores; HU-17 amplía.

**5. Separación de responsabilidades WRITE-TIME vs READ-TIME.**

- **Write-time** (cierre de sesión): Detección de condiciones + creación/resolución de alertas en tabla `alert`. Datos persistidos: tipo, nivel, entidad afectada, mensaje, estado activo/resuelto.
- **Read-time** (detalle de alerta en H2): Análisis causal y recomendaciones correctivas. Computados dinámicamente desde datos crudos (series, sesiones).
- **Justificación:** El Modelo de Datos §3.16 es explícito: *"Los datos que dispararon la alerta y las recomendaciones escalonadas no se almacenan — se recalculan dinámicamente en la capa de aplicación a partir de las series, sesiones y la lógica del motor de reglas. Dado que las sesiones cerradas son inmutables, el recálculo siempre produce el mismo resultado."*

**6. El análisis causal (CA-12.10-12.12) recibe datos pre-computados.**

`PlateauCausalAnalysisRule.analyze()` recibe: (a) lista de avgRIR de las últimas 3 sesiones del ejercicio, (b) flag de estancamiento grupal. La obtención de estos datos (queries históricas + JOINs de `exercise_muscle_zone`) la hace el Repository/UseCase que sirve H2 — no la regla. La regla solo clasifica.

**7. Las recomendaciones correctivas (CA-12.15-12.17) se derivan del contador existente.**

`exercise_progression.sessions_without_progression` ya se mantiene por HU-10. `CorrectiveActionRule.recommend(counter)` aplica umbrales: `≥ 4` → microincremento/extensión de reps, `≥ 6` → rotar versión (acumulativo, CA-12.17). No requiere persistencia adicional — la información se computa al renderizar H2.

**8. Deduplicación y resolución automática de alertas.**

- **Deduplicación:** Antes de insertar un `PLATEAU` alert, verificar que no exista uno activo para el mismo `exercise_id`. Antes de insertar `MODULE_REQUIRES_DELOAD`, verificar que no exista uno activo para el mismo `module_code`.
- **Resolución automática (CA-12.14 implícito):** Cuando un ejercicio sale de `IN_PLATEAU` (progresión positiva → `IN_PROGRESSION`, counter reset a 0), la alerta PLATEAU se resuelve (`is_active = 0`, `resolved_at = hoy`). Cuando el módulo ya no cumple el umbral de descarga, la alerta MODULE_REQUIRES_DELOAD se resuelve.

**9. El umbral del 50% tiene denominadores diferentes según el contexto.**

| Detección | Denominador | CA |
|---|---|---|
| Fatiga acumulada del módulo | Ejercicios **con registros** en la sesión (no prescritos totales) | CA-12.05 |
| Necesidad de descarga | Total de ejercicios **prescritos** para la versión del módulo | CA-12.22 |

**10. `moduleVersionId` se obtiene de la tabla `session` y se pasa como parámetro.**

`ActiveSessionInfo` no incluye `moduleVersionId` — solo `moduleCode` y `versionNumber`. Para las queries de deload need (CA-12.22), se necesita `moduleVersionId`. Se obtiene directamente de `session.module_version_id` mediante una nueva query en `SessionDao` y se pasa como parámetro a `evaluateProgression()`.

**11. Los mensajes de alerta son genéricos; el nombre de ejercicio/módulo se resuelve por JOIN.**

`alert.message` almacena texto descriptivo genérico ("3 sesiones sin progresión", "≥50% ejercicios en meseta/regresión"). Los nombres se obtienen al leer la alerta vía FKs (`exercise_id` → `exercise.name`, `module_code` → `module.name`). Esto evita denormalización y mantiene los mensajes actualizables.

**12. Guardia de descarga: la detección de módulo-nivel se omite durante sesiones de descarga.**

Si la sesión que se cierra pertenece a un ciclo de descarga activo (`session.deload_id != null`), el paso 6 (detección de fatiga/deload del módulo) se omite completamente. Justificación: durante la descarga, la carga se reduce al 60% — las regresiones en ese contexto son esperadas y no representan fatiga real. Emitir una alerta `MODULE_REQUIRES_DELOAD` durante una descarga que ya está en curso sería paradójico. La guardia usa `session.deload_id` que ya existe en `SessionEntity`. El paso 5d (alertas per-ejercicio de meseta) también se omite durante descarga porque `resolveNewProgressionState()` de HU-10 ya tiene guardia `IN_DELOAD`: no modifica estado ni contador durante descarga.

**13. E5 (HU-13) deriva "Considerar descarga" combinando clasificación y alertas.**

CA-13.05 espera una señal "Considerar descarga" por ejercicio en el resumen post-sesión (E5). La ruta de derivación es: E5 muestra "Considerar descarga" cuando `session_exercise.progression_classification == 'REGRESSION'` en la sesión actual **Y** existe una alerta activa `MODULE_REQUIRES_DELOAD` para el módulo del ejercicio. Si solo hay regresión aislada sin alerta de módulo, E5 muestra "↓ Regresión" pero no "Considerar descarga". La implementación de esta lógica de derivación es responsabilidad de HU-13, pero HU-12 provee los datos subyacentes.

### Especificaciones de Componentes Nuevos

#### `ModuleFatigueRule.kt`

```kotlin
package com.estebancoloradogonzalez.tension.domain.rules

object ModuleFatigueRule {

    const val FATIGUE_THRESHOLD = 0.50

    fun detectFatigue(regressionCount: Int, exercisesWithRecords: Int): Boolean {
        if (exercisesWithRecords == 0) return false
        return regressionCount.toDouble() / exercisesWithRecords >= FATIGUE_THRESHOLD
    }
}
```

#### `DeloadNeedRule.kt`

```kotlin
package com.estebancoloradogonzalez.tension.domain.rules

object DeloadNeedRule {

    const val DELOAD_THRESHOLD = 0.50

    fun needsDeload(
        affectedCount: Int,
        totalCount: Int,
        fatigueDetected: Boolean,
    ): Boolean {
        if (fatigueDetected) return true // CA-12.21
        if (totalCount == 0) return false
        return affectedCount.toDouble() / totalCount >= DELOAD_THRESHOLD // CA-12.20
    }
}
```

#### `PlateauCausalAnalysisRule.kt`

```kotlin
package com.estebancoloradogonzalez.tension.domain.rules

import com.estebancoloradogonzalez.tension.domain.model.PlateauCause

object PlateauCausalAnalysisRule {

    const val LOW_RIR_THRESHOLD = 1.0
    const val HIGH_RIR_THRESHOLD = 3.0

    fun analyze(
        lastSessionsAvgRir: List<Double>,
        isGroupStagnant: Boolean,
    ): PlateauCause {
        if (isGroupStagnant) return PlateauCause.GROUP_STAGNATION // CA-12.12
        if (lastSessionsAvgRir.isEmpty()) return PlateauCause.MIXED
        val overallAvg = lastSessionsAvgRir.average()
        return when {
            overallAvg <= LOW_RIR_THRESHOLD -> PlateauCause.LOW_RIR_LIMIT // CA-12.10
            overallAvg >= HIGH_RIR_THRESHOLD -> PlateauCause.HIGH_RIR_CONSERVATIVE // CA-12.11
            else -> PlateauCause.MIXED
        }
    }
}
```

#### `CorrectiveActionRule.kt`

```kotlin
package com.estebancoloradogonzalez.tension.domain.rules

import com.estebancoloradogonzalez.tension.domain.model.CorrectiveAction

object CorrectiveActionRule {

    const val MICRO_INCREMENT_THRESHOLD = 4
    const val ROTATE_VERSION_THRESHOLD = 6

    fun recommend(sessionsWithoutProgression: Int): List<CorrectiveAction> {
        if (sessionsWithoutProgression < MICRO_INCREMENT_THRESHOLD) return emptyList()
        val actions = mutableListOf(CorrectiveAction.MICRO_INCREMENT_OR_EXTEND_REPS) // CA-12.15
        if (sessionsWithoutProgression >= ROTATE_VERSION_THRESHOLD) {
            actions.add(CorrectiveAction.ROTATE_VERSION) // CA-12.16, CA-12.17
        }
        return actions
    }
}
```

#### `PlateauCause.kt`

```kotlin
package com.estebancoloradogonzalez.tension.domain.model

enum class PlateauCause {
    LOW_RIR_LIMIT,         // CA-12.10: RIR 0-1, entrenando cerca del fallo
    HIGH_RIR_CONSERVATIVE, // CA-12.11: RIR 3+, carga conservadora
    GROUP_STAGNATION,      // CA-12.12: múltiples ejercicios del grupo muscular estancados
    MIXED,                 // Ningún patrón claro dominante
}
```

#### `CorrectiveAction.kt`

```kotlin
package com.estebancoloradogonzalez.tension.domain.model

enum class CorrectiveAction {
    MICRO_INCREMENT_OR_EXTEND_REPS, // CA-12.15: sesión 4+, microincremento o extensión de reps
    ROTATE_VERSION,                 // CA-12.16: sesión 6+, rotar a otra versión del módulo
}
```

#### `AlertEntity.kt`

```kotlin
package com.estebancoloradogonzalez.tension.data.local.entity

import androidx.room.ColumnInfo
import androidx.room.Entity
import androidx.room.ForeignKey
import androidx.room.Index
import androidx.room.PrimaryKey

@Entity(
    tableName = "alert",
    foreignKeys = [
        ForeignKey(
            entity = ExerciseEntity::class,
            parentColumns = ["id"],
            childColumns = ["exercise_id"],
            onDelete = ForeignKey.RESTRICT,
        ),
        ForeignKey(
            entity = ModuleEntity::class,
            parentColumns = ["code"],
            childColumns = ["module_code"],
            onDelete = ForeignKey.RESTRICT,
        ),
    ],
    indices = [
        Index(value = ["is_active"]),
        Index(value = ["type"]),
        Index(value = ["exercise_id"]),
        Index(value = ["module_code"]),
    ],
)
data class AlertEntity(
    @PrimaryKey(autoGenerate = true)
    @ColumnInfo(name = "id")
    val id: Long = 0,

    @ColumnInfo(name = "type")
    val type: String,

    @ColumnInfo(name = "level")
    val level: String,

    @ColumnInfo(name = "exercise_id")
    val exerciseId: Long? = null,

    @ColumnInfo(name = "module_code")
    val moduleCode: String? = null,

    @ColumnInfo(name = "muscle_group")
    val muscleGroup: String? = null,

    @ColumnInfo(name = "message")
    val message: String,

    @ColumnInfo(name = "is_active", defaultValue = "1")
    val isActive: Int = 1,

    @ColumnInfo(name = "created_at")
    val createdAt: String,

    @ColumnInfo(name = "resolved_at")
    val resolvedAt: String? = null,
)
```

#### `AlertDao.kt`

```kotlin
package com.estebancoloradogonzalez.tension.data.local.dao

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.Query
import com.estebancoloradogonzalez.tension.data.local.entity.AlertEntity
import kotlinx.coroutines.flow.Flow

@Dao
interface AlertDao {

    @Insert
    suspend fun insert(alert: AlertEntity): Long

    @Query(
        """
        SELECT EXISTS(
            SELECT 1 FROM alert
            WHERE exercise_id = :exerciseId AND type = :type AND is_active = 1
        )
        """,
    )
    suspend fun existsActiveByExercise(exerciseId: Long, type: String): Boolean

    @Query(
        """
        SELECT EXISTS(
            SELECT 1 FROM alert
            WHERE module_code = :moduleCode AND type = :type AND is_active = 1
        )
        """,
    )
    suspend fun existsActiveByModule(moduleCode: String, type: String): Boolean

    @Query(
        """
        UPDATE alert
        SET is_active = 0, resolved_at = :resolvedAt
        WHERE exercise_id = :exerciseId AND type = :type AND is_active = 1
        """,
    )
    suspend fun resolveByExerciseAndType(exerciseId: Long, type: String, resolvedAt: String)

    @Query(
        """
        UPDATE alert
        SET is_active = 0, resolved_at = :resolvedAt
        WHERE module_code = :moduleCode AND type = :type AND is_active = 1
        """,
    )
    suspend fun resolveByModuleAndType(moduleCode: String, type: String, resolvedAt: String)

    @Query("SELECT COUNT(*) FROM alert WHERE is_active = 1")
    fun countActive(): Flow<Int>

    @Query("SELECT * FROM alert WHERE is_active = 1 ORDER BY level ASC, created_at DESC")
    fun getActiveAlerts(): Flow<List<AlertEntity>>
}
```

### Especificaciones de Componentes Modificados

#### Modificación #1 — `SessionExerciseForProgression` (verificación)

El DTO ya tiene `moduleCode: String` desde HU-10. No se requiere modificación adicional.

#### Modificación #2 — `SessionDao` (queries nuevas)

```kotlin
@Query("SELECT module_version_id FROM session WHERE id = :sessionId")
suspend fun getModuleVersionIdBySessionId(sessionId: Long): Long

@Query("SELECT deload_id FROM session WHERE id = :sessionId")
suspend fun getDeloadIdBySessionId(sessionId: Long): Long?
```

#### Modificación #3 — `PlanAssignmentDao` (queries nuevas)

```kotlin
@Query("SELECT COUNT(*) FROM plan_assignment WHERE module_version_id = :moduleVersionId")
suspend fun countExercisesForModuleVersion(moduleVersionId: Long): Int

@Query(
    """
    SELECT COUNT(DISTINCT pa.exercise_id)
    FROM plan_assignment pa
    LEFT JOIN exercise_progression ep ON pa.exercise_id = ep.exercise_id
    LEFT JOIN session_exercise se ON pa.exercise_id = se.exercise_id
        AND se.session_id = :sessionId
    WHERE pa.module_version_id = :moduleVersionId
    AND (ep.status = 'IN_PLATEAU' OR se.progression_classification = 'REGRESSION')
    """,
)
suspend fun countAffectedForDeload(moduleVersionId: Long, sessionId: Long): Int
```

#### Modificación #4 — `evaluateProgression()` en `SessionRepositoryImpl`

Cambio de firma: `private suspend fun evaluateProgression(sessionId: Long)` → `private suspend fun evaluateProgression(sessionId: Long, moduleVersionId: Long, isDeloadSession: Boolean)`

```kotlin
private suspend fun evaluateProgression(
    sessionId: Long,
    moduleVersionId: Long,
    isDeloadSession: Boolean,
) {
    val exercises = sessionExerciseDao.getSessionExercisesForProgression(sessionId)
    val today = LocalDate.now().toString()

    // HU-12: Accumulators for module-level analysis
    var regressionCount = 0
    var exercisesWithRecords = 0

    for (exercise in exercises) {
        // === Existing HU-10 code: fetch sets, build currentData/previousData, classify ===
        // === Existing HU-11 code: prescribe load ===

        // HU-12 Step 5c: Collect classification for module-level analysis (CA-12.04, CA-12.05)
        if (classification != null) {
            exercisesWithRecords++
            if (classification == ProgressionClassification.REGRESSION) regressionCount++
        }

        // HU-12 Step 5d: Plateau alert management (CA-12.09, CA-12.06)
        // Guarded by deload: IN_DELOAD guard in resolveNewProgressionState() prevents
        // status/counter changes during deload, so no plateau transitions occur.
        val previousStatus = currentProgression.status
        if (previousStatus != "IN_PLATEAU" && newStatus == "IN_PLATEAU") {
            if (!alertDao.existsActiveByExercise(exercise.exerciseId, "PLATEAU")) {
                alertDao.insert(
                    AlertEntity(
                        type = "PLATEAU",
                        level = "HIGH_ALERT",
                        exerciseId = exercise.exerciseId,
                        message = "3 sesiones sin progresión",
                        isActive = 1,
                        createdAt = today,
                    ),
                )
            }
        } else if (previousStatus == "IN_PLATEAU" && newStatus != "IN_PLATEAU") {
            alertDao.resolveByExerciseAndType(exercise.exerciseId, "PLATEAU", today)
        }

        exerciseProgressionDao.update(
            currentProgression.copy(
                status = newStatus,
                sessionsWithoutProgression = newCounter,
                prescribedLoadKg = prescribedLoadKg,
            ),
        )
    }

    // HU-12 Step 6: Module-level detection (post-loop)
    // DELOAD GUARD: Skip module-level detection during deload sessions (Decision 12)
    if (isDeloadSession) return

    val moduleCode = exercises.firstOrNull()?.moduleCode ?: return

    // CA-12.04, CA-12.05: Module fatigue detection
    val fatigueDetected = ModuleFatigueRule.detectFatigue(regressionCount, exercisesWithRecords)

    // CA-12.20, CA-12.21, CA-12.22: Deload need detection
    val totalExercises = planAssignmentDao.countExercisesForModuleVersion(moduleVersionId)
    val affectedCount = planAssignmentDao.countAffectedForDeload(moduleVersionId, sessionId)
    val deloadNeeded = DeloadNeedRule.needsDeload(affectedCount, totalExercises, fatigueDetected)

    if (deloadNeeded) {
        // CA-12.20, CA-12.21, CA-12.23, CA-12.24
        if (!alertDao.existsActiveByModule(moduleCode, "MODULE_REQUIRES_DELOAD")) {
            alertDao.insert(
                AlertEntity(
                    type = "MODULE_REQUIRES_DELOAD",
                    level = "HIGH_ALERT",
                    moduleCode = moduleCode,
                    message = "≥50% ejercicios en meseta/regresión",
                    isActive = 1,
                    createdAt = today,
                ),
            )
        }
    } else {
        alertDao.resolveByModuleAndType(moduleCode, "MODULE_REQUIRES_DELOAD", today)
    }
}
```

Cambio en `closeSession()` que invoca `evaluateProgression()`:

```kotlin
// Before (HU-10):
evaluateProgression(sessionId)

// After (HU-12):
val moduleVersionId = sessionDao.getModuleVersionIdBySessionId(sessionId)
val deloadId = sessionDao.getDeloadIdBySessionId(sessionId)
evaluateProgression(sessionId, moduleVersionId, isDeloadSession = deloadId != null)
```

#### Modificación #5 — `TensionDatabase` (versión + entity + DAO)

```kotlin
// entities: agregar AlertEntity::class
// version: 4 → 5
// agregar: abstract fun alertDao(): AlertDao
```

#### Modificación #6 — `DatabaseModule` (provider)

```kotlin
@Provides
fun provideAlertDao(database: TensionDatabase): AlertDao {
    return database.alertDao()
}
```

### Verificación Cruzada de CAs

| CA | Estado | Mecanismo | Implementado en |
|---|---|---|---|
| CA-12.01 | ✅ Ya cubierto | `ProgressionClassificationRule.classifyStandard()` → `REGRESSION` por reps | HU-10 |
| CA-12.02 | ✅ Ya cubierto | `ProgressionClassificationRule.classifyStandard()` → `REGRESSION` por RIR | HU-10 |
| CA-12.03 | ✅ Ya cubierto | `ProgressionClassificationRule.classifyStandard()` → `REGRESSION` por carga | HU-10 |
| CA-12.04 | 🔨 Por implementar | `ModuleFatigueRule.detectFatigue()` — acumuladores post-loop | HU-12 (Rule + Repository) |
| CA-12.05 | 🔨 Por implementar | Denominador = `exercisesWithRecords` (no prescritos) | HU-12 (Rule) |
| CA-12.06 | 🔨 Por implementar | `alertDao.insert()` — alerta PLATEAU o MODULE_REQUIRES_DELOAD | HU-12 (Repository) |
| CA-12.07 | ✅ Ya cubierto | `resolveNewProgressionState()` → `counter >= 3 → IN_PLATEAU` | HU-10 |
| CA-12.08 | ✅ Ya cubierto | Diseño inherente — `sessionsWithoutProgression` del ejercicio | HU-10 |
| CA-12.09 | 🔨 Por implementar | Insert alerta PLATEAU al transicionar a `IN_PLATEAU` | HU-12 (Repository) |
| CA-12.10 | 🔨 Por implementar | `PlateauCausalAnalysisRule.analyze()` → `LOW_RIR_LIMIT` | HU-12 (Rule) |
| CA-12.11 | 🔨 Por implementar | `PlateauCausalAnalysisRule.analyze()` → `HIGH_RIR_CONSERVATIVE` | HU-12 (Rule) |
| CA-12.12 | 🔨 Por implementar | `PlateauCausalAnalysisRule.analyze()` → `GROUP_STAGNATION` | HU-12 (Rule) |
| CA-12.13 | 🔨 Parcial HU-12 + HU-17 | `alert.level = "HIGH_ALERT"` (HU-12) + renderizado visual (HU-17) | HU-12 (Rule) + HU-17 (UI) |
| CA-12.14 | ✅ Ya cubierto | `resolveNewProgressionState()` → `status = IN_PLATEAU` | HU-10 |
| CA-12.15 | 🔨 Por implementar | `CorrectiveActionRule.recommend(4+)` → `MICRO_INCREMENT_OR_EXTEND_REPS` | HU-12 (Rule) |
| CA-12.16 | 🔨 Por implementar | `CorrectiveActionRule.recommend(6+)` → `ROTATE_VERSION` | HU-12 (Rule) |
| CA-12.17 | 🔨 Por implementar | Retorno acumulativo de `recommend()` — ambas acciones si ≥6 | HU-12 (Rule) |
| CA-12.18 | ✅ Ya cubierto | Diseño inherente — alertas y recomendaciones no alteran flujos | — |
| CA-12.19 | 🔨 Parcial HU-12 + HU-17 | Reglas proveen datos; H2 los presenta juntos | HU-12 (Rules) + HU-17 (UI) |
| CA-12.20 | 🔨 Por implementar | `DeloadNeedRule.needsDeload()` — umbral ≥50% meseta/regresión | HU-12 (Rule + Repository) |
| CA-12.21 | 🔨 Por implementar | `DeloadNeedRule.needsDeload(fatigueDetected = true)` → `true` | HU-12 (Rule) |
| CA-12.22 | 🔨 Por implementar | `countExercisesForModuleVersion()` — denominador sobre prescritos | HU-12 (DAO) |
| CA-12.23 | 🔨 Por implementar | `alert.type = "MODULE_REQUIRES_DELOAD"` — señal informativa | HU-12 (Repository) |
| CA-12.24 | 🔨 Por implementar | `alertDao.insert()` con `moduleCode`, `createdAt` | HU-12 (Repository) |

### Hitos de Implementación

| # | Entregable | Archivos | CAs |
|---|---|---|---|
| 1 | Infraestructura de alertas — `AlertEntity` + `AlertDao` + migración DB v4→v5 + DI | `AlertEntity.kt`, `AlertDao.kt`, `TensionDatabase.kt`, `DatabaseModule.kt` | CA-12.06, CA-12.24 |
| 2 | Reglas write-time — `ModuleFatigueRule` + `DeloadNeedRule` + tests unitarios | `ModuleFatigueRule.kt`, `DeloadNeedRule.kt`, `ModuleFatigueRuleTest.kt`, `DeloadNeedRuleTest.kt` | CA-12.04, CA-12.05, CA-12.20, CA-12.21, CA-12.22 |
| 3 | Reglas read-time — `PlateauCausalAnalysisRule` + `CorrectiveActionRule` + enums + tests | `PlateauCausalAnalysisRule.kt`, `CorrectiveActionRule.kt`, `PlateauCause.kt`, `CorrectiveAction.kt`, `PlateauCausalAnalysisRuleTest.kt`, `CorrectiveActionRuleTest.kt` | CA-12.10, CA-12.11, CA-12.12, CA-12.15, CA-12.16, CA-12.17 |
| 4 | Queries de agregación — `SessionDao`, `PlanAssignmentDao` | `SessionDao.kt`, `PlanAssignmentDao.kt` | CA-12.22, Decisión 12 (deload guard) |
| 5 | Integración — extender `evaluateProgression()` con alertas per-ejercicio + detección módulo-nivel | `SessionRepositoryImpl.kt` | CA-12.09, CA-12.13, CA-12.19, CA-12.23 |

### Validación de Impacto

**Código real verificado:**

- `ProgressionClassificationRule.kt`: `classify()` ya implementa CA-12.01/02/03 mediante `classifyStandard()`. `resolveNewProgressionState()` ya implementa CA-12.07 y CA-12.14 con `PLATEAU_THRESHOLD = 3`.
- `DoubleThresholdRule.kt`: No afectado por HU-12. Funciona independientemente.
- `ExerciseProgressionEntity.kt`: Campo `sessionsWithoutProgression` ya existe y se mantiene por HU-10.
- `SessionRepositoryImpl.evaluateProgression()`: El loop actual genera `classification`, `newStatus` y `newCounter` — exactamente los datos que HU-12 consume.
- `SessionExerciseDao.getSessionExercisesForProgression()`: Devuelve `SessionExerciseForProgression` con `moduleCode`, `isBodyweight`, `isIsometric`. Suficiente para el loop HU-12.
- `SessionDao`: `ActiveSessionInfo` NO tiene `moduleVersionId`. Se agrega `getModuleVersionIdBySessionId()` para obtenerlo.
- `TensionDatabase`: Versión actual = 4. `fallbackToDestructiveMigration()` configurado. No existe `AlertEntity` ni `alertDao()`.
- `DatabaseModule`: No tiene `provideAlertDao()`.
- `PlanAssignmentDao`: No tiene queries de conteo por module_version.

**Impacto en performance:**

- 4 reglas puras: operaciones O(1) o O(n) con n ≤ 11 ejercicios. Despreciable.
- 2 INSERTs condicionales + 2 UPDATEs condicionales en `alert`: máximo 2 alertas por sesión.
- `countAffectedForDeload`: JOIN triple sobre máximo 11 filas. Despreciable.
- Todo dentro de `database.withTransaction {}`: atomicidad garantizada.

### Notas Técnicas

**Nota 1 — CA-12.06 (registro de detecciones) se resuelve por la tabla `alert`.**

Los CAs de registro (CA-12.06, CA-12.24) son cubiertos por la inserción de alertas en la tabla `alert`. La alerta persiste la detección vinculada a la sesión (fecha de creación), al ejercicio o módulo afectado, y queda disponible para consulta futura. No se requiere una tabla de "detecciones" separada.

**Nota 2 — CA-12.09 y CA-12.13 (señales visuales) son responsabilidad de HU-17 (H1/H2).**

HU-12 produce los datos (alertas en tabla `alert`); HU-17 los consume y aplica las señales visuales diferenciadas (colores, iconografía, RNF05). La alerta `PLATEAU` tiene `level = "HIGH_ALERT"`, que H1 renderizará con el estilo visual correspondiente.

**Nota 3 — CA-12.19 (vinculación alerta + recomendación) se resuelve en H2.**

La pantalla H2 (Detalle de Alerta) de HU-17 leerá la alerta PLATEAU, computará el análisis causal via `PlateauCausalAnalysisRule` y las acciones correctivas via `CorrectiveActionRule`, y los presentará juntos.

**Nota 4 — Resolución automática de `MODULE_REQUIRES_DELOAD` tiene dos vías.**

Vía 1 (HU-12): Si en una sesión posterior el módulo ya no cumple el umbral ≥50%, la alerta se resuelve automáticamente en `evaluateProgression()`. Vía 2 (HU-14): Cuando el ejecutante activa el protocolo de descarga, HU-14 resolverá explícitamente la alerta como parte del flujo de activación. Ambas vías son complementarias.

**Nota 5 — La detección de estancamiento grupal (CA-12.12) requiere query de `exercise_muscle_zone`.**

Para determinar si hay estancamiento grupal, el consumidor read-time (H2 en HU-17) necesitará: (a) obtener las zonas musculares del ejercicio en meseta, (b) consultar si otros ejercicios del mismo `muscle_group` también están en `IN_PLATEAU` o `REGRESSION`. Esta query la implementará el Repository que sirve H2. La regla `PlateauCausalAnalysisRule` recibe el resultado como boolean `isGroupStagnant`.

**Nota 6 — Señales visuales diferenciadas (CA-12.13, RNF05) y orden de prioridad.**

`alert.level` distingue severidades: `CRISIS` > `HIGH_ALERT` > `MEDIUM_ALERT`. Las alertas de HU-12 son todas `HIGH_ALERT`. La diferenciación por color/icono según `level` es responsabilidad de HU-17 (H1/H2). HU-12 solo garantiza que el campo `level` esté correctamente asignado.

**Nota 7 — La alerta de fatiga acumulada del módulo (CA-12.04) no tiene tipo propio.**

La fatiga acumulada del módulo se detecta (CA-12.04) y se usa como input para `DeloadNeedRule` (CA-12.21). Si hay fatiga → automáticamente se marca `MODULE_REQUIRES_DELOAD`. No se crea una alerta separada de tipo "FATIGA" porque el Modelo de Datos §3.16 no define ese tipo, y funcionalmente es un escalamiento directo a "requiere descarga".

**Nota 8 — Guardia de descarga: sesiones de deload no generan alertas de módulo.**

Durante la descarga, las cargas se reducen al 60% (MDS R5), lo que produce regresiones naturales en los datos crudos. Si no se guarda esta distinción, `ModuleFatigueRule` detectaría fatiga falsa y `DeloadNeedRule` emitiría `MODULE_REQUIRES_DELOAD` durante una descarga activa — resultado paradójico. La guardia se implementa con `session.deload_id != null` para omitir el paso 6.

**Nota 9 — Los nombres de reglas que HU-10 anticipó no aplican en la estructura consolidada.**

HU-10 Nota Técnica 2 anticipó `RegressionDetectionRule` y `PlateauDetectionRule` como futuros archivos en `domain/rules/`. En la estructura consolidada de HU-12, la detección de regresión ya está resuelta por `ProgressionClassificationRule.classify()` (HU-10), y el análisis de meseta se divide en `PlateauCausalAnalysisRule` y `CorrectiveActionRule`. No se crean reglas con los nombres originalmente anticipados.

**Nota 10 — RF29 define regresión per-serie pero la implementación usa criterio agregado.**

RF29 establece: "las repeticiones caen en ≥ 2 de las 4 series". HU-10 implementa un criterio agregado (`totalReps < previous → REGRESSION`). CA-12.01 hereda este criterio por reutilización. Discrepancia aceptable para MVP: si totalReps baja, al menos 1 serie bajó; el edge case donde series individuales cambian pero el total se mantiene es atípico en entrenamiento real.

### Referencias y Validación

**Documentación consultada:**

- Manifiesto de Dominio Sistémico §6-A — Reglas R3 (Detección de Meseta), R4 (Regresión y Fatiga Acumulada)
- Modelo de Datos §3.13 (`exercise_progression`), §3.11 (`session_exercise`), §3.16 (`alert`), §3.15 (`deload`)
- ADR-06 — Motor de reglas Kotlin puro en `domain/rules/`
- Arquitectura Técnica §5.2 — Naming `{Nombre}Rule`
- Wireframes E5, B1, H1, H2
- Mapa de Navegación §4 — HU-12 outputs en E5/H1/H2
- Requerimientos — RF29, RF30, RF34, RF35, RF36, RF37, RNF05

### Auditoría Post-Análisis

**Fecha:** 2026-02-16

**Hallazgos y correcciones aplicadas:**

| # | Severidad | Hallazgo | Corrección |
|---|---|---|---|
| 1 | CRÍTICO | Sesiones de descarga (`session.deload_id != null`) producirían falsos positivos en `ModuleFatigueRule`/`DeloadNeedRule` porque las cargas al 60% generan regresiones esperadas | Agregada Decisión 12 (guardia de descarga), `isDeloadSession` como parámetro de `evaluateProgression()`, `getDeloadIdBySessionId()` en `SessionDao`, `if (isDeloadSession) return` antes del paso 6 |
| 2 | MEDIO | CA-13.05 (E5 "Considerar descarga") no tiene ruta de derivación documentada desde HU-12 | Agregada Decisión 13 con la ruta de derivación: `REGRESSION` + alerta activa `MODULE_REQUIRES_DELOAD`. Responsabilidad de implementación de HU-13 |
| 3 | BAJO | HU-10 anticipó nombres `RegressionDetectionRule` y `PlateauDetectionRule` que no se crearon | Agregada Nota 9 documentando la divergencia y justificación |
| 4 | BAJO | RF29 define regresión per-serie pero HU-10/HU-12 usan criterio agregado | Agregada Nota 10 documentando la discrepancia aceptable para MVP |
| 5 | VERIFICADO | Modelo de Datos §3.16 `alert.level` CHECK values (`CRISIS`, `HIGH_ALERT`, `MEDIUM_ALERT`) alineados | Sin corrección — ya correcto |
| 6 | VERIFICADO | ADR-06 naming `{Nombre}Rule` alineado con las 4 reglas propuestas | Sin corrección — ya correcto |
| 7 | VERIFICADO | `SessionEntity.deloadId` nullable ya existe en el código — disponible para guardia | Sin corrección — dato confirmado |
| 8 | VERIFICADO | `exercise_progression.sessions_without_progression` no tiene tope compatible con `CorrectiveActionRule` | Sin corrección — ya correcto |

---

## §6 Refinamiento Técnico

### Consideraciones Generales

**Basado en análisis arquitectónico:**
Análisis Arquitectónico de HU-12 con 13 decisiones de diseño, 5 hitos de implementación, 13 componentes (8 nuevos + 5 modificados). Hallazgos principales: (1) HU-12 es lógica pura de backend — no tiene pantalla propia (E5/H1/H2 consumen datos via HU-13/HU-17). (2) 7 de los 24 CAs ya están cubiertos por HU-10 — HU-12 consume sin reimplementar. (3) 4 reglas puras nuevas separadas en write-time (`ModuleFatigueRule`, `DeloadNeedRule`) y read-time (`PlateauCausalAnalysisRule`, `CorrectiveActionRule`). (4) Establece infraestructura de alertas (`AlertEntity` + `AlertDao`) que HU-17 reutilizará — solo escribe 2 de 7 tipos (`PLATEAU`, `MODULE_REQUIRES_DELOAD`). (5) Evaluación post-loop a nivel de módulo: fatiga acumulada + necesidad de descarga. (6) Guardia de descarga: `session.deload_id != null` omite paso 6 para evitar falsos positivos (Decisión 12). (7) Denominadores diferentes: fatiga → ejercicios con registros; descarga → prescritos para la versión. (8) Deduplicación + resolución automática de alertas. (9) `moduleVersionId` se obtiene de `session.module_version_id`. (10) Mensajes de alerta genéricos; nombres se resuelven por JOIN al leer. (11) Análisis causal y acciones correctivas son read-time — no se persisten. (12) La cumulatividad de acciones correctivas (sesión 6 = ambas) es decisión de diseño de CA-12.17. (13) La fatiga acumulada del módulo (CA-12.04) escala directamente a `MODULE_REQUIRES_DELOAD` — no tiene tipo de alerta propio.

**Nivel de complejidad:**
MEDIA-ALTA — HU-12 toca 13 archivos (8 nuevos + 5 modificados) con lógica distribuida en 5 bloques funcionales. Las 4 reglas puras son individualmente simples (O(1)/O(n)), pero la integración en `evaluateProgression()` es compleja: acumuladores intra-loop + evaluación post-loop + deduplicación/resolución de alertas + guardia de descarga. La migración de BD (v4→v5) agrega una nueva tabla con 4 índices y 2 FKs. El total de tests unitarios estimados es ~26 escenarios. No hay componentes UI.

**Riesgos técnicos conocidos:**

1. **Migración destructiva (v4→v5):** `TensionDatabase` usa `fallbackToDestructiveMigration()`. Al subir la versión de 4 a 5 con `AlertEntity`, Room destruirá la BD existente y la recreará. Aceptable para MVP pre-producción — no hay datos de usuario reales. En producción se necesitará migración incremental.
2. **`countAffectedForDeload` query con triple LEFT JOIN:** La query combina `plan_assignment + exercise_progression + session_exercise` para contar ejercicios afectados. Con máximo 11 ejercicios por versión, el costo es despreciable. Sin embargo, la lógica es sutilmente compleja — un ejercicio cuenta como "afectado" si está `IN_PLATEAU` en `exercise_progression` OR si tiene `REGRESSION` en `session_exercise` de esta sesión.
3. **Race condition teórica: resolución de alerta vs creación simultánea.** Si `evaluateProgression()` resuelve una alerta MODULE_REQUIRES_DELOAD y en la misma transacción la recrearía, la lógica secuencial del código evita esto: primero evalúa, luego actúa condicionalmente. La guardia `existsActiveByModule` previene duplicados.
4. **Guardia de descarga pre-HU-14:** `session.deload_id` es siempre `null` hasta que HU-14 cree DeloadEntity. La guardia `isDeloadSession = deloadId != null` es inerte (siempre false) hasta HU-14 — diseño forward-compatible.

**Patrones y convenciones del equipo (establecidos en HU-01—HU-11):**

- Código fuente en inglés, UI y datos de dominio en español (Arquitectura Técnica §5.1)
- Naming: `{Nombre}Rule` para reglas del motor (§5.2) — `ModuleFatigueRule`, `DeloadNeedRule`, `PlateauCausalAnalysisRule`, `CorrectiveActionRule`
- `object` singleton para reglas puras (patrón `ProgressionClassificationRule`, `DoubleThresholdRule`)
- `@Entity` con `@ForeignKey`, `@Index` — patrón `SessionEntity`, `ExerciseProgressionEntity`
- `@Dao` interface con `@Insert`, `@Query`, `@Update` — patrón `SessionDao`, `ExerciseProgressionDao`
- `exerciseProgressionDao.update(currentProgression.copy(...))` — extend el `copy()` (HU-10/HU-11)
- `database.withTransaction {}` para atomicidad — patrón `closeSession()` (HU-09)
- Constantes como `const val` dentro del `object` (patrón `PLATEAU_THRESHOLD = 3`)
- Tests: JUnit 4, helper factories `sessionData(vararg sets)` + `set(w, r, rir)`, sin mocks
- `fallbackToDestructiveMigration()` para migración — patrón actual en `DatabaseModule`
- Hilt `@Provides` para nuevos DAOs — patrón `provideExerciseSetDao()` en `DatabaseModule`

**Dependencias nuevas a instalar:**
Ninguna.

**Estrategia de testing:**
JUnit 4 (sin MockK — funciones puras) | 4 archivos de test: `ModuleFatigueRuleTest` (~5 escenarios), `DeloadNeedRuleTest` (~7 escenarios), `PlateauCausalAnalysisRuleTest` (~7 escenarios), `CorrectiveActionRuleTest` (~7 escenarios). Total: ~26 escenarios. Cubren umbrales de 50%, combinaciones de fatiga+meseta+regresión, análisis causal por RIR, y escalonamiento de acciones correctivas a sesiones 4/6.

### Historias Relacionadas Consultadas

**Implementaciones similares analizadas:**

- HU-10 (ProgressionClassificationRule) — Implementó `evaluateProgression()` con loop per-ejercicio, `resolveNewProgressionState()`, estados `IN_PLATEAU`/`IN_PROGRESSION`/`IN_DELOAD`, y contador `sessionsWithoutProgression`. HU-12 consume TODO esto: el `classification` del loop alimenta los acumuladores de regresión; `newStatus` informa las transiciones de meseta para alertas; el contador alimenta `CorrectiveActionRule`.
- HU-11 (DoubleThresholdRule) — Implementó prescripción de carga en el mismo loop (`evaluateProgression()`, paso 5b). HU-12 se integra DESPUÉS de HU-11: el paso 5c (acumuladores) y 5d (alertas de meseta) van entre la prescripción de carga (HU-11) y el `exerciseProgressionDao.update()`.
- HU-09 (closeSession) — Definió la transacción `closeSession()` donde se invoca `evaluateProgression()`. HU-12 modifica la firma de `evaluateProgression()` agregando `moduleVersionId` y `isDeloadSession`, y extiende la invocación en `closeSession()` con queries previas a `SessionDao`.
- HU-05 (alert badge placeholder) — Implementó `alertCount = 0` y `onNavigateToAlerts = { /* TODO */ }` proactivamente. HU-17 los conectará con `AlertDao.countActive()`.

**Patrones de código reutilizados:**

- `object` singleton con funciones puras → 4 nuevas reglas
- `@Entity` + `@Dao` + `TensionDatabase` + `DatabaseModule` → AlertEntity/AlertDao
- Extension del loop `evaluateProgression()` → acumuladores + alertas
- `database.withTransaction {}` → atomicidad ya garantizada por `closeSession()`
- `LocalDate.now().toString()` → patrón para `createdAt`/`resolvedAt` en alertas

**Mejores prácticas aplicadas:**

- Write-time vs read-time: las detecciones se persisten como alertas; el análisis causal y recomendaciones se computan on-demand desde datos inmutables
- Deduplicación por `existsActiveByExercise/Module` antes de insertar — previene alertas duplicadas
- Resolución automática bidireccional: salir de `IN_PLATEAU` → resolver alerta PLATEAU; módulo no cumple umbral → resolver alerta `MODULE_REQUIRES_DELOAD`
- Guardia de descarga omite evaluación módulo-nivel durante deload — evita falsos positivos

---

## §7 Tareas de Implementación

### Fase 1: Domain — Reglas puras write-time + tests

#### Domain Rules (write-time)

- [ ] **Crear `ModuleFatigueRule`** (AC: 4, 5)
  - [ ] Crear archivo: `domain/rules/ModuleFatigueRule.kt`
  - [ ] Constante: `FATIGUE_THRESHOLD = 0.50`
  - [ ] `fun detectFatigue(regressionCount: Int, exercisesWithRecords: Int): Boolean` — guard `exercisesWithRecords == 0` → false, luego `regressionCount / exercisesWithRecords >= 0.50`
- [ ] **Test unitario `ModuleFatigueRuleTest`** (~5 escenarios)
  - [ ] Crear archivo: `src/test/java/com/estebancoloradogonzalez/tension/domain/rules/ModuleFatigueRuleTest.kt`
  - [ ] 0 ejercicios con registros → `false` (guard)
  - [ ] 5 regresiones de 10 ejercicios → `true` (exactamente 50%, CA-12.04)
  - [ ] 4 regresiones de 10 ejercicios → `false` (40%, bajo umbral)
  - [ ] 6 regresiones de 10 ejercicios → `true` (60%, CA-12.04)
  - [ ] 1 regresión de 1 ejercicio → `true` (100%, sesión incompleta CA-12.05)
- [ ] **Crear `DeloadNeedRule`** (AC: 20, 21, 22)
  - [ ] Crear archivo: `domain/rules/DeloadNeedRule.kt`
  - [ ] Constante: `DELOAD_THRESHOLD = 0.50`
  - [ ] `fun needsDeload(affectedCount: Int, totalCount: Int, fatigueDetected: Boolean): Boolean` — si `fatigueDetected` → `true` (CA-12.21); guard `totalCount == 0` → false; luego `affectedCount / totalCount >= 0.50` (CA-12.20)
- [ ] **Test unitario `DeloadNeedRuleTest`** (~7 escenarios)
  - [ ] Crear archivo: `src/test/java/com/estebancoloradogonzalez/tension/domain/rules/DeloadNeedRuleTest.kt`
  - [ ] fatiga detectada, 0 afectados → `true` (CA-12.21 — fatiga prevalece)
  - [ ] fatiga detectada, 100% afectados → `true` (ambos criterios)
  - [ ] sin fatiga, 6 de 11 afectados → `true` (54.5% ≥ 50%, CA-12.20)
  - [ ] sin fatiga, 5 de 11 afectados → `false` (45.5% < 50%)
  - [ ] sin fatiga, 5 de 9 afectados (módulo C) → `true` (55.6% ≥ 50%, CA-12.22)
  - [ ] sin fatiga, 0 total → `false` (guard)
  - [ ] boundary: sin fatiga, 5 de 10 → `true` (exactamente 50%)

### Fase 2: Domain — Reglas puras read-time + enums + tests

#### Domain Models (enums)

- [ ] **Crear `PlateauCause`** (AC: 10, 11, 12)
  - [ ] Crear archivo: `domain/model/PlateauCause.kt`
  - [ ] Enum values: `LOW_RIR_LIMIT`, `HIGH_RIR_CONSERVATIVE`, `GROUP_STAGNATION`, `MIXED`
- [ ] **Crear `CorrectiveAction`** (AC: 15, 16)
  - [ ] Crear archivo: `domain/model/CorrectiveAction.kt`
  - [ ] Enum values: `MICRO_INCREMENT_OR_EXTEND_REPS`, `ROTATE_VERSION`

#### Domain Rules (read-time)

- [ ] **Crear `PlateauCausalAnalysisRule`** (AC: 10, 11, 12)
  - [ ] Crear archivo: `domain/rules/PlateauCausalAnalysisRule.kt`
  - [ ] Constantes: `LOW_RIR_THRESHOLD = 1.0`, `HIGH_RIR_THRESHOLD = 3.0`
  - [ ] `fun analyze(lastSessionsAvgRir: List<Double>, isGroupStagnant: Boolean): PlateauCause` — si `isGroupStagnant` → GROUP_STAGNATION (CA-12.12); guard lista vacía → MIXED; promedio ≤ 1.0 → LOW_RIR_LIMIT (CA-12.10); promedio ≥ 3.0 → HIGH_RIR_CONSERVATIVE (CA-12.11); else → MIXED
- [ ] **Test unitario `PlateauCausalAnalysisRuleTest`** (~7 escenarios)
  - [ ] Crear archivo: `src/test/java/com/estebancoloradogonzalez/tension/domain/rules/PlateauCausalAnalysisRuleTest.kt`
  - [ ] avgRir [0.5, 0.8, 1.0] → `LOW_RIR_LIMIT` (CA-12.10 — promedio 0.77)
  - [ ] avgRir [3.5, 4.0, 3.0] → `HIGH_RIR_CONSERVATIVE` (CA-12.11 — promedio 3.5)
  - [ ] avgRir [1.5, 2.0, 2.5] → `MIXED` (promedio 2.0, entre umbrales)
  - [ ] isGroupStagnant = true, cualquier RIR → `GROUP_STAGNATION` (CA-12.12 — prioridad)
  - [ ] lista vacía → `MIXED` (guard)
  - [ ] boundary: avgRir [1.0, 1.0, 1.0] → `LOW_RIR_LIMIT` (exactamente 1.0)
  - [ ] boundary: avgRir [3.0, 3.0, 3.0] → `HIGH_RIR_CONSERVATIVE` (exactamente 3.0)
- [ ] **Crear `CorrectiveActionRule`** (AC: 15, 16, 17)
  - [ ] Crear archivo: `domain/rules/CorrectiveActionRule.kt`
  - [ ] Constantes: `MICRO_INCREMENT_THRESHOLD = 4`, `ROTATE_VERSION_THRESHOLD = 6`
  - [ ] `fun recommend(sessionsWithoutProgression: Int): List<CorrectiveAction>` — < 4 → vacía; ≥ 4 → `[MICRO_INCREMENT_OR_EXTEND_REPS]` (CA-12.15); ≥ 6 → `[MICRO_INCREMENT_OR_EXTEND_REPS, ROTATE_VERSION]` (CA-12.16, CA-12.17 acumulativo)
- [ ] **Test unitario `CorrectiveActionRuleTest`** (~7 escenarios)
  - [ ] Crear archivo: `src/test/java/com/estebancoloradogonzalez/tension/domain/rules/CorrectiveActionRuleTest.kt`
  - [ ] counter 0 → `[]` (sin recomendaciones)
  - [ ] counter 3 → `[]` (justo debajo del umbral, meseta recién detectada)
  - [ ] counter 4 → `[MICRO_INCREMENT_OR_EXTEND_REPS]` (CA-12.15 — 1ª intervención)
  - [ ] counter 5 → `[MICRO_INCREMENT_OR_EXTEND_REPS]` (mantiene 1ª sin 2ª)
  - [ ] counter 6 → `[MICRO_INCREMENT_OR_EXTEND_REPS, ROTATE_VERSION]` (CA-12.16/17 — acumulativo)
  - [ ] counter 10 → `[MICRO_INCREMENT_OR_EXTEND_REPS, ROTATE_VERSION]` (se mantiene en ≥ 6)
  - [ ] counter 1 → `[]` (progresión sin meseta)

### Fase 3: Data — Infraestructura de alertas (AlertEntity + AlertDao + migración)

#### Data Layer (entity + DAO)

- [ ] **Crear `AlertEntity`** (AC: 6, 24)
  - [ ] Crear archivo: `data/local/entity/AlertEntity.kt`
  - [ ] `@Entity(tableName = "alert")` con 10 campos: `id` (PK autoGenerate), `type` (String), `level` (String), `exerciseId` (Long?), `moduleCode` (String?), `muscleGroup` (String?), `message` (String), `isActive` (Int, default 1), `createdAt` (String), `resolvedAt` (String?)
  - [ ] ForeignKeys: `exerciseId → exercise(id) RESTRICT`, `moduleCode → module(code) RESTRICT`
  - [ ] Indices: `is_active`, `type`, `exercise_id`, `module_code`
- [ ] **Crear `AlertDao`** (AC: 6, 9, 24)
  - [ ] Crear archivo: `data/local/dao/AlertDao.kt`
  - [ ] `insert(alert: AlertEntity): Long`
  - [ ] `existsActiveByExercise(exerciseId: Long, type: String): Boolean` — SELECT EXISTS WHERE is_active = 1
  - [ ] `existsActiveByModule(moduleCode: String, type: String): Boolean` — SELECT EXISTS WHERE is_active = 1
  - [ ] `resolveByExerciseAndType(exerciseId: Long, type: String, resolvedAt: String)` — UPDATE is_active = 0
  - [ ] `resolveByModuleAndType(moduleCode: String, type: String, resolvedAt: String)` — UPDATE is_active = 0
  - [ ] `countActive(): Flow<Int>` — SELECT COUNT WHERE is_active = 1
  - [ ] `getActiveAlerts(): Flow<List<AlertEntity>>` — SELECT WHERE is_active = 1 ORDER BY level, created_at
- [ ] **Modificar `TensionDatabase`** (AC: 6, 24)
  - [ ] Agregar `AlertEntity::class` al array de entities
  - [ ] Subir versión de 4 a 5
  - [ ] Agregar `abstract fun alertDao(): AlertDao`
  - [ ] Agregar import de `AlertEntity`
- [ ] **Modificar `DatabaseModule`** (AC: 6, 24)
  - [ ] Agregar import de `AlertDao`
  - [ ] Agregar `@Provides fun provideAlertDao(database: TensionDatabase): AlertDao`

### Fase 4: Data — Queries de agregación

#### Data Layer (queries nuevas)

- [ ] **Modificar `SessionDao`** (AC: 20, Decisión 12)
  - [ ] Agregar query `getModuleVersionIdBySessionId(sessionId: Long): Long` — `SELECT module_version_id FROM session WHERE id = :sessionId`
  - [ ] Agregar query `getDeloadIdBySessionId(sessionId: Long): Long?` — `SELECT deload_id FROM session WHERE id = :sessionId`
- [ ] **Modificar `PlanAssignmentDao`** (AC: 22)
  - [ ] Agregar query `countExercisesForModuleVersion(moduleVersionId: Long): Int` — `SELECT COUNT(*) FROM plan_assignment WHERE module_version_id = :moduleVersionId`
  - [ ] Agregar query `countAffectedForDeload(moduleVersionId: Long, sessionId: Long): Int` — triple LEFT JOIN: `plan_assignment + exercise_progression + session_exercise`, cuenta DISTINCT exercise_id donde `ep.status = 'IN_PLATEAU' OR se.progression_classification = 'REGRESSION'`

### Fase 5: Data — Integración en evaluateProgression()

#### Repository

- [ ] **Extender `evaluateProgression()` en `SessionRepositoryImpl`** (AC: 4, 5, 6, 9, 20, 21, 23, 24)
  - [ ] Cambiar firma: `evaluateProgression(sessionId: Long)` → `evaluateProgression(sessionId: Long, moduleVersionId: Long, isDeloadSession: Boolean)`
  - [ ] Agregar imports: `ModuleFatigueRule`, `DeloadNeedRule`, `AlertEntity`, `ProgressionClassification`, `LocalDate`
  - [ ] Inyectar `AlertDao` en `SessionRepositoryImpl` (agregar a constructor — `PlanAssignmentDao` ya está inyectado desde HU-09)
  - [ ] Agregar `val today = LocalDate.now().toString()` al inicio
  - [ ] Agregar acumuladores antes del loop: `var regressionCount = 0`, `var exercisesWithRecords = 0`
  - [ ] Paso 5c (dentro del loop, después de HU-11 paso 5b): Si `classification != null` → `exercisesWithRecords++`; si `classification == REGRESSION` → `regressionCount++`
  - [ ] Paso 5d (dentro del loop, después de paso 5c, antes de `exerciseProgressionDao.update()`): Detectar transición a/desde IN_PLATEAU — si `previousStatus != "IN_PLATEAU" && newStatus == "IN_PLATEAU"` → `alertDao.insert(PLATEAU alert)` con guardia `existsActiveByExercise`; si `previousStatus == "IN_PLATEAU" && newStatus != "IN_PLATEAU"` → `alertDao.resolveByExerciseAndType()`
  - [ ] Paso 6 (post-loop, fuera del for): Guardia `if (isDeloadSession) return` (Decisión 12). Obtener `moduleCode` de `exercises.firstOrNull()?.moduleCode`. Detectar fatiga: `ModuleFatigueRule.detectFatigue()`. Evaluar necesidad de descarga: `DeloadNeedRule.needsDeload()` con `countExercisesForModuleVersion()` y `countAffectedForDeload()`. Si `deloadNeeded` → insertar alerta `MODULE_REQUIRES_DELOAD` (con guardia `existsActiveByModule`); else → `resolveByModuleAndType()`.
  - [ ] Modificar `closeSession()`: Agregar queries `getModuleVersionIdBySessionId()` y `getDeloadIdBySessionId()` antes de invocar `evaluateProgression()`. Pasar `moduleVersionId` y `isDeloadSession = deloadId != null` como argumentos.

### Fase N: QA y Deployment

- [ ] **Ejecutar Agente Peer Review** (MANUAL)
- [ ] **Resolver incidentes del Peer Review** (MANUAL, condicional)
- [ ] **Crear Pull Request** (MANUAL)
- [ ] **Ejecutar pipeline deployment DEV** (MANUAL)
- [ ] **Diseñar set de pruebas manuales** (MANUAL)
- [ ] **Ejecutar pruebas manuales** (MANUAL)

---

**Notas sobre vinculación con Criterios de Aceptación:**

**Bloque A — Regresión (CA-12.01 a CA-12.03):**
- CA-12.01 → ✅ Ya cubierto por HU-10 (`classifyStandard()` → `REGRESSION` por totalReps < previous). Nota: RF29 define criterio per-serie (≥2 series); HU-10 usa criterio agregado (totalReps). Discrepancia aceptable para MVP (Nota 10).
- CA-12.02 → ✅ Ya cubierto por HU-10 (`classifyStandard()` → `REGRESSION` por `rirRise >= 1.5`).
- CA-12.03 → ✅ Ya cubierto por HU-10 (`classifyStandard()` → `REGRESSION` por `isWeightLower`).

**Bloque B — Fatiga Acumulada (CA-12.04 a CA-12.06):**
- CA-12.04 → Fase 1 (`ModuleFatigueRule.detectFatigue()` — ≥50% regresiones en ejercicios con registros) + Fase 5 (acumuladores intra-loop)
- CA-12.05 → Fase 1 (`exercisesWithRecords` como denominador — solo cuenta ejercicios que tienen `classification != null`) + tests
- CA-12.06 → Fase 3 (`alertDao.insert()` — persiste detecciones como alertas) + Fase 5 (paso 5d + paso 6)

**Bloque C — Meseta (CA-12.07 a CA-12.14):**
- CA-12.07 → ✅ Ya cubierto por HU-10 (`PLATEAU_THRESHOLD = 3` en `resolveNewProgressionState()`).
- CA-12.08 → ✅ Ya cubierto por HU-10 (diseño inherente — `sessionsWithoutProgression` es per-exerciseId).
- CA-12.09 → Fase 5 paso 5d (insertar alerta PLATEAU al transicionar a `IN_PLATEAU`).
- CA-12.10 → Fase 2 (`PlateauCausalAnalysisRule.analyze()` → `LOW_RIR_LIMIT` cuando avgRir ≤ 1.0).
- CA-12.11 → Fase 2 (`PlateauCausalAnalysisRule.analyze()` → `HIGH_RIR_CONSERVATIVE` cuando avgRir ≥ 3.0).
- CA-12.12 → Fase 2 (`PlateauCausalAnalysisRule.analyze()` → `GROUP_STAGNATION` cuando `isGroupStagnant = true`).
- CA-12.13 → Parcial HU-12 + HU-17: HU-12 asigna `level = "HIGH_ALERT"` a alertas PLATEAU; HU-17 aplica diferenciación visual por level (colores/iconos, RNF05).
- CA-12.14 → ✅ Ya cubierto por HU-10 (`resolveNewProgressionState()` → status = `IN_PLATEAU`).

**Bloque D — Acciones Correctivas (CA-12.15 a CA-12.19):**
- CA-12.15 → Fase 2 (`CorrectiveActionRule.recommend(4+)` → `[MICRO_INCREMENT_OR_EXTEND_REPS]`).
- CA-12.16 → Fase 2 (`CorrectiveActionRule.recommend(6+)` → agrega `ROTATE_VERSION`).
- CA-12.17 → Fase 2 (retorno acumulativo — `recommend(6)` retorna ambas acciones como lista).
- CA-12.18 → ✅ Ya cubierto (diseño inherente — alertas son datos, no gates).
- CA-12.19 → Parcial HU-12 + HU-17: HU-12 provee la alerta PLATEAU + las reglas de análisis causal/acciones; HU-17 orquesta la presentación conjunta en H2.

**Bloque E — Necesidad de Descarga (CA-12.20 a CA-12.24):**
- CA-12.20 → Fase 1 (`DeloadNeedRule.needsDeload()` — umbral ≥50% meseta/regresión) + Fase 4 (`countAffectedForDeload` query) + Fase 5 paso 6.
- CA-12.21 → Fase 1 (`DeloadNeedRule.needsDeload(fatigueDetected = true)` → `true` automáticamente).
- CA-12.22 → Fase 4 (`countExercisesForModuleVersion()` — denominador sobre total prescritos para la versión).
- CA-12.23 → Fase 5 paso 6 (insertar alerta `MODULE_REQUIRES_DELOAD` — señal informativa, no bloqueante).
- CA-12.24 → Fase 3 + Fase 5 (`alertDao.insert()` con `moduleCode` + `createdAt` — persistida para consulta).

<!-- AUDITORÍA PROFUNDA v2 (2026-02-16):
Auditoría exhaustiva post-refinamiento: archivo por archivo contra toda la documentación.

=== SCOPE ===
6 docs arquitectura + 5 docs business + 18 HUs + Mapa de Historias + 14 archivos de código fuente verificados.

=== BLOQUE 1: DOCUMENTOS DE ARQUITECTURA (6 archivos) ===

Modelo de Datos §3.16 (alert):
- 10 columnas verificadas column-by-column vs AlertEntity: id, type, level, exercise_id, module_code, muscle_group, message, is_active, created_at, resolved_at ✅
- CHECK type IN (7 valores): PLATEAU, LOW_PROGRESSION_RATE, RIR_OUT_OF_RANGE, LOW_ADHERENCE, TONNAGE_DROP, MODULE_INACTIVITY, MODULE_REQUIRES_DELOAD ✅
- CHECK level IN (3 valores): CRISIS, HIGH_ALERT, MEDIUM_ALERT ✅
- FKs: exercise_id→exercise(id) RESTRICT ✅, module_code→module(code) RESTRICT ✅
- Indices (4): is_active, type, exercise_id, module_code ✅
- Detalle tipos: PLATEAU→exercise_id+HIGH_ALERT ✅, MODULE_REQUIRES_DELOAD→module_code+HIGH_ALERT ✅
- Room no soporta CHECK constraints declarativos — aceptable, validación por dominio ✅
- NO hay columna session_id en alert — por diseño: datos de disparo se recalculan en read-time ✅

Modelo de Datos §3.13 (exercise_progression):
- sessions_without_progression INTEGER NOT NULL DEFAULT 0, CHECK >= 0, sin MAX/cap ✅
- Status CHECK: NO_HISTORY, IN_PROGRESSION, IN_PLATEAU, IN_DELOAD, MASTERED (5 valores) ✅
- prescribed_load_kg REAL NULL ✅

Modelo de Datos §3.10 (session):
- deload_id INTEGER NULL FK→deload(id) ON DELETE RESTRICT ✅
- deload_id con índice ✅
- SessionEntity tiene columna+índice pero NO FK (DeloadEntity no existe aún — HU-14) ✅

Modelo de Datos §3.15 (deload): ACTIVE/COMPLETED, creado por HU-14 — no existe en código ✅
Modelo de Datos §3.11 (session_exercise): progression_classification TEXT NULL ✅

ADR-06: reglas puras en domain.rules — 4 nuevas reglas como object singleton ✅
ADR-18: JUnit 4, tests en src/test/, sin emulador para reglas ✅

Arquitectura Técnica:
- §5.1 idioma: código en inglés ✅
- §5.2 naming: {Nombre}Rule — se respeta ✅
- §3.2 paquetes: domain/rules/, domain/model/, data/local/entity/, data/local/dao/ — correctos ✅
- Nota: §5.2 ejemplo dice PlateauDetectionRule pero refinamiento crea PlateauCausalAnalysisRule — documentado en Nota 9 ✅

Especificación Visual:
- Señales progresión (RNF05): ↓ Regresión = #C62828 (light) / #EF9A9A (dark) ✅
- Estados progresión: En Meseta = 🟡 #8D6E00 / #FFD54F ✅
- Niveles alerta: Crisis 🔴, Alerta alta 🟠, Alerta media 🟡 — PLATEAU y MODULE_REQUIRES_DELOAD = HIGH_ALERT = 🟠 ✅
- Indicador Descarga: #1565C0 / #64B5F6 ✅

Mapa de Navegación:
- B1: badge alertas activas (HU-17 consume AlertDao.countActive()) ✅
- E5: "señales de acción...considerar descarga" — HU Indirectas: HU-12 ✅
- H1: "módulo requiere descarga (≥50% meseta/regresión)" — HU Indirectas: HU-12 ✅
- H2: "análisis causal (RIR bajo, RIR alto, estancamiento grupal) y recomendaciones escalonadas" — HU Indirectas: HU-12 ✅
- Flujo H: B1 → H1 → H2 → F3/I1 ✅

Wireframes:
- E5: "↓ Regresión" + "Considerar descarga" visibles en wireframe ✅
- B1: Badge "🔔 N" con conteo alertas activas ✅
- H1: Centro de Alertas — secciones Crisis 🔴 y Alertas 🟠🟡, tipos: 🟠 Meseta detectada, 🟠 Módulo requiere descarga ✅
- H2: Detalle de Alerta — trigger data, análisis causal, recomendaciones correctivas ✅
- ORDER BY level ASC: funciona por coincidencia alfabética (CRISIS < HIGH_ALERT < MEDIUM_ALERT) ✅

=== BLOQUE 2: DOCUMENTOS DE NEGOCIO (5 archivos) ===

Requerimientos:
- RF29: "≥ 2 de las 4 series" (per-serie). HU-10 usa agregado (totalReps). Discrepancia MVP documentada en Nota 10 ✅
- RF30: "≥ 50% de los ejercicios de una misma sesión" → ModuleFatigueRule usa exercisesWithRecords ✅
- RF34: "3 sesiones consecutivas del mismo ejercicio" — SIN restricción de versión ✅
- RF35: "RIR (0-1)" = ≤1.0 ✅, "RIR (3+)" = ≥3.0 ✅, estancamiento grupal ✅
- RF36: sesión 4 microincremento o extensión reps ✅, sesión 6 rotar versión ✅
- RF37: "≥ 50% meseta/regresión O fatiga acumulada (RF30)" — OR relación implementada ✅
- RNF05: señales visuales por colores e iconografía ✅

Manifiesto de Dominio Sistémico:
- R3 meseta: "3 sesiones consecutivas en la misma versión del módulo" — MDS dice "misma versión" PERO RF34 no lo dice. Decisión documentada en HU-10 Nota 9: seguir RF34 + Modelo de Datos (counter global per exercise). Deliberada, no omisión ✅
- R3 intervenciones: 1ª (sesión 4) = "verificar técnica y microincremento o extensión reps" — "verificar técnica" omitido por RF36, gap presentación-HU-17 (LOW) ✅
  2ª (sesión 6) = rotar versión ✅. 3ª (≥50% módulo) = descarga ✅
- R3 cumulatividad: MDS no dice explícitamente "acumulativo" — CA-12.17 interpreta así. Decisión de diseño razonable ✅
- R4 regresión: "≥ 2 series" (MDS) vs "≥ 2 de las 4 series" (RF29) — ambos resueltos por agregado ✅
- R4 fatiga: "≥ 50% de los ejercicios de una sesión" ✅
- §5 lifecycle states: Sin Historial, En Progresión, En Meseta, En Descarga. MASTERED solo en código (isométricos) ✅
- R5 protocolo descarga: carga 60%, no cambiar versión — refuerza guardia de descarga ✅

Visión del Producto: Detectar, meseta, fatiga, regresión, descarga, alertas, análisis causal — todo mencionado como core. Valida scope HU-12 ✅

Plan de Entrenamiento: A=11, B=11, C=9 por versión (9 versiones verificadas) — denominadores DeloadNeedRule ✅

=== BLOQUE 3: TRAZABILIDAD 24 CAs ===

Ya cubiertos por HU-10 (7):
- CA-12.01 → classifyStandard() totalReps < previous → REGRESSION ✅
- CA-12.02 → classifyStandard() rirRise >= 1.5 → REGRESSION ✅
- CA-12.03 → classifyStandard() isWeightLower → REGRESSION ✅
- CA-12.07 → resolveNewProgressionState() counter >= 3 → IN_PLATEAU ✅
- CA-12.08 → diseño inherente, sessionsWithoutProgression per exerciseId ✅
- CA-12.14 → resolveNewProgressionState() → status = IN_PLATEAU ✅
- CA-12.18 → diseño inherente, alertas informativas no bloquean ✅

Por implementar en HU-12 (15):
- CA-12.04 → Fase 1 ModuleFatigueRule + Fase 5 acumuladores ✅
- CA-12.05 → Fase 1 exercisesWithRecords denominator ✅
- CA-12.06 → Fase 3 AlertDao.insert + Fase 5 paso 5d/6 ✅
- CA-12.09 → Fase 5 paso 5d insert PLATEAU alert ✅
- CA-12.10 → Fase 2 PlateauCausalAnalysisRule → LOW_RIR_LIMIT ✅
- CA-12.11 → Fase 2 PlateauCausalAnalysisRule → HIGH_RIR_CONSERVATIVE ✅
- CA-12.12 → Fase 2 PlateauCausalAnalysisRule → GROUP_STAGNATION ✅
- CA-12.15 → Fase 2 CorrectiveActionRule counter ≥ 4 ✅
- CA-12.16 → Fase 2 CorrectiveActionRule counter ≥ 6 ✅
- CA-12.17 → Fase 2 retorno acumulativo ✅
- CA-12.20 → Fase 1 DeloadNeedRule + Fase 4 countAffectedForDeload + Fase 5 paso 6 ✅
- CA-12.21 → Fase 1 DeloadNeedRule fatigueDetected → true ✅
- CA-12.22 → Fase 4 countExercisesForModuleVersion ✅
- CA-12.23 → Fase 5 paso 6 insert MODULE_REQUIRES_DELOAD alert ✅
- CA-12.24 → Fase 3 AlertEntity + Fase 5 alertDao.insert con moduleCode + createdAt ✅

Parciales HU-12+HU-17 (2):
- CA-12.13 → HU-12 asigna level HIGH_ALERT; HU-17 aplica colores/iconos RNF05 ✅
- CA-12.19 → HU-12 provee PLATEAU alert + reglas causal/correctivas; HU-17 presenta conjuntamente en H2 ✅

Total: 7 + 15 + 2 = 24 CAs ✅

=== BLOQUE 4: ERRORES ENCONTRADOS Y CORREGIDOS ===

E1-CORREGIDO: Fase 5 decía "Inyectar AlertDao y PlanAssignmentDao en SessionRepositoryImpl". PlanAssignmentDao YA está inyectado (SessionRepositoryImpl.kt L40). Corregido a "Inyectar AlertDao en SessionRepositoryImpl (PlanAssignmentDao ya está inyectado desde HU-09)".

=== BLOQUE 5: CONTRADICCIONES DOCUMENTALES (4 analizadas) ===

C1-INFO (heredada, documentada): MDS R3 dice "en la misma versión del módulo" pero RF34 NO dice eso. HU-10 Nota 9 documenta explícitamente la divergencia: counter global per exercise, siguiendo RF34 + Modelo de Datos. Decisión deliberada, no omisión.
C2-LOW (heredada de HU-10, documentada): RF29 define regresión per-serie (≥2 de 4 series) pero HU-10 usa criterio agregado (totalReps < previous). Nota 10 documenta discrepancia aceptable para MVP.
C3-LOW (gap presentación): MDS R3 1ª intervención incluye "verificar la técnica de ejecución". RF36 y CA-12.15 omiten técnica. Gap es presentación-layer, addressable en texto H2 (HU-17) sin cambio de código.
C4-INFO (decisión de diseño): MDS R3 lista intervenciones como escalamiento sin decir "acumulativo". CA-12.17 interpreta sesión 6 = ambas acciones. Razonable.

=== CHECKLIST FORMAL (25 items) ===

1. 8 archivos nuevos marcados "Crear archivo" ✅
2. 5 archivos modificados existen en codebase ✅
3. 4 archivos de test con ~26 escenarios ✅
4. JUnit 4, helpers sessionData/set, sin mocks ✅
5. 13 decisiones arquitectónicas consumidas ✅
6. 5 fases alineadas con 5 hitos ✅
7. 24 CAs mapeados (7 cubiertos + 15 por implementar + 2 parciales) ✅
8. 4 riesgos técnicos documentados ✅
9. 0 dependencias nuevas ✅
10. Migración BD v4→v5, fallbackToDestructiveMigration ✅
11. DI: provideAlertDao() en DatabaseModule ✅
12. Guards: deload session guard post-loop ✅
13. Dedup: existsActiveByExercise/Module antes de insert ✅
14. Forward-compatible: deload guard inerte pre-HU-14 ✅
15. Alertas extensibles: solo 2 de 7 tipos, HU-17 agrega 5 ✅
16. Integer division: .toDouble() en ambas reglas de umbral ✅
17. SessionRepository interface: evaluateProgression es private, no cambia API ✅
18. RepositoryModule: no necesita cambios, Hilt resuelve AlertDao automáticamente ✅
19. PlanAssignmentDao: ya inyectado en SessionRepositoryImpl — solo AlertDao es nuevo (CORREGIDO) ✅
20. ProgressionClassification: import necesario en SessionRepositoryImpl (documentado) ✅
21. ModuleEntity PK = code: String — FK AlertEntity→module(code) coherente ✅
22. ExerciseEntity PK = id: Long — FK AlertEntity→exercise(id) coherente ✅
23. ORDER BY level ASC: CRISIS < HIGH_ALERT < MEDIUM_ALERT por orden alfabético — funcional ✅
24. countAffectedForDeload: LEFT JOIN + DISTINCT correctos ✅
25. Patrones JUnit verificados contra ProgressionClassificationRuleTest.kt + DoubleThresholdRuleTest.kt ✅

=== RESULTADO FINAL ===
1 ERROR CORREGIDO (E1: PlanAssignmentDao ya inyectado), 0 HIGH, 0 MEDIUM, 2 LOW (RF29 per-serie + técnica MDS R3), 2 INFO (version-scoping + cumulatividad).
Totales verificados: 6 docs arquitectura + 5 docs business + 18 HUs + Mapa + 14 archivos código + 24 CAs + ~26 escenarios test + 25 items checklist.
Refinamiento técnico APROBADO con 1 corrección aplicada. -->
