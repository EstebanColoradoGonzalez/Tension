# HU-09 — Cerrar sesión y avanzar rotación

## Requisitos relacionados

RF18, RF19, RF20, RF21, RNF10

## Descripción

Como ejecutante, quiero poder cerrar mi sesión de entrenamiento como Completada o Incompleta, que el sistema calcule automáticamente el tonelaje y avance la rotación cíclica, para que mis datos queden consolidados, la posición del plan se actualice correctamente y yo pueda continuar con confianza en la próxima sesión.

## Criterios de Aceptación

### CA-09.01 — Cierre como Completada

**Dado que** el ejecutante tiene una sesión activa y todas las series de todos los ejercicios de la sesión han sido registradas (todos los ejercicios en estado "Completado" con 4 series cada uno),
**cuando** selecciona la opción de cerrar sesión,
**entonces** el sistema cierra la sesión con estado "Completada" y la marca como inmutable.

### CA-09.02 — Cierre como Incompleta

**Dado que** el ejecutante tiene una sesión activa pero no ha completado todas las series de todos los ejercicios,
**cuando** selecciona la opción de cerrar sesión anticipadamente,
**entonces** el sistema cierra la sesión con estado "Incompleta", conservando todos los datos parciales registrados hasta ese momento sin descartar ninguna serie.

### CA-09.03 — Confirmación antes de cierre incompleto

**Dado que** el ejecutante tiene una sesión activa con ejercicios sin completar,
**cuando** intenta cerrar la sesión,
**entonces** el sistema solicita confirmación indicando que la sesión se cerrará como "Incompleta" y mostrando cuántos ejercicios quedaron sin completar, antes de ejecutar el cierre.

### CA-09.04 — Cálculo automático de tonelaje al cerrar

**Dado que** el ejecutante cierra una sesión (ya sea Completada o Incompleta),
**cuando** el sistema procesa el cierre,
**entonces** calcula automáticamente el tonelaje total de la sesión sumando el producto de Peso × Repeticiones de todas las series registradas en la sesión, y almacena el resultado como dato derivado de la sesión.

### CA-09.05 — Avance de la rotación cíclica al cerrar

**Dado que** el ejecutante cierra una sesión (ya sea Completada o Incompleta),
**cuando** el sistema procesa el cierre,
**entonces** avanza la posición en la rotación cíclica de módulos al siguiente módulo (A→B, B→C, C→A) y registra la versión ejecutada para actualizar la secuencia de versiones del módulo.

### CA-09.06 — Avance de rotación solo al cerrar

**Dado que** el ejecutante tiene una sesión activa en progreso,
**cuando** aún no ha cerrado la sesión,
**entonces** la posición en la rotación cíclica y la secuencia de versiones no se modifican; el avance ocurre únicamente en el momento del cierre.

### CA-09.07 — Inmutabilidad de sesiones cerradas

**Dado que** una sesión ha sido cerrada (Completada o Incompleta),
**cuando** el ejecutante consulta esa sesión en el historial,
**entonces** los datos de la sesión son de solo lectura; no se permite editar, eliminar ni agregar series retroactivamente a sesiones cerradas.

### CA-09.08 — Preservación ante cierre inesperado durante el cierre

**Dado que** la aplicación se cierra inesperadamente mientras el sistema procesa el cierre de una sesión (cálculo de tonelaje, avance de rotación),
**cuando** el ejecutante reabre la aplicación,
**entonces** el sistema detecta el estado inconsistente y permite al ejecutante completar el cierre de la sesión o recuperar la sesión activa sin pérdida de datos registrados.

---

## Análisis Arquitectónico (Arquitecto)

### Decisiones de Diseño

**1. E4 es un AlertDialog, no una ruta de navegación.**

ADR D-04 y Arquitectura Técnica §4.3 fila 12 establecen que E4 es un diálogo modal superpuesto sobre E1, gestionado por estado del ViewModel (`showCloseDialog: Boolean`). No entra al back stack. El `AlertDialog` M3 se muestra/oculta por estado reactivo, no por navegación.

**2. El tonelaje NO se almacena en base de datos.**

CA-09.04 dice "almacena el resultado como dato derivado". Sin embargo, el Modelo de Datos §2 (Convenciones) establece: *"Datos calculados: No se almacenan en base de datos"*. Y §3.10 (`session`) confirma: *"El tonelaje total de la sesión no se almacena — se calcula como `SUM(exercise_set.weight_kg * exercise_set.reps)`"*. El Modelo de Datos es autoritativo para la capa de datos. El tonelaje se calcula on-demand cuando E5 (HU-13), F1 (HU-24) o G1 (HU-20) lo necesiten. **En HU-09 no se calcula ni se almacena** — solo se cierra la sesión y se avanza la rotación.

**3. La operación de cierre es una transacción atómica con 2 operaciones.**

`database.withTransaction` que: (a) actualiza `session.status` a COMPLETED o INCOMPLETE, y (b) avanza `rotation_state`. Si cualquier operación falla, todo se revierte, garantizando consistencia (CA-09.08). La sesión permanece `IN_PROGRESS` y el usuario puede reintentar.

**4. Determinación del status: COMPLETED vs INCOMPLETE.**

Una sesión es COMPLETED si y solo si **todos** sus ejercicios tienen exactamente 4 series registradas (`completedExercises == totalExercises`). En cualquier otro caso es INCOMPLETE. Esta lógica ya existe de forma reactiva en `ActiveSessionUiState` (`completedCount`, `totalCount`) para la UI. En el cierre, se reutiliza la query `getActiveSessionWithModuleVersion()` via `.first()` dentro de la transacción para obtener los conteos sincrónicamente.

**5. Avance de rotación: posición + versiones.**

El avance sigue la lógica documentada en Modelo de Datos §3.14:
- Si `microcyclePosition < 6`: `microcyclePosition += 1`. Solo se avanza la posición.
- Si `microcyclePosition == 6`: `microcyclePosition = 1`, `microcycleCount += 1`, y todas las versiones avanzan: `(currentVersion % 3) + 1` (wrap-around V1→V2→V3→V1).

Fórmula de wrap-around para versiones: `(v % 3) + 1` — produce: 1→2, 2→3, 3→1. Aplica a los 3 módulos simultáneamente puesto que todos tienen exactamente 3 versiones (Modelo de Datos §3.6, CHECK constraint `version_number >= 1 AND version_number <= 3`).

**6. HU-17 futuro: descarga congela versiones.**

Cuando HU-17 se implemente, el avance de rotación durante descarga deberá saltar el avance de versiones (CA-17.04: versiones se congelan). La posición sí avanza. El código de HU-09 se diseña para que este behavior sea extensible: la lógica de avance es una función pura testeable (`advanceRotation`) en el `RotationResolver`, que en HU-17 recibirá un parámetro `isDeload: Boolean` adicional.

**7. Navegación post-cierre: a Home (E5 diferido a HU-13).**

E5 (Resumen Post-Sesión) es responsabilidad de HU-13 y aún no existe. Por consistencia con el patrón de stubs establecido (HU-05, HU-06, HU-07), tras confirmar el cierre exitoso se navega a Home (B1) con `popUpTo(HOME) { inclusive = true }`. La ruta `session-summary/{sessionId}` se registrará en `NavigationRoutes` como preparación para HU-13 (costo marginal cero). Cuando HU-13 se implemente, la navegación post-cierre se redireccionará a E5.

**8. BackHandler muestra el diálogo de cierre.**

HU-05 documentó: *"En HU-09 se reemplaza con diálogo de confirmación"*. El `BackHandler { /* no-op */ }` actual se reemplaza con `BackHandler { viewModel.onCloseSessionRequested() }`. El comportamiento del back press pasa de "no hacer nada" a "mostrar el diálogo de confirmación de cierre (E4)".

**9. CA-09.06 y CA-09.07 son garantías estructurales, no funcionalidades.**

CA-09.06 (rotación solo avanza al cerrar) se garantiza porque la única invocación de `advanceRotation` está dentro de `closeSession`. No existe otra ruta en el código que modifique `rotation_state`.

CA-09.07 (inmutabilidad post-cierre) se garantiza porque: (a) no existe funcionalidad de edición/eliminación de sesiones cerradas (ADR D-03), (b) `registerSet` valida que la sesión esté `IN_PROGRESS` (implícitamente — la UI solo muestra E1 para sesiones activas), (c) no hay endpoint de API que modifique sesiones pasadas.

**10. CA-09.08 (crash recovery) ya está implementado.**

B1 (Home) ya detecta sesiones `IN_PROGRESS` y muestra la card "Reanudar Sesión" (implementada en HU-05). Si la app crasha durante el cierre, la transacción se revierte (nada se commitea parcialmente), la sesión permanece `IN_PROGRESS`, y al reabrir la app B1 muestra la card de reanudación. El ejecutante puede reintentar el cierre desde E1. No se requiere lógica adicional.

### Componentes Nuevos

**1. `CloseSessionUseCase`** — `domain/usecase/session/CloseSessionUseCase.kt`

```kotlin
class CloseSessionUseCase @Inject constructor(
    private val sessionRepository: SessionRepository,
) {
    suspend operator fun invoke(sessionId: Long) {
        sessionRepository.closeSession(sessionId)
    }
}
```

Delega al repositorio. La determinación de COMPLETED/INCOMPLETE se hace en el repositorio dentro de la transacción para garantizar consistencia (la query de conteo y el update deben ser atómicos).

**2. `RotationResolver.advanceRotation()`** — extensión del object existente

```kotlin
fun advanceRotation(current: RotationState): RotationState {
    return if (current.microcyclePosition < 6) {
        current.copy(microcyclePosition = current.microcyclePosition + 1)
    } else {
        current.copy(
            microcyclePosition = 1,
            currentVersionModuleA = (current.currentVersionModuleA % 3) + 1,
            currentVersionModuleB = (current.currentVersionModuleB % 3) + 1,
            currentVersionModuleC = (current.currentVersionModuleC % 3) + 1,
            microcycleCount = current.microcycleCount + 1,
        )
    }
}
```

Función pura, testeable unitariamente. Opera sobre el modelo de dominio `RotationState`, no sobre la entity.

### Componentes Modificados

**3. `SessionRepository`** — agregar método

```kotlin
suspend fun closeSession(sessionId: Long)
```

**4. `SessionRepositoryImpl.closeSession()`** — implementación transaccional

```kotlin
override suspend fun closeSession(sessionId: Long) {
    database.withTransaction {
        val sessionInfo = sessionDao.getActiveSessionWithModuleVersion().first()
            ?: throw IllegalStateException("No active session found")
        if (sessionInfo.sessionId != sessionId) {
            throw IllegalStateException("Session $sessionId is not the active session")
        }

        val status = if (sessionInfo.completedExercises == sessionInfo.totalExercises) {
            "COMPLETED"
        } else {
            "INCOMPLETE"
        }
        sessionDao.updateStatus(sessionId, status)

        val rotationEntity = rotationStateDao.getRotationState().first()
            ?: throw IllegalStateException("Rotation state not found")
        val currentRotation = RotationState(
            microcyclePosition = rotationEntity.microcyclePosition,
            currentVersionModuleA = rotationEntity.currentVersionModuleA,
            currentVersionModuleB = rotationEntity.currentVersionModuleB,
            currentVersionModuleC = rotationEntity.currentVersionModuleC,
            microcycleCount = rotationEntity.microcycleCount,
        )
        val newRotation = RotationResolver.advanceRotation(currentRotation)
        rotationStateDao.update(
            rotationEntity.copy(
                microcyclePosition = newRotation.microcyclePosition,
                currentVersionModuleA = newRotation.currentVersionModuleA,
                currentVersionModuleB = newRotation.currentVersionModuleB,
                currentVersionModuleC = newRotation.currentVersionModuleC,
                microcycleCount = newRotation.microcycleCount,
            ),
        )
    }
}
```

**5. `SessionDao`** — agregar query

```kotlin
@Query("UPDATE session SET status = :status WHERE id = :sessionId")
suspend fun updateStatus(sessionId: Long, status: String)
```

**6. `ActiveSessionViewModel`** — agregar lógica de diálogo y cierre

Campos nuevos en `_uiState`:
- `showCloseDialog: Boolean = false` — controla visibilidad de E4
- `isClosing: Boolean = false` — loading state durante cierre

Funciones nuevas:
- `onCloseSessionRequested()` — muestra diálogo (`showCloseDialog = true`)
- `onCloseDialogDismissed()` — oculta diálogo (`showCloseDialog = false`)
- `onCloseSessionConfirmed()` — invoca `CloseSessionUseCase`, emite navegación a E5 (temporalmente Home)

```kotlin
fun onCloseSessionRequested() {
    _uiState.update { it.copy(showCloseDialog = true) }
}

fun onCloseDialogDismissed() {
    _uiState.update { it.copy(showCloseDialog = false) }
}

fun onCloseSessionConfirmed() {
    viewModelScope.launch {
        _uiState.update { it.copy(isClosing = true, showCloseDialog = false) }
        try {
            closeSessionUseCase(sessionId)
            _navigateToSessionSummary.emit(sessionId)
        } catch (_: Exception) {
            _uiState.update { it.copy(isClosing = false) }
        }
    }
}
```

**Nuevo SharedFlow:** `_navigateToSessionSummary: MutableSharedFlow<Long>` + `navigateToSessionSummary: SharedFlow<Long>` — one-shot navigation event que emite el `sessionId`. La Screen observa este flujo y llama `onNavigateToSessionSummary(sessionId)`. Esto reutiliza el callback existente (stub de HU-05) en vez de crear un mecanismo paralelo, facilitando la transición cuando HU-13 implemente E5.

**7. `ActiveSessionUiState`** — agregar campos

```kotlin
data class ActiveSessionUiState(
    val isLoading: Boolean = true,
    val moduleCode: String = "",
    val versionNumber: Int = 0,
    val exercises: List<ExerciseUiItem> = emptyList(),
    val showCloseDialog: Boolean = false,
    val isClosing: Boolean = false,
) {
    // existentes
    val completedCount: Int get() = exercises.count { ... }
    val totalCount: Int get() = exercises.size
    val progress: Float get() = ...
    // nuevo
    val incompleteCount: Int get() = totalCount - completedCount
    val isAllCompleted: Boolean get() = completedCount == totalCount && totalCount > 0
}
```

**8. `ActiveSessionScreen`** — wiring del botón, BackHandler, y AlertDialog

Cambios:
1. **`BackHandler`**: `BackHandler { viewModel.onCloseSessionRequested() }` (reemplaza no-op).
2. **Botón "Cerrar Sesión"**: `onClick = { viewModel.onCloseSessionRequested() }` (reemplaza TODO).
3. **AlertDialog E4**: Composable condicional basado en `uiState.showCloseDialog`.
4. **Navigation event**: `LaunchedEffect` que observa `navigateToSessionSummary` y llama `onNavigateToSessionSummary(sessionId)`.

AlertDialog tiene 2 variantes (Especificación Visual §8 E4):
- **Caso A (completa)**: título "Cerrar sesión", texto "Todos los ejercicios están completados. La sesión se cerrará como Completada.", botón "Cerrar ✓" (Primary).
- **Caso B (parcial)**: título "Cerrar sesión", ícono ⚠️ (Error), texto "Hay N ejercicios sin completar. La sesión se cerrará como Incompleta. Los datos parciales se conservarán.", botón "Cerrar ⚠️" (Error).

```kotlin
@Composable
private fun CloseSessionDialog(
    isAllCompleted: Boolean,
    incompleteCount: Int,
    isClosing: Boolean,
    onConfirm: () -> Unit,
    onDismiss: () -> Unit,
) {
    AlertDialog(
        onDismissRequest = onDismiss,
        icon = if (!isAllCompleted) {
            { Icon(Icons.Default.Warning, contentDescription = null, tint = MaterialTheme.colorScheme.error) }
        } else null,
        title = { Text(stringResource(R.string.session_close_title)) },
        text = {
            Text(
                if (isAllCompleted)
                    stringResource(R.string.session_close_complete_message)
                else
                    stringResource(R.string.session_close_incomplete_message, incompleteCount)
            )
        },
        confirmButton = {
            Button(
                onClick = onConfirm,
                enabled = !isClosing,
                colors = ButtonDefaults.buttonColors(
                    containerColor = if (isAllCompleted)
                        MaterialTheme.colorScheme.primary
                    else
                        MaterialTheme.colorScheme.error,
                ),
            ) {
                if (isClosing) {
                    CircularProgressIndicator(
                        modifier = Modifier.size(18.dp),
                        strokeWidth = 2.dp,
                        color = MaterialTheme.colorScheme.onPrimary,
                    )
                } else {
                    Text(
                        if (isAllCompleted)
                            stringResource(R.string.session_close_confirm_complete)
                        else
                            stringResource(R.string.session_close_confirm_incomplete)
                    )
                }
            }
        },
        dismissButton = {
            TextButton(onClick = onDismiss, enabled = !isClosing) {
                Text(stringResource(R.string.session_close_cancel))
            }
        },
    )
}
```

**9. `NavigationRoutes`** — agregar ruta E5 (preparación para HU-13)

```kotlin
const val SESSION_SUMMARY = "session-summary/{sessionId}"
fun sessionSummaryRoute(sessionId: Long) = "session-summary/$sessionId"
```

**10. `TensionNavHost`** — wiring de `onNavigateToSessionSummary`

Temporalmente navegará a Home (E5 no existe aún):
```kotlin
onNavigateToSessionSummary = { sessionId ->
    // TODO: HU-13 — navigate to session-summary/$sessionId
    navController.navigate(NavigationRoutes.HOME) {
        popUpTo(NavigationRoutes.HOME) { inclusive = true }
    }
},
```

**11. `strings.xml`** — agregar strings de E4

```xml
<!-- Close Session Dialog E4 -->
<string name="session_close_title">Cerrar sesión</string>
<string name="session_close_complete_message">Todos los ejercicios están completados. La sesión se cerrará como Completada.</string>
<string name="session_close_incomplete_message">Hay %1$d ejercicios sin completar. La sesión se cerrará como Incompleta. Los datos parciales se conservarán.</string>
<string name="session_close_confirm_complete">Cerrar ✓</string>
<string name="session_close_confirm_incomplete">Cerrar ⚠️</string>
<string name="session_close_cancel">Cancelar</string>
```

### Verificación Exhaustiva CA por CA

**CA-09.01 — Cierre como Completada:**

| Capa | Implementación | Evidencia |
|------|----------------|-----------|
| Repository | `if (completedExercises == totalExercises) "COMPLETED"` | Transacción atómica en `closeSession()` |
| DAO | `SessionDao.updateStatus(sessionId, "COMPLETED")` | UPDATE query |
| ViewModel | `onCloseSessionConfirmed()` → UseCase → Repository | Delegación limpia |
| UI (E4) | Caso A: texto confirmatorio + botón "Cerrar ✓" (Primary) | AlertDialog variante A |

**CA-09.02 — Cierre como Incompleta:**

| Capa | Implementación | Evidencia |
|------|----------------|-----------|
| Repository | `else "INCOMPLETE"` | Cualquier estado parcial → INCOMPLETE |
| Data | Series existentes no se eliminan | `updateStatus` solo cambia status, no toca exercise_set |
| ViewModel | Misma función `onCloseSessionConfirmed()` | Sin distinción — el repositorio decide |

**CA-09.03 — Confirmación antes de cierre incompleto:**

| Capa | Implementación | Evidencia |
|------|----------------|-----------|
| UI (E4) | Caso B: ícono ⚠️ Error + texto con N ejercicios + botón "Cerrar ⚠️" (Error) | AlertDialog variante B |
| UiState | `incompleteCount: Int` calculado en `ActiveSessionUiState` | `totalCount - completedCount` |
| Wireframes | "Hay N ejercicios sin completar" vs "Todos ... completados" | Mapeado 1:1 |

**Nota:** El diálogo se muestra en AMBOS casos (completo e incompleto), no solo cuando incompleto. Wireframes E4 documenta ambas variantes. La diferencia es visual: Caso A es confirmatorio (Primary), Caso B es de advertencia (Error).

**CA-09.04 — Cálculo automático de tonelaje:**

| Capa | Implementación | Evidencia |
|------|----------------|-----------|
| Datos | Tonelaje = `SUM(weight_kg * reps)` — derivado, NO almacenado | Modelo de Datos §2 y §3.10 |
| HU-09 | No calcula tonelaje directamente | El tonelaje se calcula cuando E5 (HU-13) lo consume |
| Query | `SELECT COALESCE(SUM(es.weight_kg * es.reps), 0.0) FROM exercise_set es INNER JOIN session_exercise se ON es.session_exercise_id = se.id WHERE se.session_id = :sessionId` | Será implementada en HU-13 |

**Justificación:** CA-09.04 dice "calcula ... y almacena". El Modelo de Datos contradice el "almacena" explícitamente. La resolución es que el CA usa "almacena" en sentido de "el dato queda disponible" (las series están persistidas, el tonelaje se puede derivar en cualquier momento). No se agrega columna `tonnage` a `session`. La query de cálculo es responsabilidad de quien muestre el dato (E5/HU-13, F1/HU-24, G1/HU-20).

**CA-09.05 — Avance de rotación al cerrar:**

| Capa | Implementación | Evidencia |
|------|----------------|-----------|
| Domain | `RotationResolver.advanceRotation()` — función pura | `(microcyclePosition % 3) + 1` para versiones, +1 para posición |
| Repository | `rotationStateDao.update(newState)` dentro de transacción | Atómico con update de status |
| Lógica pos. < 6 | Solo incrementa posición | `copy(microcyclePosition = pos + 1)` |
| Lógica pos. == 6 | Reset a 1 + advance versions + increment count | `copy(pos=1, vA=(vA%3)+1, vB=(vB%3)+1, vC=(vC%3)+1, count+1)` |

**CA-09.06 — Avance solo al cerrar:**

| Capa | Implementación | Evidencia |
|------|----------------|-----------|
| Estructural | `advanceRotation()` solo se invoca desde `closeSession()` | No existe otro call site |
| Garantía | No hay cron, timer, ni trigger que modifique `rotation_state` | Código inmutable post-cierre |

**CA-09.07 — Inmutabilidad de sesiones cerradas:**

| Capa | Implementación | Evidencia |
|------|----------------|-----------|
| Estructural | No existe endpoint de edición/eliminación post-cierre | ADR D-03 |
| UI | `registerSet` solo funciona durante sesión activa (E1 se muestra solo para IN_PROGRESS) | La navegación a E1 requiere sessionId activo |
| Data | `exercise_set.insert()` valida dentro de sesión activa | `getNextSetNumber` devuelve > 4 si ya está completo |

**CA-09.08 — Preservación ante cierre inesperado:**

| Capa | Implementación | Evidencia |
|------|----------------|-----------|
| Transaction | `database.withTransaction` — si el crash ocurre mid-transaction, nada se commitea | Room rollback automático |
| Recovery | B1 detecta `IN_PROGRESS` → muestra ResumeSessionCard | Implementado en HU-05 |
| Garantía | La sesión queda como estaba antes del intento de cierre | El usuario retoma desde E1 |

### Validación de Impacto

**Archivos modificados (estimación):**

| Archivo | Acción | Sección |
|---|---|---|
| `SessionRepository.kt` | Agregar `closeSession(sessionId: Long)` | Interface |
| `SessionRepositoryImpl.kt` | Implementar `closeSession()` con transacción | Repository |
| `SessionDao.kt` | Agregar `updateStatus()` | DAO |
| `RotationResolver.kt` | Agregar `advanceRotation()` | Domain model |
| `ActiveSessionViewModel.kt` | Agregar diálogo state + cierre + navegación | ViewModel |
| `ActiveSessionUiState.kt` | Agregar `showCloseDialog`, `isClosing`, derivados | UiState |
| `ActiveSessionScreen.kt` | Wiring botón + BackHandler + AlertDialog E4 | UI |
| `NavigationRoutes.kt` | Agregar `SESSION_SUMMARY` | Navigation |
| `TensionNavHost.kt` | Wiring `onNavigateToSessionSummary` temporal a Home | Navigation |
| `strings.xml` | 6 strings de E4 | Resources |

**Archivos nuevos:**

| Archivo | Propósito |
|---|---|
| `CloseSessionUseCase.kt` | Use Case de cierre de sesión |

**Archivos no tocados (verificación):**

| Archivo | Razón |
|---|---|
| `ExerciseSetDao.kt` | No se agrega query de tonelaje — diferido a HU-13 |
| `ExerciseProgressionDao.kt` | No se modifica — HU-10 lo usará |
| `RepositoryModule.kt` | SessionRepository ya está bound — no cambia |
| `HomeScreen.kt` | Crash recovery ya funciona — no cambia |

### Notas Técnicas

1. **Patrón `.first()` dentro de transacción.** El codebase ya usa `sessionDao.getActiveSession().first()` dentro de `database.withTransaction` en `startSession()`. Mantenemos el mismo patrón para `getActiveSessionWithModuleVersion().first()` y `getRotationState().first()` en `closeSession()`. No se requieren queries suspend adicionales en los DAOs.

2. **El ViewModel usa `onNavigateToSessionSummary` desde el principio.** El SharedFlow `_navigateToSessionSummary` emite el `sessionId`, la Screen observa y llama `onNavigateToSessionSummary(sessionId)`, y TensionNavHost redirige temporalmente a Home. Cuando HU-13 implemente E5, solo cambia la lambda en TensionNavHost — ni el ViewModel ni la Screen necesitan modificaciones.

3. **La ruta `SESSION_SUMMARY` se agrega proactivamente** en `NavigationRoutes.kt`. Costo marginal cero — una constante y una función. Prepara HU-13 para que solo necesite crear el composable y registrar la ruta en NavHost.

4. **Progresión (HU-10/HU-11/HU-12) no se invoca en HU-09.** El cierre de sesión no evalúa progresión ni prescribe carga. Eso se implementará en HU-10/HU-11/HU-12 como extensiones del flujo de cierre. El punto de extensión será dentro de la transacción de `closeSession()`, después de actualizar el status y antes de avanzar la rotación — cuando esos HUs se implementen deben insertarse ahí.

5. **Deload awareness (HU-17) no implementada aún.** `advanceRotation()` siempre avanza las versiones cuando `position == 6`. Cuando HU-17 se implemente, `advanceRotation` recibirá `isDeload: Boolean` y, si true, saltará el avance de versiones (las congela). Por ahora, el comportamiento normal es correcto.

### Historias Relacionadas

**Predecesoras (datos que HU-09 consume):**

- HU-05: Proporcionó `RotationStateDao.update()`, stubs de `BackHandler`, botón "Cerrar Sesión", `onNavigateToSessionSummary`, y la card de crash recovery en B1.
- HU-06: Proporcionó `exercise_set` data que determina el conteo de sets por ejercicio → status COMPLETED/INCOMPLETE.
- HU-07: Sustituciones funcionan transparentemente — `session_exercise.exercise_id` apunta al ejercicio real ejecutado sin lógica condicional.

**Sucesoras (dependen de HU-09):**

- HU-10: Motor de progresión — se ejecutará al cierre de sesión. Clasificará cada ejercicio como POSITIVE_PROGRESSION/MAINTENANCE/REGRESSION. Se integrará dentro de la transacción de `closeSession()`.
- HU-11: Doble Umbral — prescribirá carga objetivo. También se integra en el cierre.
- HU-12: Detección de regresión/fatiga — también post-cierre.
- HU-13: E5 (Resumen Post-Sesión) — navegación post-cierre. Consumirá `sessionId` para mostrar tonelaje, clasificación de progresión, señales de acción. La ruta `session-summary/{sessionId}` se prepara en HU-09.
- HU-17: Protocolo de descarga — modificará `advanceRotation` para congelar versiones durante deload.
- HU-18: Usará `microcycleCount` incrementado por HU-09 en B1.

**Consumidoras indirectas (usan datos derivados del cierre):**

- HU-20: Métricas globales — tonelaje por sesión.
- HU-21: Adherencia — cuenta sesiones COMPLETED + INCOMPLETE.
- HU-24: Historial de sesiones — muestra tonelaje derivado.
- HU-15 (ex HU-25 pre-renumeración): Evolución de tonelaje por grupo muscular.

### Referencias y Validación

**Documentación consultada:**

- Modelo de Datos §3.10 (`session`): status IN_PROGRESS → COMPLETED/INCOMPLETE. Tonelaje no almacenado.
- Modelo de Datos §3.14 (`rotation_state`): lógica de avance pos < 6 → +1; pos = 6 → reset + increment + advance versions.
- Wireframes E4: 2 variantes de diálogo (completa / parcial). Elementos: título, mensaje, botón cancelar, botón cerrar.
- Especificación Visual §8 E4: Caso A (Primary) vs Caso B (Error). AlertDialog M3, corner 28 dp, scrim 32%.
- Mapa de Navegación: E4 → HU-09 | E5 → HU-13. E4 no tiene ruta de navegación.
- Arquitectura Técnica §4.3 fila 12: E4 es dialog, no ruta. Fila 13: E5 = `session-summary/{sessionId}`.
- ADR D-03: Sesiones cerradas inmutables. D-04: E4 es dialog gestionado por ViewModel.
- MDS §4 Restricción 8 + §5: "Sesión completada" → calcular métricas, actualizar rotación.
- Requerimientos: RF18 (cerrar completada), RF19 (cerrar incompleta), RF20 (tonelaje), RF21 (avance rotación), RNF10 (crash recovery).
- HU-05 análisis: stubs de BackHandler, botón cerrar, onNavigateToSessionSummary, RotationStateDao.update().

### Verificación Cruzada de CAs

| CA | Estado | Mecanismo | Implementado en |
|---|---|---|---|
| CA-09.01 | 🔨 Por implementar | `closeSession()` → `updateStatus("COMPLETED")` si todos completos | HU-09 (transacción) |
| CA-09.02 | 🔨 Por implementar | `closeSession()` → `updateStatus("INCOMPLETE")` si parcial | HU-09 (transacción) |
| CA-09.03 | 🔨 Por implementar | AlertDialog E4 con 2 variantes (completa/parcial) + conteo | HU-09 (UI) |
| CA-09.04 | ⏳ Parcial | Tonelaje = dato derivado, no almacenado. Query será en HU-13 | HU-09 persiste datos; HU-13 calcula |
| CA-09.05 | 🔨 Por implementar | `RotationResolver.advanceRotation()` + `rotationStateDao.update()` | HU-09 (transacción) |
| CA-09.06 | ✅ Estructural | `advanceRotation` solo se invoca desde `closeSession` | Garantía por diseño |
| CA-09.07 | ✅ Estructural | No existe endpoint de edición post-cierre (ADR D-03) | Garantía por diseño |
| CA-09.08 | ✅ Ya implementado | `withTransaction` rollback + B1 crash recovery card | HU-05 (B1 recovery) + Room (transaction) |

### Hitos de implementación

| # | Entregable | Archivos | CAs |
|---|---|---|---|
| 1 | `RotationResolver.advanceRotation()` — función pura + test unitario | `RotationResolver.kt`, `RotationResolverTest.kt` | CA-09.05 |
| 2 | `SessionDao.updateStatus()` — query de update de status | `SessionDao.kt` | CA-09.01, CA-09.02 |
| 3 | `CloseSessionUseCase` + `SessionRepository.closeSession()` + implementación transaccional | `CloseSessionUseCase.kt`, `SessionRepository.kt`, `SessionRepositoryImpl.kt` | CA-09.01, CA-09.02, CA-09.05, CA-09.08 |
| 4 | `ActiveSessionUiState` + `ActiveSessionViewModel` — diálogo state + cierre + navegación | `ActiveSessionUiState.kt`, `ActiveSessionViewModel.kt` | CA-09.01, CA-09.02, CA-09.03 |
| 5 | `ActiveSessionScreen` — BackHandler + botón + AlertDialog E4 | `ActiveSessionScreen.kt`, `strings.xml` | CA-09.03 |
| 6 | `NavigationRoutes` + `TensionNavHost` — ruta E5 stub + wiring navegación post-cierre | `NavigationRoutes.kt`, `TensionNavHost.kt` | — (preparación HU-13) |

---

## Refinamiento Técnico (Developer)

<!-- ============================================================================ -->
<!-- SECCIÓN AGREGADA POR: Workflow refinamiento-tecnico                         -->
<!-- ETAPA: Refinamiento Técnico                                                  -->
<!-- RESPONSABLE: Developer                                                       -->
<!-- BASE: Análisis Arquitectónico (Arquitecto) - Ver sección arriba             -->
<!-- FECHA: 2026-02-15                                                            -->
<!-- ESTADO: Refinado (Developer) - Basado en Análisis Arquitectónico             -->
<!-- ============================================================================ -->

### Consideraciones Generales

**Basado en análisis arquitectónico:**
Análisis Arquitectónico de HU-09 con 10 decisiones de diseño, 6 hitos de implementación, 11 componentes (1 nuevo + 10 modificados). Hallazgos principales: (1) E4 es AlertDialog modal sobre E1, no ruta de navegación. (2) Tonelaje NO se almacena — dato derivado calculado on-demand por HU-13/HU-24/HU-20. (3) Cierre es transacción atómica: update status + advance rotation. (4) `advanceRotation()` como función pura en `RotationResolver`. (5) Navegación post-cierre temporal a Home (E5 diferido a HU-13). (6) CA-09.06, CA-09.07 son garantías estructurales; CA-09.08 ya implementado (transaction rollback + B1 recovery card).

**Nivel de complejidad:**
MEDIA — HU-09 toca 11 archivos (1 nuevo + 10 modificados) distribuidos en 4 capas (Domain, Data, UI, Navigation). La lógica de negocio es directa (status + rotación), pero la transaccionalidad, el AlertDialog con 2 variantes, y el SharedFlow de navegación requieren coordinación cuidadosa. La función pura `advanceRotation()` es la pieza testeable más crítica (fórmula de wrap-around `(v % 3) + 1`).

**Riesgos técnicos conocidos:**

1. **Race condition en `closeSession()`:** Si el usuario presiona "Cerrar" dos veces rápidamente, `closeSessionUseCase` podría invocarse dos veces. Mitigación: `isClosing` flag en UiState deshabilita el botón y `_uiState.update { it.copy(isClosing = true, showCloseDialog = false) }` se ejecuta sincrónicamente antes del launch. La segunda invocación vería `isClosing = true` y el botón estaría disabled.
2. **`.first()` dentro de transacción:** Ya es patrón establecido en `startSession()` (línea 67 de `SessionRepositoryImpl.kt`). Room garantiza que `.first()` dentro de `withTransaction` emite el snapshot transaccional.

**Patrones y convenciones del equipo (establecidos en HU-01—HU-07):**

- Código fuente en inglés, UI y datos de dominio en español (Arquitectura Técnica §5.1)
- Mensajes de validación de dominio (`require()`, `IllegalStateException`) en inglés (§5.7)
- Naming: `{Feature}Screen`, `{Feature}ViewModel`, `{Acción}{Entidad}UseCase`, `{Entidad}Entity` (§5.2)
- Transacciones atómicas con `database.withTransaction` para operaciones multi-tabla (`startSession`, `registerSet`, `substituteExercise` → ahora `closeSession`)
- Stubs con `/* TODO: HU-XX */` para funcionalidad futura (patrón HU-05/HU-06/HU-07)
- SharedFlow con `replay = 0` para one-shot navigation events (patrón `RegisterSetViewModel._navigateBack`)
- `AlertDialog` M3 gestionado por estado del ViewModel (patrón `SubstituteExerciseViewModel.showConfirmDialog` en HU-07)

**Dependencias nuevas a instalar:**
Ninguna.

**Estrategia de testing:**
JUnit 4 + MockK + kotlinx-coroutines-test | Unitarios: `RotationResolverTest` (función pura, 6 escenarios pos 1-5 y pos 6 con wrap-around) + `CloseSessionUseCaseTest` (delegación al repositorio) | Cobertura: foco en `advanceRotation()` que es lógica pura crítica; `closeSession()` transaccional se valida por integración manual

### Historias Relacionadas Consultadas

**Implementaciones similares analizadas:**

- HU-05 (StartSession) — Proporcionó stubs de `BackHandler { no-op }`, botón "Cerrar Sesión" con `/* TODO: HU-09 */`, callback `onNavigateToSessionSummary: (Long) -> Unit`, y la card de crash recovery en B1 (`home_resume_title`). También estableció el patrón de `database.withTransaction` + `.first()` en `startSession()`.
- HU-06 (RegisterSet) — Patrón de SharedFlow one-shot (`_navigateBack: MutableSharedFlow<Boolean>(replay = 0)`) para navegación post-acción. HU-09 lo reutiliza con `_navigateToSessionSummary: MutableSharedFlow<Long>`.
- HU-07 (SubstituteExercise) — Patrón de AlertDialog gestionado por estado del ViewModel (`showConfirmDialog: Boolean`), con `onDismiss` que resetea el flag y `onConfirm` que ejecuta la acción. HU-09 replica este patrón para E4.

**Patrones de código reutilizados:**

- Transacción atómica multi-tabla: `database.withTransaction { query → validate → update }` (de `startSession`, `registerSet`, `substituteExercise`)
- AlertDialog reactivo: ViewModel state `show*Dialog: Boolean` → Screen composable condicional (de HU-07 `SubstituteExerciseScreen`)
- SharedFlow one-shot navigation: `MutableSharedFlow<T>(replay = 0)` + `LaunchedEffect` collector en Screen (de HU-06 `RegisterSetViewModel._navigateBack`)
- `RotationResolver` object con funciones puras: ya tiene `resolveModuleCode()` y `resolveVersionNumber()` — `advanceRotation()` se agrega al mismo object

**Mejores prácticas aplicadas:**

- Función pura `advanceRotation()` testeable unitariamente sin emulador ni mocks (ADR-06: reglas de dominio como Kotlin puro)
- Preparación de ruta `SESSION_SUMMARY` con costo marginal cero para HU-13 (patrón de stubs de HU-05/HU-06)
- `isClosing` flag para UX feedback durante operación asíncrona + prevención de double-tap (patrón `isSaving` en `RegisterSetUiState`)

---

## Tareas de Implementación (Developer)

### Fase 1: Domain — Lógica de rotación pura

<!-- Basado en Hito #1 del Análisis Arquitectónico -->

#### 📦 Domain Model

- [ ] **Implementar `RotationResolver.advanceRotation()`** (AC: 5)
  - [ ] Agregar función `fun advanceRotation(current: RotationState): RotationState` en `domain/model/RotationResolver.kt`
  - [ ] Lógica: si `microcyclePosition < 6` → `copy(microcyclePosition = pos + 1)`; si `== 6` → `copy(pos=1, vA=(vA%3)+1, vB=(vB%3)+1, vC=(vC%3)+1, count+1)`
- [ ] **Test unitario `RotationResolverTest`** (AC: 5)
  - [ ] Crear archivo: `src/test/java/com/estebancoloradogonzalez/tension/domain/model/RotationResolverTest.kt`
  - [ ] Test: posición 1→2 (solo avanza posición)
  - [ ] Test: posición 5→6 (solo avanza posición)
  - [ ] Test: posición 6→1 con wraparound V1→V2 (versiones avanzan, count+1)
  - [ ] Test: posición 6→1 con wraparound V3→V1 (boundary case de `(3%3)+1=1`)
  - [ ] Test: posición 6→1 con versiones mixtas (V1, V2, V3 → V2, V3, V1)
  - [ ] Test: posición 6→1 incrementa `microcycleCount`

### Fase 2: Data — DAO + Repository

<!-- Basado en Hitos #2 y #3 del Análisis Arquitectónico -->

#### 📦 Data Layer

- [ ] **Agregar `SessionDao.updateStatus()`** (AC: 1, 2)
  - [ ] Agregar query `@Query("UPDATE session SET status = :status WHERE id = :sessionId") suspend fun updateStatus(sessionId: Long, status: String)` - Archivo: `data/local/dao/SessionDao.kt`
- [ ] **Agregar `SessionRepository.closeSession()`** (AC: 1, 2, 5, 8)
  - [ ] Agregar método `suspend fun closeSession(sessionId: Long)` en interface - Archivo: `domain/repository/SessionRepository.kt`
- [ ] **Implementar `SessionRepositoryImpl.closeSession()`** (AC: 1, 2, 5, 8)
  - [ ] Implementar método transaccional en `data/repository/SessionRepositoryImpl.kt`
  - [ ] Dentro de `database.withTransaction`: (1) obtener sessionInfo vía `sessionDao.getActiveSessionWithModuleVersion().first()`, (2) validar `sessionId` coincide, (3) determinar status COMPLETED/INCOMPLETE, (4) `sessionDao.updateStatus(sessionId, status)`, (5) obtener rotationState, (6) mapear a `RotationState`, (7) invocar `RotationResolver.advanceRotation()`, (8) `rotationStateDao.update()` con resultado

#### 📦 Domain UseCase

- [ ] **Crear `CloseSessionUseCase`** (AC: 1, 2)
  - [ ] Crear archivo: `domain/usecase/session/CloseSessionUseCase.kt`
  - [ ] Constructor: `@Inject constructor(private val sessionRepository: SessionRepository)`
  - [ ] Operador `invoke(sessionId: Long)` que delega a `sessionRepository.closeSession(sessionId)`
  - [ ] Anotación `@HiltViewModel` no aplica — usar solo `@Inject`
- [ ] **Test unitario `CloseSessionUseCaseTest`** (AC: 1, 2)
  - [ ] Crear archivo: `src/test/java/com/estebancoloradogonzalez/tension/domain/usecase/session/CloseSessionUseCaseTest.kt`
  - [ ] Test: `invoke delegates to repository` — `coVerify { repository.closeSession(sessionId) }`
  - [ ] Test: `invoke propagates exception` — `coEvery { ... } throws IllegalStateException`

### Fase 3: UI — ViewModel + UiState

<!-- Basado en Hito #4 del Análisis Arquitectónico -->

#### 📦 ViewModel

- [ ] **Actualizar `ActiveSessionUiState`** (AC: 3)
  - [ ] Agregar campos: `showCloseDialog: Boolean = false`, `isClosing: Boolean = false` - Archivo: `ui/session/ActiveSessionUiState.kt`
  - [ ] Agregar propiedades derivadas: `val incompleteCount: Int get() = totalCount - completedCount`, `val isAllCompleted: Boolean get() = completedCount == totalCount && totalCount > 0`
- [ ] **Actualizar `ActiveSessionViewModel`** (AC: 1, 2, 3)
  - [ ] Agregar dependencia: `CloseSessionUseCase` en constructor - Archivo: `ui/session/ActiveSessionViewModel.kt`
  - [ ] Agregar SharedFlow: `private val _navigateToSessionSummary = MutableSharedFlow<Long>(replay = 0)` + `val navigateToSessionSummary: SharedFlow<Long> = _navigateToSessionSummary.asSharedFlow()`
  - [ ] Implementar `fun onCloseSessionRequested()` → `_uiState.update { it.copy(showCloseDialog = true) }`
  - [ ] Implementar `fun onCloseDialogDismissed()` → `_uiState.update { it.copy(showCloseDialog = false) }`
  - [ ] Implementar `fun onCloseSessionConfirmed()` → launch: set `isClosing=true, showCloseDialog=false`, invoke UseCase, emit `_navigateToSessionSummary(sessionId)`. Catch: reset `isClosing=false`
  - [ ] Ajustar `combine` collector para preservar `showCloseDialog` e `isClosing` cuando llegan nuevos datos del Flow (el combine actual sobreescribe todo — necesita preservar dialog state)

### Fase 4: UI — Screen + Dialog E4

<!-- Basado en Hito #5 del Análisis Arquitectónico -->

#### 📦 Screen

- [ ] **Actualizar `ActiveSessionScreen`** (AC: 1, 2, 3)
  - [ ] Reemplazar `BackHandler { /* no-op */ }` con `BackHandler { viewModel.onCloseSessionRequested() }` - Archivo: `ui/session/ActiveSessionScreen.kt`
  - [ ] Reemplazar `onClick = { /* TODO: HU-09 */ }` del botón "Cerrar Sesión" con `onClick = { viewModel.onCloseSessionRequested() }`
  - [ ] Agregar `LaunchedEffect` para observar `viewModel.navigateToSessionSummary` y llamar `onNavigateToSessionSummary(sessionId)`
  - [ ] Agregar composable condicional: `if (uiState.showCloseDialog) { CloseSessionDialog(...) }`
- [ ] **Crear composable `CloseSessionDialog`** (AC: 3)
  - [ ] Agregar función privada `CloseSessionDialog(isAllCompleted, incompleteCount, isClosing, onConfirm, onDismiss)` - Archivo: `ui/session/ActiveSessionScreen.kt`
  - [ ] Caso A (completa): sin ícono, texto confirmatorio, botón "Cerrar ✓" con color Primary
  - [ ] Caso B (parcial): ícono `Icons.Default.Warning` tint Error, texto con N ejercicios sin completar, botón "Cerrar ⚠️" con color Error
  - [ ] Ambos: `CircularProgressIndicator` en botón cuando `isClosing`, botones disabled cuando `isClosing`
- [ ] **Agregar strings de E4** (AC: 3)
  - [ ] Agregar 6 strings en `res/values/strings.xml`: `session_close_title`, `session_close_complete_message`, `session_close_incomplete_message` (con `%1$d`), `session_close_confirm_complete`, `session_close_confirm_incomplete`, `session_close_cancel`

### Fase 5: Navigation — Ruta E5 stub + wiring

<!-- Basado en Hito #6 del Análisis Arquitectónico -->

#### 📦 Navigation

- [ ] **Agregar ruta `SESSION_SUMMARY` en `NavigationRoutes`** (preparación HU-13)
  - [ ] Agregar `const val SESSION_SUMMARY = "session-summary/{sessionId}"` - Archivo: `ui/navigation/NavigationRoutes.kt`
  - [ ] Agregar `fun sessionSummaryRoute(sessionId: Long) = "session-summary/$sessionId"`
- [ ] **Actualizar `TensionNavHost` wiring** (AC: 1, 2)
  - [ ] Reemplazar `onNavigateToSessionSummary = { /* TODO: HU-09 */ }` con navegación temporal a Home - Archivo: `ui/navigation/TensionNavHost.kt`
  - [ ] Lambda: `onNavigateToSessionSummary = { _ -> navController.navigate(NavigationRoutes.HOME) { popUpTo(NavigationRoutes.HOME) { inclusive = true } } }`
  - [ ] Agregar comentario `// TODO: HU-13 — navigate to session-summary/$sessionId`

### Fase N: QA y Deployment

- [ ] **Ejecutar Agente Peer Review** (MANUAL)
- [ ] **Resolver incidentes del Peer Review** (MANUAL, condicional)
- [ ] **Crear Pull Request** (MANUAL)
- [ ] **Ejecutar pipeline deployment DEV** (MANUAL)
- [ ] **Diseñar set de pruebas manuales** (MANUAL)
- [ ] **Ejecutar pruebas manuales** (MANUAL)

---

**Notas sobre vinculación con Criterios de Aceptación:**

- CA-09.01 → Fase 2 (`SessionRepositoryImpl.closeSession()` → `updateStatus("COMPLETED")`) + Fase 3 (ViewModel) + Fase 4 (Dialog Caso A) + Fase 5 (navegación post-cierre)
- CA-09.02 → Fase 2 (`else "INCOMPLETE"` en `closeSession()`) + Fase 3 (ViewModel — misma función) + Fase 4 (Dialog Caso B)
- CA-09.03 → Fase 3 (`showCloseDialog`, `incompleteCount`, `isAllCompleted`) + Fase 4 (AlertDialog E4 con 2 variantes)
- CA-09.04 → No implementado directamente. El tonelaje es dato derivado (Modelo de Datos §2/§3.10). Los datos (series) se persisten; el cálculo (`SUM(weight_kg * reps)`) será query de HU-13 (E5)
- CA-09.05 → Fase 1 (`RotationResolver.advanceRotation()`) + Fase 2 (`rotationStateDao.update()` dentro de transacción)
- CA-09.06 → Garantía estructural: `advanceRotation()` solo se invoca desde `closeSession()`. Sin código adicional
- CA-09.07 → Garantía estructural: no existe funcionalidad de edición/eliminación post-cierre (ADR D-03). Sin código adicional
- CA-09.08 → Ya implementado: `database.withTransaction` garantiza rollback ante crash + B1 ya detecta `IN_PROGRESS` y muestra card de reanudación (HU-05). Sin código adicional
<!-- AUDITORÍA PROFUNDA (2026-02-15):
Scope: 7 docs arquitectura + 5 docs business + 32 HUs + Mapa de Historias + 13 archivos de código fuente.
Docs cruzados: Modelo de Datos §2 (datos calculados), §3.10 (session status/tonelaje), §3.14 (rotation_state avance), ADR D-03 (inmutabilidad), ADR D-04 (E4 dialog), ADR-06 (Kotlin puro), Arquitectura Técnica §4.3 filas 12-13 (E4 dialog/E5 route), Wireframes E4 (2 variantes Caso A/B), Especificación Visual §8 E4 (Primary vs Error), Mapa de Navegación (E4→HU-09, E5→HU-13), Requerimientos RF18/RF19/RF20/RF21/RNF10, MDS §4/§5 (sesión completada).
HUs cruzadas (14 con referencias): HU-05 (18 refs: stubs BackHandler/botón/onNavigateToSessionSummary, RotationStateDao.update(), crash recovery B1 ✅), HU-06 (6 refs: exercise_set para status COMPLETED/INCOMPLETE, SharedFlow pattern ✅), HU-07 (4 refs: sustituto transparente, AlertDialog pattern ✅), HU-08 (3 refs: HU-09 trigger → HU-10 lógica ✅), HU-10 (2 refs: progresión al cerrar ✅ — sucesor), HU-11 (3 refs: doble umbral al cerrar ✅ — sucesor), HU-12 (3 refs: regresión al cerrar ✅ — sucesor), HU-13 (4 refs: resumen post-sesión E5, tonelaje derivado ✅ — sucesor), HU-14 (1 ref: meseta — indirecto via HU-10), HU-16 (1 ref: fatiga módulo — indirecto via HU-10/12), HU-17 (3 refs: deload congela versiones ✅ — sucesor), HU-18 (2 refs: microcycleCount B1 ✅ — sucesor), HU-21 (1 ref: COMPLETED+INCOMPLETE ✅ — consumidor), HU-24 (2 refs: historial tonelaje ✅ — consumidor).
Mapa de Historias: 7 referencias verificadas (RF18/RF19/RF20/RF21/RNF10 trazabilidad ✅).
Código verificado (13 archivos): RotationResolver.kt (23 líneas, advanceRotation() será agregada ✅), RotationState.kt (9 líneas, 5 campos ✅), SessionDao.kt (50 líneas, getActiveSessionWithModuleVersion() disponible ✅), SessionRepository.kt (22 líneas, interface ✅), SessionRepositoryImpl.kt (267 líneas, patrón withTransaction+.first() en startSession() L67 ✅), ActiveSessionUiState.kt (32 líneas, completedCount/totalCount existentes ✅), ActiveSessionViewModel.kt (77 líneas, combine overwrite issue documentado ✅), ActiveSessionScreen.kt (426 líneas, stubs L63/L97 confirmados ✅), NavigationRoutes.kt (27 líneas, SESSION_SUMMARY no existe aún ✅), TensionNavHost.kt (312 líneas, stub L264 confirmado, showBottomBar no necesita cambio ✅), strings.xml (159 líneas, session_close existe L46 ✅), RotationStateEntity.kt (27 líneas, 6 campos ✅), RotationStateDao.kt (21 líneas, update() disponible ✅).
Patrones verificados: SharedFlow(replay=0) en RegisterSetViewModel L35 y SubstituteExerciseViewModel L50; AlertDialog por ViewModel state en SubstituteExerciseScreen L156-190; isSaving flag en RegisterSetViewModel L115; database.withTransaction en SessionRepositoryImpl L66/L202/L250; LaunchedEffect+collect en SubstituteExerciseScreen L46-48.

RESULTADO FINAL: 0 HIGH, 0 MEDIUM, 0 LOW, 6 INFO (todas correctas, ninguna requiere corrección). Refinamiento APROBADO. -->